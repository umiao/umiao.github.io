<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <author>
    <name>Blur - Shenghui Xu</name>
  </author>
  <generator uri="https://hexo.io/">Hexo</generator>
  <id>https://umiao.github.io/</id>
  <link href="https://umiao.github.io/" rel="alternate"/>
  <link href="https://umiao.github.io/atom.xml" rel="self"/>
  <rights>All rights reserved 2026, Blur - Shenghui Xu</rights>
  <subtitle>Hearing the fall of snow.</subtitle>
  <title>The blog of Blur</title>
  <updated>2026-03-10T03:08:47.976Z</updated>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Life" scheme="https://umiao.github.io/categories/Life/"/>
    <category term="Baking" scheme="https://umiao.github.io/categories/Life/Baking/"/>
    <category term="Cake" scheme="https://umiao.github.io/tags/Cake/"/>
    <category term="Baking" scheme="https://umiao.github.io/tags/Baking/"/>
    <category term="Inspiration" scheme="https://umiao.github.io/tags/Inspiration/"/>
    <content>
      <![CDATA[<style>/* Smooth page scroll */html { scroll-behavior: smooth; }/* Category sections */.cake-section {  margin: 2em 0;  padding: 0.8em 1.2em;  border-left: 4px solid #f8b4c8;  background: linear-gradient(135deg, #fff5f7 0%, #ffffff 100%);  border-radius: 0 8px 8px 0;}.cake-section h2 { margin-top: 0; }/* Gallery grid */.cake-gallery {  display: grid;  grid-template-columns: repeat(3, 1fr);  gap: 14px;  margin: 1em 0;  align-items: start;}/* Each card -- scroll-unfurl on hover */.cake-item {  position: relative;  overflow: hidden;  border-radius: 10px;  box-shadow: 0 2px 8px rgba(0,0,0,0.08);  background: #fff;  max-height: 260px;  transition: max-height 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94),              box-shadow 0.3s;  z-index: 1;}.cake-item:hover {  max-height: 800px;  box-shadow: 0 8px 24px rgba(0,0,0,0.18);  z-index: 10;}/* Image -- natural size, clipped by card */.cake-item img {  width: 100%;  display: block;  transition: filter 0.3s;}.cake-item:hover img {  filter: brightness(1.05);}/* Reset constraints when mediumzoom opens the image */img.medium-zoom-image--opened {  max-height: none !important;  object-fit: contain !important;  border-radius: 0 !important;}/* Caption overlay -- rides the bottom edge as card unfurls */.cake-item .caption {  position: absolute;  bottom: 0; left: 0; right: 0;  padding: 8px 10px;  background: linear-gradient(transparent, rgba(0,0,0,0.65));  color: #fff;  font-size: 0.82em;  line-height: 1.4;  opacity: 0;  transition: opacity 0.3s;}.cake-item:hover .caption {  opacity: 1;}/* Responsive */@media (max-width: 900px) {  .cake-gallery { grid-template-columns: repeat(2, 1fr); }}@media (max-width: 500px) {  .cake-gallery { grid-template-columns: 1fr; }}</style><p>A collection of cake inspiration shared by my friend on 2026-03-09. Each image captures a unique technique or flavor combination worth revisiting. Organized by category for easy reference.</p><p><strong>Quick Nav:</strong> <a href="#1-Character-Cakes-x2F-IP%E5%8D%A1%E9%80%9A%E9%80%A0%E5%9E%8B">IP Cakes</a> | <a href="#2-Cream-Techniques-x2F-%E5%A5%B6%E6%B2%B9%E6%8A%80%E6%B3%95">Cream</a> | <a href="#3-Fruit-amp-Flavor-x2F-%E6%B0%B4%E6%9E%9C%E4%B8%8E%E5%8F%A3%E5%91%B3">Fruit</a> | <a href="#4-Innovative-Structures-x2F-%E5%88%9B%E6%96%B0%E5%B7%A5%E8%89%BA">Structure</a> | <a href="#5-Artistic-Styles-x2F-%E8%89%BA%E6%9C%AF%E9%A3%8E%E6%A0%BC">Art</a></p><hr><div class="cake-section"><h2 id="1-Character-Cakes-x2F-IP卡通造型"><a href="#1-Character-Cakes-x2F-IP卡通造型" class="headerlink" title="1. Character Cakes &#x2F; IP卡通造型"></a>1. Character Cakes &#x2F; IP卡通造型</h2><p>各种可爱的卡通IP造型蛋糕，包括Jellycat系列、Sanrio角色和动物造型。</p><div class="cake-gallery"><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/jellycat%20%E5%B8%83%E4%B8%81%E7%8B%97.png" class="" title="Jellycat布丁狗"><div class="caption">Jellycat布丁狗 -- 毛绒质感奶油造型</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/jellycat%E6%A0%97%E5%AD%90.png" class="" title="Jellycat栗子"><div class="caption">Jellycat栗子 -- 立体栗子玩偶造型</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/jellycat%E8%9C%A1%E7%83%9B.png" class="" title="Jellycat蜡烛"><div class="caption">Jellycat蜡烛 -- 蜡烛玩偶装饰</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/loopy.png" class="" title="Loopy"><div class="caption">Loopy -- 粉色小海狸造型</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E5%BA%93%E6%B4%9B%E7%B1%B3.png" class="" title="库洛米"><div class="caption">库洛米 -- 经典紫黑配色</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E5%BA%93%E6%B4%9B%E7%B1%B3%20%E9%BB%91%E8%89%B2.png" class="" title="库洛米黑色"><div class="caption">库洛米黑色 -- 暗黑风格版本</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E7%8E%89%E6%A1%82%E7%8B%97.png" class="" title="玉桂狗"><div class="caption">玉桂狗 -- Sanrio经典角色</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E7%90%B3%E5%A8%9C%E8%B4%9D%E5%B0%94.png" class="" title="琳娜贝尔"><div class="caption">琳娜贝尔 -- 迪士尼粉色小狐狸</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E7%BF%BB%E7%B3%96hello%20kitty.png" class="" title="翻糖Hello Kitty"><div class="caption">翻糖Hello Kitty -- 需要翻糖技法</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E5%A5%B6%E6%B2%B9%E5%B0%8F%E7%8B%97.png" class="" title="奶油小狗"><div class="caption">奶油小狗 -- 奶油裱花小狗造型</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E5%B0%8F%E7%8B%97%E8%9B%8B%E7%B3%95%E9%80%A0%E5%9E%8B.png" class="" title="小狗蛋糕造型"><div class="caption">小狗蛋糕造型 -- 立体小狗整体造型</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E6%A0%97%E5%AD%90%E5%B0%8F%E7%8B%97.png" class="" title="栗子小狗"><div class="caption">栗子小狗 -- 栗子色系小狗</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E5%A4%A7%E9%B9%85%E5%A5%B6%E6%B2%B9%E9%80%A0%E5%9E%8B.png" class="" title="大鹅奶油造型"><div class="caption">大鹅奶油造型 -- 立体大鹅奶油塑形</div></div></div></div><hr><div class="cake-section"><h2 id="2-Cream-Techniques-x2F-奶油技法"><a href="#2-Cream-Techniques-x2F-奶油技法" class="headerlink" title="2. Cream Techniques &#x2F; 奶油技法"></a>2. Cream Techniques &#x2F; 奶油技法</h2><p>奶油抹面、调色、裱花等核心手法参考。</p><div class="cake-gallery"><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E4%B8%89%E8%89%B2%E5%A5%B6%E6%B2%B9.png" class="" title="三色奶油"><div class="caption">三色奶油 -- 三种颜色奶油分层/拼接</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E5%8F%8C%E5%B1%82%E5%BC%82%E8%89%B2%E5%A5%B6%E6%B2%B9%E6%8A%B9%E9%9D%A2%E5%88%AE%E8%8A%B1.png" class="" title="双层异色奶油抹面刮花"><div class="caption">双层异色奶油抹面刮花 -- 两色奶油抹面后刮出纹理</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E6%B8%90%E5%8F%98%E5%A5%B6%E6%B2%B9%20%E6%8A%B9%E8%8C%B6%E6%92%92%E9%9D%A2.png" class="" title="渐变奶油抹茶撒面"><div class="caption">渐变奶油抹茶撒面 -- 奶油渐变色+抹茶粉撒面</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E7%A2%A7%E8%9E%BA%E6%98%A5%E5%A5%B6%E6%B2%B9.png" class="" title="碧螺春奶油"><div class="caption">碧螺春奶油 -- 碧螺春茶粉调味奶油</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E9%BE%99%E4%BA%95%E8%8C%B6%E5%A5%B6%E6%B2%B9.png" class="" title="龙井茶奶油"><div class="caption">龙井茶奶油 -- 龙井茶风味奶油</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E7%BA%A2%E8%B1%86%E5%86%85%E9%99%B7%20%E8%9E%BA%E6%97%8B%E6%8A%B9%E9%9D%A2.png" class="" title="红豆内馅螺旋抹面"><div class="caption">红豆内馅螺旋抹面 -- 螺旋纹路抹面技法+红豆夹心</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E5%8F%82%E8%80%83%E5%A5%B6%E6%B2%B9%E9%9C%9C%E9%80%A0%E5%9E%8B.png" class="" title="参考奶油霜造型"><div class="caption">参考奶油霜造型 -- 奶油霜造型灵感参考</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E8%9D%B4%E8%9D%B6%E4%BF%AE%E8%83%9A.png" class="" title="蝴蝶修胚"><div class="caption">蝴蝶修胚 -- 蝴蝶造型的修胚手法</div></div></div></div><hr><div class="cake-section"><h2 id="3-Fruit-amp-Flavor-x2F-水果与口味"><a href="#3-Fruit-amp-Flavor-x2F-水果与口味" class="headerlink" title="3. Fruit &amp; Flavor &#x2F; 水果与口味"></a>3. Fruit &amp; Flavor &#x2F; 水果与口味</h2><p>各种水果搭配与风味组合灵感。</p><div class="cake-gallery"><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E6%8A%B9%E8%8C%B6%E6%8A%B1%E6%8A%B1%E8%8E%93.png" class="" title="抹茶抱抱莓"><div class="caption">抹茶抱抱莓 -- 抹茶+草莓的经典搭配</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E6%8A%B9%E8%8C%B6%E6%B3%B0%E8%8A%92.png" class="" title="抹茶泰芒"><div class="caption">抹茶泰芒 -- 抹茶+泰国芒果组合</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E6%9D%A8%E6%9E%9D%E7%94%98%E9%9C%B2.png" class="" title="杨枝甘露"><div class="caption">杨枝甘露 -- 经典港式甜品风味蛋糕</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E5%9B%BA%E4%BD%93%E6%9D%A8%E6%9E%9D%E7%94%98%E9%9C%B2.png" class="" title="固体杨枝甘露"><div class="caption">固体杨枝甘露 -- 杨枝甘露的固体化呈现</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E6%B3%B0%E8%8A%92%E4%BA%86.png" class="" title="泰芒了"><div class="caption">泰芒了 -- 泰式芒果风味</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E8%8D%89%E8%8E%93%E5%A4%9A%E5%B7%B4%E8%83%BA.png" class="" title="草莓多巴胺"><div class="caption">草莓多巴胺 -- 多巴胺配色草莓蛋糕</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E8%8D%89%E8%8E%93%E6%99%B4%E7%8E%8B%EF%BC%88%E9%85%B8%E5%A5%B6%E5%A5%B6%E6%B2%B9%EF%BC%89.png" class="" title="草莓晴王"><div class="caption">草莓晴王 -- 草莓+晴王葡萄，酸奶奶油基底</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E8%8D%89%E8%8E%93%E9%85%B8%E5%A5%B6%EF%BC%88%E9%85%B8%E5%A5%B6%E5%A5%B6%E6%B2%B9%EF%BC%89.png" class="" title="草莓酸奶"><div class="caption">草莓酸奶 -- 酸奶奶油基底的草莓蛋糕</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E8%8A%92%E6%9E%9C%E8%8A%8B%E6%B3%A5%E9%80%A0%E5%9E%8B.png" class="" title="芒果芋泥造型"><div class="caption">芒果芋泥造型 -- 芒果+芋泥双层口味</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E8%8A%92%E6%9E%9C%E8%A1%80%E7%B3%AF%E7%B1%B3.png" class="" title="芒果血糯米"><div class="caption">芒果血糯米 -- 芒果搭配血糯米内馅</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E8%8A%9D%E8%8A%9D%E8%8E%93%E8%8E%93%E8%8A%B1.png" class="" title="芝芝莓莓花"><div class="caption">芝芝莓莓花 -- 芝士+莓果+花朵装饰</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E8%93%9D%E8%8E%93%E5%A4%9A%E5%A4%9A.png" class="" title="蓝莓多多"><div class="caption">蓝莓多多 -- 蓝莓+养乐多风味</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E8%93%9D%E8%8E%93%E5%A4%9A%E5%A4%9A%E5%B7%B4%E6%96%AF%E5%85%8B.png" class="" title="蓝莓多多巴斯克"><div class="caption">蓝莓多多巴斯克 -- 蓝莓养乐多口味巴斯克</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E8%8E%93%E6%9E%9C%E5%B7%A7%E5%BC%80%E5%BF%83.png" class="" title="莓果巧开心"><div class="caption">莓果巧开心 -- 莓果+巧克力+开心果</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E8%9C%9C%E7%93%9C%E5%A4%B9%E5%BF%83.png" class="" title="蜜瓜夹心"><div class="caption">蜜瓜夹心 -- 蜜瓜果肉夹心</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E5%8F%82%E8%80%83%E6%B0%B4%E6%9E%9C%E8%9B%8B%E7%B3%95%E6%90%AD%E9%85%8D.png" class="" title="参考水果蛋糕搭配"><div class="caption">参考水果蛋糕搭配 -- 水果装饰搭配参考</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E6%B0%B4%E6%9E%9C%E5%88%87%E5%9D%97%E4%B8%8E%E8%A3%85%E9%A5%B0.png" class="" title="水果切块与装饰"><div class="caption">水果切块与装饰 -- 水果切法和摆盘技巧</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E7%B3%AF%E7%B1%B3%E6%B3%B0%E6%A4%B0.png" class="" title="糯米泰椰"><div class="caption">糯米泰椰 -- 糯米+泰式椰浆</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E8%8A%AD%E4%B9%90%E5%B8%83%E8%95%BE.png" class="" title="芭乐布蕾"><div class="caption">芭乐布蕾 -- 芭乐+焦糖布蕾</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E8%8A%AD%E4%B9%90%E9%85%B8%E5%A5%B6%E5%B7%B4%E6%96%AF%E5%85%8B.png" class="" title="芭乐酸奶巴斯克"><div class="caption">芭乐酸奶巴斯克 -- 芭乐酸奶口味巴斯克</div></div></div></div><hr><div class="cake-section"><h2 id="4-Innovative-Structures-x2F-创新工艺"><a href="#4-Innovative-Structures-x2F-创新工艺" class="headerlink" title="4. Innovative Structures &#x2F; 创新工艺"></a>4. Innovative Structures &#x2F; 创新工艺</h2><p>结构创新、特殊工艺和材料需求。</p><div class="cake-gallery"><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E5%88%9B%E6%96%B0%E7%82%B9%EF%BC%9A%E5%B8%83%E4%B8%81%E3%80%81%E5%B7%B4%E6%96%AF%E5%85%8B%E5%A4%B9%E5%B1%82.png" class="" title="布丁巴斯克夹层"><div class="caption">创新点：布丁、巴斯克夹层 -- 在蛋糕中嵌入布丁和巴斯克层</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E6%A0%91%E8%8E%93%E8%8A%92%E6%9E%9C%E5%8F%8C%E6%8B%BC%E5%B7%B4%E6%96%AF%E5%85%8B.png" class="" title="树莓芒果双拼巴斯克"><div class="caption">树莓芒果双拼巴斯克 -- 双口味拼接巴斯克</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E6%9D%8F%E4%BB%81%E6%8C%9E%E5%BA%95%E3%80%81%E5%BC%80%E5%BF%83%E6%9E%9C%E6%B7%8B%E9%9D%A2%E3%80%81%E5%8D%83%E5%B1%82%E5%8D%B7%E5%86%85%E9%A6%85.png" class="" title="杏仁挞底开心果淋面千层卷内馅"><div class="caption">杏仁挞底+开心果淋面+千层卷内馅 -- 三重工艺叠加</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E5%B7%A7%E5%85%8B%E5%8A%9B%E8%84%86%E7%8F%A0%E5%92%8C%E6%B7%8B%E9%9D%A2.png" class="" title="巧克力脆珠和淋面"><div class="caption">巧克力脆珠和淋面 -- 脆珠口感+淋面技法</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E7%A2%A7%E6%A0%B9%E6%9E%9C%E8%B4%B4%E9%9D%A2.png" class="" title="碧根果贴面"><div class="caption">碧根果贴面 -- 碧根果片贴面装饰</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E5%BC%82%E5%BD%A2%E4%B9%A6%E6%9C%AC.png" class="" title="异形书本"><div class="caption">异形书本 -- 书本造型异形蛋糕</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E9%9C%80%E8%A6%81%E7%99%BD%E5%B7%A7%E5%85%8B%E5%8A%9B%E6%A8%A1%E5%85%B7.png" class="" title="需要白巧克力模具"><div class="caption">需要白巧克力模具 -- 需准备白巧克力模具</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E9%9C%80%E8%A6%81%E7%BF%BB%E7%B3%96%E8%A3%85%E9%A5%B0.png" class="" title="需要翻糖装饰"><div class="caption">需要翻糖装饰 -- 需掌握翻糖装饰技法</div></div></div></div><hr><div class="cake-section"><h2 id="5-Artistic-Styles-x2F-艺术风格"><a href="#5-Artistic-Styles-x2F-艺术风格" class="headerlink" title="5. Artistic Styles &#x2F; 艺术风格"></a>5. Artistic Styles &#x2F; 艺术风格</h2><p>具有艺术感的装饰风格和整体设计。</p><div class="cake-gallery"><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E5%A4%95%E9%98%B3.png" class="" title="夕阳"><div class="caption">夕阳 -- 夕阳渐变色彩意境</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E6%B2%B9%E7%94%BB%E7%8E%AB%E7%8F%91.png" class="" title="油画玫珑"><div class="caption">油画玫珑 -- 油画风格+玫珑瓜元素</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E7%BB%BD%E6%94%BE.png" class="" title="绽放"><div class="caption">绽放 -- 花朵绽放造型</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E7%88%B1%E5%BF%83%E8%8A%B1%E7%AF%AE.png" class="" title="爱心花篮"><div class="caption">爱心花篮 -- 爱心+花篮装饰组合</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E8%8A%AD%E8%95%BE%E5%B0%91%E5%A5%B3-%E5%A4%96%E9%83%A8%E4%BA%A4%E5%8F%89%E7%BD%91%E7%BA%B9%E6%88%96%E8%A3%99%E8%BE%B9%E5%A5%B6%E6%B2%B9%E8%A3%85%E9%A5%B0.png" class="" title="芭蕾少女"><div class="caption">芭蕾少女 -- 外部交叉网纹或裙边奶油装饰</div></div><div class="cake-item"><img src="/2026/03/09/Cake-Inspiration-Gallery/%E8%87%B3%E8%87%BB%E6%A8%A1%E6%9D%BF.png" class="" title="至臻模板"><div class="caption">至臻模板 -- 高级感模板参考</div></div></div></div><hr><blockquote><p>Thanks to my friend for sharing these wonderful cake inspirations!</p></blockquote>]]>
    </content>
    <id>https://umiao.github.io/2026/03/09/Cake-Inspiration-Gallery/</id>
    <link href="https://umiao.github.io/2026/03/09/Cake-Inspiration-Gallery/"/>
    <published>2026-03-09T07:00:00.000Z</published>
    <summary>
      <![CDATA[<style>
/* Smooth page scroll */
html { scroll-behavior: smooth; }

/* Category sections */
.cake-section {
  margin: 2em 0;
  padding:]]>
    </summary>
    <title>Cake Inspiration Gallery</title>
    <updated>2026-03-10T03:08:47.976Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Cyber Security" scheme="https://umiao.github.io/tags/Cyber-Security/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Data Security" scheme="https://umiao.github.io/tags/Data-Security/"/>
    <content>
      <![CDATA[<p>Notes and revisits of data &amp; cyber security training session.</p><img src="/2024/09/15/Data-Security-Training/cover.jpg" class="" title="ML_note"><span id="more"></span><h2 id="SQL-Injection"><a href="#SQL-Injection" class="headerlink" title="SQL Injection"></a>SQL Injection</h2><h3 id="Example-of-attack"><a href="#Example-of-attack" class="headerlink" title="Example of attack"></a>Example of attack</h3><p><code>SELECT * FROM Users WHERE Username=&#39;admin&#39; AND Password = abc&#39;OR 1=1;--</code><br>Here, “abc’OR1&#x3D;1;–” is the constructed malicious input, and <code>--</code> marks the end of line.</p><p><strong>Solution</strong>: Use <strong>parameterized</strong> query.</p><ol><li><strong>Wrong Code</strong>: Straight forward concatenantion (trust user’s input as part of the SQL command) like <code>VALUES(request_user_name&quot;,&quot;+request_user_age+&quot;&quot;)</code></li><li><strong>Correct Code</strong>: Parameterized query. <code>insert_user = db.prepare &quot;INSERT INTO users(name, age) VALUES(?,?)&quot;</code><br><code>insert_user.execute(request_user_name, request_user_age)</code></li></ol><h3 id="Other-ways-of-injection-attack"><a href="#Other-ways-of-injection-attack" class="headerlink" title="Other ways of injection attack"></a>Other ways of injection attack</h3><p><strong>Union attack</strong>: Use <code>UNION</code> to extend the results returned by the original query. It requires both queries to contail the <strong>same</strong> amount of columns.</p><h3 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h3><ol><li>All popular dev frameworks have <strong>secure construction</strong> of db queries.</li><li>Use <strong>allowist</strong> validation on all user input (e.g. ban the <code>&#39;</code>)</li><li>Apply <strong>least privilege</strong> principle on all backend db users.</li><li>Consider GET and POST parameters, Cookies and other HTTP headers.</li></ol><h2 id="Cross-Site-Scripting-XSS"><a href="#Cross-Site-Scripting-XSS" class="headerlink" title="Cross-Site Scripting (XSS)"></a>Cross-Site Scripting (XSS)</h2><p>The user recognized a valid and known host name is the URL, clicks on the link and surfs to the URL. The website does not validate or encode the params before rendering &#x2F; reflecting it back to the user. Then, the browser may execute the injected Javascript and redirects the user to a phising site, tricking them into submitting their passwords.</p><p><strong>Prevention</strong>:</p><ol><li>Never trsut the user input. All user input rendered on the application output should be validated or encoded.</li><li>If you do not validate the user input, unescaped input would be dispalyed in an immediate response to the user. This would allow attacker to perform actions within the application on behalf of other users.</li><li>Frameworks offer specific API calls to protect the application from XSS by HTML encoding untrusted user input. </li><li>Cookies should be securely configured.</li><li>Set the <code>HttpOnly</code> flag to prevent scripts from accessing them.<br>* Note that you should not mark strings as “safe”, e.g. <code>&#123;&#123;recipe_searchedIssafe&#125;&#125;</code>; Otherwise, the engine would escape the user input so that they would not got executed &#x2F; interpreted as html &#x2F; js.</li></ol><h2 id="OS-Command-Injection"><a href="#OS-Command-Injection" class="headerlink" title="OS Command Injection"></a>OS Command Injection</h2><p>Such vulnerability can happen when user controlled input, through parameters, cookies, http headers, etc are passed to the system shell without any prior validation.</p><p><strong>Example</strong>: If the application simply appends the GET params to the command string, the malicious command would got executed. Note that in this case, the command would run with the privilege of the <strong>running application</strong>.<br>Note that many of the characters needs to be “url encoded” in order to be transferred and parsed correctly.</p><p><strong>Solution</strong>:</p><ol><li>User framework specific API calls instead of OS commands. If not possible, validate all user controlled output against a <strong>white-list</strong> before passing to the shell. </li><li>Apply <strong>least privilege</strong> to the application.</li><li><strong>Never</strong> set <code>shell=True</code>. This would bypass the validation and have it executed in the shell directly.</li><li>Can make use of “check_output” to validate if the user provided part is a shell parameter. If not, then you should fail it.</li></ol><hr><p><strong>Remote File Inclusion</strong>: Open any file &#x2F; Iclude in the page makes the application vulnearable to remote file inclusion injection.</p><p>An adversary could upload a malicious script to be executed on the client-side server.<br>You can specify it as “text&#x2F;plain” also set security policy to solve the issue.</p><hr><p><strong>File Traverse</strong>:<br>Using the URL GET parameter to open local files makes the application vulnerable to <strong>Path Traversal</strong> attacks.</p><p>An adversary could modify the iamge parameter path to include any server file in the application page (e.g., <code>?image=/etc/passwd</code>) </p><p><strong>Solution</strong>: Remove file response. Also not open server files from URL GET params.</p><h2 id="Weak-Session-Token-Generation"><a href="#Weak-Session-Token-Generation" class="headerlink" title="Weak Session Token Generation"></a>Weak Session Token Generation</h2><p>If the generation is too weak, an attacker may easily associate to another active user’s session with constructed ID.<br>Use <strong>built-in</strong> session management functionalities instead of inventing your own.</p><ul><li>Store the Session ID in a cookie and then protect session cookies. This can be done by setting an <strong>expiry timestamp</strong>, path, “secure” and “HttpOnly” flag and <strong>invalidate</strong> on logout.</li><li>Session ID properties must be secure. Make them <strong>unpredictable</strong>, <strong>time limited</strong> and <strong>single session</strong>.</li><li>Use a secure communication channel.</li></ul><hr><h2 id="Missing-Function-Level-Access-Control"><a href="#Missing-Function-Level-Access-Control" class="headerlink" title="Missing Function Level Access Control"></a>Missing Function Level Access Control</h2><p><strong>Definition</strong>: user can perform functions that they are not authorized for, or when resources can be accessed by <strong>unauthorized</strong> users.</p><p>When access checks have not been implemented, or when a protection mechanism exists but is not properly configured.</p><p><strong>Solution</strong>:</p><ol><li>Protect all business functions using a <strong>role based</strong> authorization mechanism. Implement it on the server side.</li><li>Authorization should be applied using <strong>centrailized routines</strong>, provided by the framework or external modules.</li></ol>]]>
    </content>
    <id>https://umiao.github.io/2024/09/15/Data-Security-Training/</id>
    <link href="https://umiao.github.io/2024/09/15/Data-Security-Training/"/>
    <published>2024-09-16T02:55:57.000Z</published>
    <summary>
      <![CDATA[Notes and revisits of data & cyber security training session.]]>
    </summary>
    <title>
      <![CDATA[Data & Cyber Security Training Notes]]>
    </title>
    <updated>2026-03-10T01:09:06.999Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Job Search" scheme="https://umiao.github.io/categories/Job-Search/"/>
    <category term="Software Engineering" scheme="https://umiao.github.io/categories/Job-Search/Software-Engineering/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Git" scheme="https://umiao.github.io/tags/Git/"/>
    <category term="Version Control" scheme="https://umiao.github.io/tags/Version-Control/"/>
    <content>
      <![CDATA[<p>Thorough analysis and learning note based on git documentation (<a href="https://git-scm.com/book/en/v2">https://git-scm.com/book/en/v2</a>).</p><img src="/2024/08/06/Introduction-to-Git/cover.jpg" class="" title="ML_note"><span id="more"></span><ul><li><strong>Patches set</strong>: the differences between files &#x2F; versions; can be used to recreate files</li><li><strong>Centralized Version Control Systems</strong>: single server contains all versioned files. Clients checkout file from central place. Prone to <em>single point failure</em>.</li><li><strong>Distributed Version Control Systems</strong>: clients fully mirror the repository and history.</li></ul><hr><p>Git is <strong>snapshot</strong> based (store and index changed files), rather than difference based (storing the delta &#x2F; change of files).<br>Near all operation is local.<br>Integrity is enforced by <strong>checksum</strong> (empowered by <strong>SHA-1</strong> hash). Git will be able to detect change &#x2F; corruption.<br>Git generally only adds data. committed changes are hard to lost.</p><h3 id="Three-status"><a href="#Three-status" class="headerlink" title="Three status"></a>Three status</h3><ol><li><strong>Modified</strong>: file changed, not committed</li><li><strong>Staged</strong>: marked a modified file in its current version to go into your next commit snapshot</li><li><strong>Committed</strong>: data is safely stored in your local database</li></ol><p>These states lead us to main sections of git project: <strong>working tree</strong>, <strong>staging area</strong>, <strong>git directory</strong>.</p><img src="/2024/08/06/Introduction-to-Git/1.jpg" class="" title="ML_note"><hr><p>Installation: refer to this <a href="https://git-scm.com/book/en/v2/Getting-Started-Installing-Git">link</a>.</p><hr><h3 id="Git-Config"><a href="#Git-Config" class="headerlink" title="Git Config"></a>Git Config</h3><ol><li><strong>Configuration Overwrite</strong>: <code>.git/config</code> (local project) will overwrite (<code>~/.gitconfig </code> &#x2F; <code> ~/.config/git/config</code>, current user) and will overwrite <code>/etc/gitconfig</code> (system level).</li><li>Show where setting comes from: <code>git config --list --show-origin</code></li><li>Setting up user email:<br><code>git config --global user.name &quot;John Doe&quot;</code><br><code>git config --global user.email johndoe@example.com</code><br>(remove <code>--global</code> to make local setting only)</li><li>Check all configuration: <code>git config --list</code></li></ol><hr><p>Use <code>git help</code> for help page.<br>You can use <code>git COMMAND -h</code> for brief help message, like <code>git add -h</code>.  <code>git help add</code> will be more comprehensive.</p><hr><h3 id="Git-Repository-Initialize"><a href="#Git-Repository-Initialize" class="headerlink" title="Git Repository Initialize"></a>Git Repository Initialize</h3><ul><li>Create repository from scratch:</li></ul><p><code>cd /Users/user/my_project</code><br><code>cd /Users/user/my_project</code><br><code>git add *.c</code><br><code>git add LICENSE</code><br><code>git commit -m &#39;initial project version&#39;</code></p><hr><ul><li>Clone existing repo:<br><code>git clone https://github.com/libgit2/libgit2 (optional_alias)</code></li></ul><hr><h3 id="Git-Status"><a href="#Git-Status" class="headerlink" title="Git Status"></a>Git Status</h3><p>Use <code>git status</code> to check the status of your files.<br>You can check if any file is <code>untracked</code> (can be tracked via <code>git add</code>) or change <code>uncommitted</code>.</p><p>You can use <code>git status -s</code> to get more <strong>compacted</strong> output.</p><hr><h3 id="Git-Add"><a href="#Git-Add" class="headerlink" title="Git Add"></a>Git Add</h3><p><strong>Example</strong>: <code>git add &lt;files&gt;</code>, the input can be either directory or file name.<br>This command can <strong>track</strong> new files, <strong>stage</strong> files and mark merge-conflict files as <strong>resolved</strong>.<br>*Note that <code>git add</code> will only stage the snapshot of a file by the time “add” command is executed. If changes happended later, we need to add again.</p><p><strong>Ignoring files</strong>: for log or temp files which we do not want Git to automatically add or even show as “untracked”, we can configure in <strong>.gitignore</strong> file.</p><p>Rules of writing <code>.gitignore</code> file:</p><ol><li>Blank lines or lines starting with # are ignored.</li><li>Standard glob (shell simplified regular expression) patterns work, and will be applied recursively throughout the entire working tree.</li><li>You can start patterns with a forward slash (&#x2F;) to avoid recursivity.</li><li>You can end patterns with a forward slash (&#x2F;) to specify a directory.</li><li>You can negate a pattern by starting it with an exclamation point (!).</li></ol><p>For more examples of <code>.gitignore</code>, please check <a href="https://github.com/github/gitignore">here</a>.</p><hr><h3 id="Git-Diff"><a href="#Git-Diff" class="headerlink" title="Git Diff"></a>Git Diff</h3><ol><li><code>git diff</code>:<br>Inspect what have <strong>changed</strong> but not yet <strong>staged</strong>.</li><li><code>git diff --staged</code> or <code>git diff --cached</code>:<br>Inspect what you’ve <strong>staged</strong> that will go into your next <strong>commit</strong>.</li></ol><hr><h3 id="Git-Commit"><a href="#Git-Commit" class="headerlink" title="Git Commit"></a>Git Commit</h3><ol><li><code>git commit</code>: will run the default text editor for creating a message for the commit, and the changes to be committed will be shown.</li><li>Use: <code>git commit -m &quot;YOUR MESSAGE&quot;</code> can create the commit with message in one line.</li><li><code>git commit -a -m &#39;YOUR MESSAGE&#39;</code> can skip the stage area without manually adding all the changed files. However, we should be careful before doing this (should check <code>git status</code>).</li></ol><hr><h3 id="Removing-Files"><a href="#Removing-Files" class="headerlink" title="Removing Files"></a>Removing Files</h3><ol><li><code>git rm</code> will remove a file from your tracked files (more accurately, remove it from your staging area) and then commit. The file will also be removed from your working directory.<br> <strong>Example</strong>: <code>git rm YOUR_FILE_NAME</code></li><li>To remove a <strong>edited &#x2F; staged</strong> file, we need to add <code>-f</code> (force removal) to prevent accidental removal of data that hasn’t been recorded in a snapshot.</li><li><code>git rm --cached YOUR_FILE_NAME</code> can remove (accidentally staged) files from <strong>staging area</strong> but keep the file in the working tree.<br> <strong>Example</strong>: If you forget to add log files or compiled files to the <code>.gitignore</code> file and accidentally staged.</li><li>You can pass files, directories, and file-glob patterns to the git rm command.<br> <strong>Example</strong>: <code>git rm log/\*.log</code> and <code>git rm \*~</code>. Note that the <code>\</code> is needed because Git does its own filename expansion in addition to shell’s filename expansion.</li><li>We can rename file with: <code>git mv file_from file_to</code><br> This is actually equivalent to running 3 commands of: <code>mv README.md README</code>, <code>git rm README.md</code> and <code>git add README</code></li></ol><hr><h3 id="Check-commit-history"><a href="#Check-commit-history" class="headerlink" title="Check commit history"></a>Check commit history</h3><ol><li><code>git log</code>: by default,  this command lists each commit with its SHA-1 checksum, the author’s name and email, the date written, and the commit message.</li><li>Use the <code>-p</code> or <code>--patch</code> mode to show the difference of each commit in the format of <strong>patches</strong> : <code>git log -p -NUM</code>. Here <code>NUM</code> restricts to show only <code>NUM</code> entries.</li><li>Use the <code>git log --stat</code> to show some <strong>abbreviated stats</strong>.</li><li>Use <code>git log --pretty=oneline</code> to show the commit history in different <strong>formats</strong>. (also have other options like <code>short</code>, <code>full</code> and <code>fuller</code>). </li><li>We can use <code>git log --pretty=format:&quot;%h - %an, %ar : %s&quot;</code> to format the commit history and extract the information you need. E.g. <code>%h</code> shows the <strong>abbreviated hash</strong>, <code>%an</code> shows the <strong>author name</strong> and <code>%ar</code> is <strong>Author date, relative</strong> and <code>%s</code> is the subject.</li><li>Use <code>--graph</code> option to <strong>visualize</strong> the branch and merge history.<br> <strong>Example</strong>: <code>git log --pretty=format:&quot;%h %s&quot; --graph</code></li><li><strong>Time limiting</strong> options such as <code>--since</code> and <code>--until</code>. E.g., <code>git log --since=2.weeks</code>.</li><li><code>-S</code> &#x2F; <strong>pickaxe</strong> option. By using <code>git log -S function_name</code>, we will show only those commits that changed the number of occurrences of that string.</li></ol><hr><h3 id="Undo-things"><a href="#Undo-things" class="headerlink" title="Undo things"></a>Undo things</h3><ol><li>Use <code>git commit --amend</code> takes your staging area and use it for the commit. This will <strong>overwrite</strong> your previous commit.</li><li>Use <code>git reset HEAD &lt;file&gt;…</code> to <strong>unstage</strong>. In this case, it will be removed from the staging area but changes kept in working tree.<br> * It should be noted that <code>reset</code> is a dangerous command. In git, almost all <strong>commited</strong> data can be recovered. However, <strong>uncommited</strong> changes may lost forever.</li></ol><hr><h3 id="Working-with-Remotes"><a href="#Working-with-Remotes" class="headerlink" title="Working with Remotes"></a>Working with Remotes</h3><ol><li><code>git remove -v</code>: shows you the URLs that Git has stored for the shortname to be used when reading and writing to that remote.</li><li><code>git remote add &lt;shortname&gt; &lt;url&gt;</code> will add a new remote explicitly.</li><li><code>git fetch &lt;remote&gt;</code>: goes out to that remote project and pulls down all the data from that remote project that you don’t have yet. (If you used <code>clone</code> to copy a remote, it will be automatically added as remote “origin”). Also, <code>git fetch</code> will do the data downloading only without changing your work.</li><li><code>git pull</code>: automatically <strong>fetch</strong> and then <strong>merge</strong> that remote branch into your current branch.</li><li><code>git push &lt;remote&gt; &lt;branch&gt;</code> will push your changes to the upstream. (This command works only if you cloned from a server to which you have <strong>write access</strong> and if nobody has pushed in the meantime)</li><li><code>git remote show &lt;remote&gt;</code> to check more information a remote.</li><li><code>git remote rename &lt;old_name&gt; &lt;new_name&gt;</code> to <strong>rename</strong> a remote. <code>git remote remove &lt;remote&gt;</code> to <strong>remove</strong> a remote.</li></ol><hr><h3 id="Tagging"><a href="#Tagging" class="headerlink" title="Tagging"></a>Tagging</h3><p>Git has the ability to tag specific points in a repository’s history as being <strong>important</strong>.</p><ul><li><strong>Lightweight tag</strong>: a pointer to a specific commit</li><li><strong>Annotated tag</strong>: full objects, checksummed, with the tagger name, email and date and message.</li></ul><p><strong>Example</strong>: </p><ol><li>Annotated: <code>git tag -a v1.4 -m &quot;my version 1.4&quot;</code></li><li>Light-weighted: <code>git tag v1.4-lw</code></li></ol><p>Use <code>git show</code> to check the tagger information, data the commit was tagged and theannotation message. </p><p>Use <code>git tag -d &lt;tagname&gt;</code> to <strong>remove</strong> a tag.</p><p>* By default, <code>git push</code> will not transfer tags to remote. We need to do that explicityly: <code>git push origin &lt;tagname&gt;</code><br>Or we can use <code>git push origin --tags</code> to push <strong>ALL</strong> tags.<br>** if you <code>checkout</code> to a tag, your repo will be at <strong>detached HEAD</strong> status. In such case, if you make changes and then created a commit, the tag will stay the same, but your new commit will not belong to any branch and become <strong>unreachable</strong>.</p><hr><h3 id="Alias"><a href="#Alias" class="headerlink" title="Alias"></a>Alias</h3><p>We can create <strong>alias</strong> for git commands for convenience, examples:<br><code>git config --global alias.co checkout</code><br><code>git config --global alias.br branch</code><br><code>git config --global alias.unstage &#39;reset HEAD --&#39;</code><br><code>git config --global alias.last &#39;log -1 HEAD&#39;</code></p><p>To run an external command, rather than a Git subcommand, start the command with a <code>!</code> character.</p><p><code>git config --global alias.visual &#39;!gitk&#39;</code></p><hr><h1 id="Branch"><a href="#Branch" class="headerlink" title="Branch"></a>Branch</h1><p>Git stores <strong>snapshots</strong> (as <strong>BLOB</strong> objects) for files, rather than patches or diffs.</p><p>When making a commit, Git stores a commit object that contains a <strong>pointer</strong> to the snapshot of the content you staged. Also contains the author’s name and email address, the message that you typed, and pointers to the commit or commits that directly came before this commit (its <strong>parent</strong> or <strong>parents</strong>):<br>* <strong>0</strong> parents for the initial commit, <strong>1</strong> parent for a normal commit, and <strong>multiple</strong> parents for a commit that results from a merge of two or more branches.<br>** Aside from the commit information and blob obj, a <strong>tree object</strong> is used to record the directory structure and blob object reference.</p><p>A branch in Git is a <strong>movable pointer</strong> to one commit. The default branch name in Git is <code>master</code>.</p><hr><p><strong>Commands</strong>:</p><ul><li>Run <code>git branch</code> without parameters, you will see a list of branches, with the current branch marked with <code>*</code>.</li><li><code>git branch --merged</code> &#x2F; <code>git branch --no-merged</code> can show the branches which are already merged (can be safely deleted) &#x2F; not yet merged by the current branch (cannot be deleted by <code>git branch -d</code>, need to use <code>git branch -D</code>).</li></ul><h3 id="Create-a-new-branch"><a href="#Create-a-new-branch" class="headerlink" title="Create a new branch"></a>Create a new branch</h3><ol><li><code>git branch &lt;name&gt;</code>: creates a new pointer to the same commit you are currently on.</li><li>A speical pointer <code>HEAD</code> marks the current branch.<br>* Note that with <code>git branch</code>, you just created a new branch, do not checkout to that branch by default.</li><li>You can use <code>git log --decorate</code> to check which commit objects the branch pointers are pointing to.</li></ol><hr><h3 id="Checkout-to-branch"><a href="#Checkout-to-branch" class="headerlink" title="Checkout to branch"></a>Checkout to branch</h3><ol><li>Use <code>git checkout &lt;name&gt;</code> to have <code>HEAD</code> pointing to another branch (for checking out).</li><li>You can create a new branch, and checkout to that branch with: <code>git checkout -b &lt;name&gt;</code></li></ol><p>A branch in Git is actually a simple file that contains the 40 character <strong>SHA-1</strong> checksum of the commit it points to. This makes the branch creating &amp; checkout extremely efficient.</p><p>* You do not need to provide the entire 40-character SHA-1. You just need to provide <strong>at least 4 beginning characters</strong> (by default 7) (have to be <strong>unambiguous</strong>).<br>** <strong>Branch reference</strong>:  If a commit is at the tip of a branch, you can refer to it with <code>git show &lt;commit_id&gt;</code> or <code>git show &lt;branch_name&gt;</code></p><h3 id="Merge-of-branch"><a href="#Merge-of-branch" class="headerlink" title="Merge of branch"></a>Merge of branch</h3><ol><li>In case you want to switch to the master branch to add some fix while shelving the changes in your current dev branch, you can first <strong>commit</strong> and the changes and then <strong>checkout</strong> to master.</li><li>When the fix is finished, and the dev is finished after that, you can checkout to master and use <code>git merge &lt;dev_branch&gt;</code> to merge the changes.</li></ol><h3 id="Rename-of-branch"><a href="#Rename-of-branch" class="headerlink" title="Rename of branch"></a>Rename of branch</h3><ol><li>Locally: <code>git branch --move bad-branch-name corrected-branch-name</code></li><li>Globally: <code>git push --set-upstream origin corrected-branch-name</code><br>We can use <code>git branch --all</code> to check if the <strong>renaming</strong> is successful.</li></ol><hr><h2 id="Remote-branches"><a href="#Remote-branches" class="headerlink" title="Remote branches"></a>Remote branches</h2><ol><li><p>Remote-tracking branches are <strong>references</strong> to the <strong>state</strong> of remote branches. Git moves such local references (pointers) for you whenever you do network communications to make them up-to-date.</p></li><li><p>Remote-tracking branch names take the form <code>&lt;remote&gt;/&lt;branch&gt;</code></p></li><li><pre><code>Remote-tracking branches (pointers) will not move if you do **NOT** connect to remote server and pull.</code></pre></li><li><p>Use <code>git remote add &lt;remote_alia&gt; &lt;remote_url&gt;</code> to <strong>add</strong> a new remote repository for your project.</p></li><li><p><code>git checkout -b &lt;branch&gt; &lt;remote&gt;/&lt;branch&gt;</code> or <code>git checkout --track &lt;remote&gt;/&lt;branch&gt;</code>, to create a local branch which tracks the remote branch.<br> * To set the local branch to use a <strong>different</strong> name, use <code>git checkout -b local_name remote/branch</code><br> ** Use <code>-u</code> or <code>--set-upstream-to</code> to set &#x2F; update the upstream branch for an existing local branch.</p></li><li><p>Use <code>git branch -vv</code> for <strong>ALL</strong> tracked branches.<br> * This may not be updated. Run <code>git fetch --all</code> ahead.</p></li><li><p>Use <code>git push origin --delete remote_branch</code> to <strong>remove</strong> a branch from server. This can usually be recovered before garbage collection.</p></li></ol><hr><h1 id="Rebasing"><a href="#Rebasing" class="headerlink" title="Rebasing"></a>Rebasing</h1><ol><li>If <code>merge</code> is used in branch merging, a <strong>three-way merge</strong> will be performed on the 2 latest branch snapshots, and the most recent common ancestor of them 2.<br> * This will result in a <strong>new</strong> snapshot and commit.</li><li><strong>Rebasing</strong>: take the <strong>patch</strong> of the change that are introduced in dev branch, and <strong>reapply</strong> it on top of the master branch.</li><li>Rebasing works by:<br> a. Going to the common ancestor of the two branches<br> b. Getting the <strong>diff</strong> introduced by each commit of the branch you’re on<br> c. Saving those diffs to temporary files<br> d. Resetting the current branch to the same commit as the branch you are rebasing onto<br> e. Finally applying each change in turn.</li><li>Rebasing makes the commits apply <strong>cleanly</strong> and appears as the work happened in <strong>series</strong> though they are originally in parallel.</li><li>Rebasing enables a <strong>fast-forward</strong> and a clean apply.</li></ol><hr><p>It is possible to apply the generated replays &#x2F; patches to a <strong>different</strong> branch than the target branch.</p><p><code>git rebase --onto master server client</code> will:</p><ol><li>Take the client branch, figure out the patches since it <strong>diverged</strong> from the server branch</li><li>Replay these patches in the client branch as if it was based directly off the master branch instead.</li></ol><hr><h3 id="The-Perils-of-Rebasing"><a href="#The-Perils-of-Rebasing" class="headerlink" title="The Perils of Rebasing"></a>The Perils of Rebasing</h3><p>Do <strong>NOT</strong> rebase commits that exist outside your repository and that people may have based work on.</p><p>In that case, you’re <strong>abandoning</strong> existing commits and creating new ones that are similar but different.<br>If you changed the submission history in a public repo, you and cowork may face confusing results (<strong>duplicates</strong> of commit, and the deprecated commits can be found back).</p><p><strong>Solution</strong>: Ask your coworkers to <code>git pull --rebase</code>. You should not remove commits where others may be doing development on.<br><strong>* !</strong> Rebase <strong>local</strong> changes before pushing to clean up your work, but <strong>never</strong> rebase anything that you’ve <strong>pushed</strong> somewhere.</p><hr><h1 id="Git-protocols-on-Server"><a href="#Git-protocols-on-Server" class="headerlink" title="Git protocols on Server"></a>Git protocols on Server</h1><ol><li><strong>Local</strong>: shared file system based, remote drive may be slow and inconvenient. Each user will gain complete shell acess of the remote directory.</li><li><strong>HTTP</strong>: “Smart HTTP” works similar to SSH and Git, just on HTTP&#x2F;S port with autentication mechanisms. Fast and easy, can provide encrption and pass firewalls. Setting up the server end can be less convenient. We also have “Dumb HTTP” which expects the bare Git repo to be served like normal files.</li><li><strong>SSH (Secure Shell)</strong>: More common, safe and efficient. However, it does not support anonymous access to your Git repository. </li><li><strong>Git</strong>: Based on a special port 9418, <strong>NO</strong> authentication or cryptography. It is difficult to set up and requires firewall access to port 9418.</li></ol><hr><h3 id="Reflog"><a href="#Reflog" class="headerlink" title="Reflog"></a>Reflog</h3><p><strong>reflog</strong>: a log of your <code>HEAD</code> and branch references, kept for a few months. It should be noted that it is stored only <strong>locally</strong>. When a new repo is cloned, it is going to be emppty.</p><ul><li>Every time your branch tip is updated for any reason, Git stores that information for you in this temporary history. </li><li>Refer to older commits: <code>git show HEAD@&#123;5&#125;</code> to show the <strong>fifth</strong> prior value of the <code>HEAD</code> for your repo.</li><li>Use <code>git show master@&#123;yesterday&#125;</code> to check the commits which are pointed by your master branch yesterday.</li><li>Use <code>git log -g</code> to check the content of <strong>reflog</strong>, but using the format of <code>git log</code> output.</li></ul><hr><h3 id="Ancestry-References"><a href="#Ancestry-References" class="headerlink" title="Ancestry References"></a>Ancestry References</h3><ul><li>If you place a <strong>^</strong> (caret) at the end of a reference, Git resolves it to mean the <strong>parent</strong> of that commit.</li><li>git show <code>HEAD^</code> will show the parent of <code>HEAD</code>.</li><li>You can specify a number to identify which parent you want. <code>d921970^2</code> means the <strong>second parent</strong> of d921970.</li><li><code>~</code> refer to the first parent. Thus, <code>HEAD~</code> is equivalent to <code>HEAD^</code>.</li></ul><hr><h3 id="Commit-Ranges"><a href="#Commit-Ranges" class="headerlink" title="Commit Ranges"></a>Commit Ranges</h3><ol><li><strong>Double Dot</strong>: Show all commits reachable from experiment that aren’t reachable from master: <code>git log master..experiment</code>. Similarly, <code>git log experiment..master</code> shows everything in master not reachable from experiment.<br> * <code>git log origin/master..HEAD</code> Shows what is about to push to remote.</li><li><strong>Multiple Points</strong>: See what commits are in any of several branches that aren’t in the branch you’re currently on: use <code>^</code> or <code>--not</code> before any reference from which you <strong>don’t</strong> want to see reachable commits.<br> <strong>Equivalent</strong> expressions:<br> 1. <code>git log refA..refB</code><br> 2. <code>git log ^refA refB</code><br> 3. <code>git log refB --not refA</code><br> Use <code>git log refA refB ^refC</code> to see commits reachable from refA and refB, but not from refC.</li><li><strong>Triple Dot</strong>: specifies all the commits that are reachable by <strong>either</strong> of two references but not by both of them.<br> <code>git log --left-right master...experiment</code> shows which commit is unique and belongs to which branch.</li></ol><hr><h3 id="Stashing-and-Cleaning"><a href="#Stashing-and-Cleaning" class="headerlink" title="Stashing and Cleaning"></a>Stashing and Cleaning</h3><ul><li><code>git stash</code> can preserve the half-done work so you can work on other things and come back later.<br>  * <code>git stash save</code> is to be migrated to: <code>git stash push</code></li><li>Use <code>git status</code> to see if the stash command takes effect (working tree clean)</li><li><code>git stash list</code> to <strong>check</strong> the stored stashes.</li><li><code>git stash apply</code> to <strong>apply</strong> the recently stashed work (by default the <strong>most recent</strong> one)<br>  * To apply an older change, you can specify the name like <code>git stash apply stash@&#123;2&#125;</code><br>  Also the working tree should be <strong>clean</strong>; Stashed changes can be apply to another branch, if not, merge conflicts.<br>  ** The staged files will <strong>NOT</strong> be <strong>restaged</strong>. To do that, run <code>git stash apply --index</code></li><li>The <code>git stash apply</code> will not remove your stash.<br>  To <strong>remove</strong>, use <code>git stash drop stash@&#123;0&#125;</code> to drop stash by name (0 is the most recent one)<br>  <code>git stash pop</code> will apply &amp; delete the most recent stash.</li><li>By default, only <strong>tracked</strong> files will be stashed.<br>  To stash untracked files: <code>git stash -u / --include-untracked</code><br>  To stash <strong>ignored</strong> files, <code>git stash -a \ --all</code></li><li>Use <code>git stash --patch</code> to <strong>interactively</strong> determine what to stash.</li><li><code>git stash branch &lt;new branchname&gt;</code> to create a <strong>NEW</strong> branch based on the stash.</li></ul><hr><h3 id="Git-Clean"><a href="#Git-Clean" class="headerlink" title="Git Clean"></a>Git Clean</h3><ol><li>Use <code>git clean</code> to remove redundant files &#x2F; clean up working directory which are <strong>NOT ignored</strong><br> (remove <strong>untracked</strong> files &#x2F; <strong>empty</strong> dir by <code> git clean -f -d</code>)<br> (remvoe <strong>ignored</strong> files using <code>-x</code>)<br> Better <code>git stash --all</code> first, because such files may not be able to be retrieved.</li><li>Use <code>git clean -d -n / --dry-run</code> to see what it <strong>will</strong> do.</li></ol><hr><h2 id="Search"><a href="#Search" class="headerlink" title="Search"></a>Search</h2><ol><li>Use <code>git grep keyword</code> to search <strong>files</strong> in your working directory.</li><li><code>git grep -n keyword</code> to search the <strong>line number</strong>.</li><li>Use <code>-c / --count</code> to <strong>summarize</strong> the output.</li><li>Use <code>-p /  --show-function</code> to display the enclosing <strong>method or function</strong> for each matching string.</li></ol><hr><h2 id="Log-Searching"><a href="#Log-Searching" class="headerlink" title="Log Searching"></a>Log Searching</h2><ul><li>To see <strong>when</strong> a term existed or was introduced, we can use <code>-S</code>:<br><code>git log -S keyword --oneline</code>.</li><li>We can use <code>-G</code> to search with <strong>regular expression</strong>.</li><li>Use <code>-L</code> to trigger <strong>Line Log Search</strong>.<br>   <code>git log -L :FunctionName:FileName</code><br>   Git will try to figure out what the bounds of that function are and then look through the history and show us every change that was made to the function as a series of patches back to when the function was first created.</li></ul><hr><h3 id="Rewrite-History"><a href="#Rewrite-History" class="headerlink" title="Rewrite History"></a>Rewrite History</h3><ol><li><code>git commit --amend</code> to rewrite the <strong>last</strong> commit changes and message.</li><li>Use <code>git rebase -i HEAD~3</code> to rewrite the <strong>last three</strong> commits. You can also specify a <strong>branch</strong> to rewrite all the diverging commits.<br> When entering the text editor, you can change “pick” into “edit” to edit a commit, or use “squash” to merge a commit.<br> After making the changes, run <code>git commit --amend</code> and <code>git rebase --continue</code><br> You can use this method to <strong>reorder</strong> commits, or <strong>eliminate</strong> some entirely.</li><li>To rewrite huge swaths of history, you can use <strong>filter-branch</strong>, or just use the more recommended <a href="https://github.com/newren/git-filter-repo">git-filter-repo</a>.</li></ol><hr><h3 id="Reset"><a href="#Reset" class="headerlink" title="Reset"></a>Reset</h3><p><strong>Mental frame</strong> of Git: Managing 3 <strong>trees</strong> (collections of files).</p><ol><li><strong>HEAD</strong>: Last commit snapshot, next parent</li><li><strong>Index</strong>: Proposed next commit snapshot</li><li><strong>Working Directory</strong>: Sand box.</li></ol><p>After <code>git add</code>, content in working tree will be added to index. With <code>git commit</code>, content in index will be further saved as a permanent <strong>snapshot</strong>, create a commit object pointing to that snapshot and update the current <strong>branch</strong>. </p><ol><li><code>git reset --soft</code>: Move <code>HEAD</code> only to <strong>revert</strong> the last commit. This can be used to <strong>compact</strong> the commits.</li><li><code>git reset --mixed</code>: Move <code>HEAD</code> and update index to be aligned with the current <code>HEAD</code>. Aside from reverting the commit, we <strong>unstaged</strong> everything.</li><li><code>git reset --hard</code>: the working directory is eventually overwritten with the commit we reset to.<br> It is <strong>dangerous</strong> because it destroys data.</li><li>If a path is set, <code>git reset</code> will skip the moving of <code>HEAD</code> but update the index and working directory partially.</li></ol><hr><ul><li><code>git checkout &lt;branch&gt;</code> can be very similar with <code>git reset --hard &lt;branch&gt;</code>. However, for checkout, the working directory is safe, and will try to do a trivial merge in the working directory.<br>  Also, <code>reset</code> will change a commit a branch pointing to, but <code>checkout</code> will change the branch itself.</li><li>If a path is attached, checkout works like <code>git reset --hard [branch] file</code>. It will not move <code>HEAD</code> and is not safe for working directory.</li></ul><hr><h3 id="Tips-About-Merge"><a href="#Tips-About-Merge" class="headerlink" title="Tips About Merge"></a>Tips About Merge</h3><ol><li>If you <strong>revert</strong>ed a merged change, but later want to merge again, it will not work because the original merge is still reachable.<br> In that case, you will also need to revert the revert.</li><li><code>git config --global rerere.enabled true</code> can trigger the rerere function.<br> After merging, how conflicts are resolved will be recorded and can be check using <code>git rerere status</code>.<br> <code>git rerere diff</code> will raise the text editor so that conflicts will be resolved. After resolving that, the solution will be kept and remembered.<br> In the future, we can see something like <code>Resolved &#39;hello.rb&#39; using previous resolution.</code><br> * if you have the solution, just use <code>git rerere</code> to reuse solution for resolving.</li><li><code>git blame -L Line_range_beg,Line_range_end &lt;FileName&gt;</code> can check who introduced certain change.</li><li><code>git bisect</code> can be used to detect when failure is introduced.<br> a. <code>git bisect start</code>: start searching<br> b. <code>git bisect bad</code>: current commit is <strong>bad</strong><br> c. <code>git bisect good &lt;good_commit&gt;</code>: certain commit is good<br> We can keep giving “good” and “bad” feedbacks to figure out the first bad one.<br> After all, use <code>git bisect reset</code> to reset the <code>HEAD</code>pointer.</li></ol>]]>
    </content>
    <id>https://umiao.github.io/2024/08/06/Introduction-to-Git/</id>
    <link href="https://umiao.github.io/2024/08/06/Introduction-to-Git/"/>
    <published>2024-08-07T05:42:55.000Z</published>
    <summary>Thorough analysis and learning notes on Git version control based on official Git documentation.</summary>
    <title>Introduction to Git</title>
    <updated>2026-03-10T01:08:28.408Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Research" scheme="https://umiao.github.io/categories/Research/"/>
    <category term="Paper Read" scheme="https://umiao.github.io/categories/Research/Paper-Read/"/>
    <category term="Research" scheme="https://umiao.github.io/tags/Research/"/>
    <content>
      <![CDATA[<p>Discussion on challenges with stream processing.</p><span id="more"></span><h3 id="Upper-Confidence-Bound-Procedure-for-Robust-Selection-of-the-Best"><a href="#Upper-Confidence-Bound-Procedure-for-Robust-Selection-of-the-Best" class="headerlink" title="Upper-Confidence-Bound Procedure for Robust Selection of the Best"></a>Upper-Confidence-Bound Procedure for Robust Selection of the Best</h3><p>Author: Yuchen Wan</p><p>In simulation area, there may lies input uncertainty in the underlying simulation model.<br><strong>Robust selection of the best (RSB)</strong>: models this input uncertainty by a discrete <strong>ambiguity set</strong> (containing multiple possible scenarios) and then proposes a two-layer framework under which the best alternative is defined to have the best <strong>worst-case mean performance</strong> over the ambiguity set.<br>* Categorized as <strong>fixed-precision</strong> and <strong>fixed-budget</strong></p><p>This paper claims to invent a new robust <strong>upper-confidence-bound (UCB)</strong> procedure. </p><p><strong>Ranking and selection (R&amp;S)</strong> seeks to select the best alternative (have the <strong>smallest mean<br>performance</strong>) from a finite number of alternatives through repeatedly sampling a simulation model.</p><p>If the input distribution is not known, we may also want to model the <strong>input uncertainty</strong> (measurement error).<br>2-layers: 1 layer to identify the distribution of worst case, 1 layer to compare these cases mean performance.</p><p>Difference from <strong>MAB (multi-Armed Bandit)</strong>: Do not count in the regret during “exploration” stage, just aims at selecting the best one. This is under a clear explore-and-exploit 2 stages framework, and should it be easier?</p><blockquote><p>For me it is quite intuitive that higher exploration rate will lead to better simple regret..</p></blockquote><p><strong>Take Away</strong>: Figure out a well developed field like RL &#x2F; MAB, and transfer the related techniques &#x2F; success into new scenarios or (may be simplified) problems. </p><p><strong>To be improved</strong>: we may expect a stronger theoretical analysis and tighter bound of error &#x2F; probabilistic guarantee. The guarantee shown in the paper may not look very satisfying.</p><hr>]]>
    </content>
    <id>https://umiao.github.io/2024/05/14/14th-May-Researcher-Paper-Work-Group/</id>
    <link href="https://umiao.github.io/2024/05/14/14th-May-Researcher-Paper-Work-Group/"/>
    <published>2024-05-15T06:43:14.000Z</published>
    <summary>Notes from a research paper discussion group covering robust selection, stream processing, and UCB procedures.</summary>
    <title>14th_May_Researcher_Paper_Work_Group</title>
    <updated>2026-03-10T01:08:28.393Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Designing Data-Intensive-Applications" scheme="https://umiao.github.io/tags/Designing-Data-Intensive-Applications/"/>
    <content>
      <![CDATA[<p>Discussion on challenges with stream processing.</p><img src="/2024/05/06/Designing-Data-Intensive-Applications-Note-19/cover.png" class="" title="ML_note"><span id="more"></span><h3 id="Stream-Processing"><a href="#Stream-Processing" class="headerlink" title="Stream Processing"></a>Stream Processing</h3><p>Generalization of batch processing, which removes the assumption that the input is <strong>bounded</strong> of a known and <strong>finite</strong> size.</p><p>Daily batch processes may be too slow to reflect chagnes, that is why we can process data continuously, abandoning the fixed time slices entirely and simply processing every event as it happens. That is the idea behind <strong>stream processing</strong>.</p><p><strong>Stream</strong>: data that is incrementally made available over time.</p><p><strong>Event</strong>: A small, self-contained, <strong>immutable</strong> object containing the details of something that happened at some point in time.<br>An event usually contains a <strong>timestamp</strong>, and may be encoded as a text string, JSON or in binary form.</p><p>In streaming terminology, an event is generated once by a <strong>producer</strong> (also known as a <strong>publisher</strong> or <strong>sender</strong>), and then potentially processed by multiple <strong>consumers</strong> (<strong>subscribers</strong> or <strong>recipients</strong>)<br>Related events are usually grouped together into a topic or stream.<br>In case of <strong>continual</strong> processing with <strong>low</strong> delays, <strong>polling</strong> can be expensive without special optimization.<br>Lower the percentage of requests can return new events, and make the overhead high. </p><hr><p>Instead, it is better for consumers to be <strong>notified</strong> when new events appear (cannot be handled well by traditional DB).<br>A common solution is to use <strong>Messaging Systems</strong> (a producer sends a message containing the event, which is then pushed to consumers).<br>* Unix pipe and TCP connection are easy way to implement, but usually it is extended so that multiple producers &#x2F; consumers are allowed to send &#x2F; receive.</p><ol><li>In case producers send messages <strong>faster</strong> than the consumers can process them: <strong>drop</strong> message, store in <strong>queue</strong> or apply <strong>backpressure</strong> (a.k.a <strong>flow control</strong>, block the producer from sending more messages) [we can have a small fixed-size buffer, block sender if filled up].</li><li>In case node <strong>crashes</strong> or <strong>temporarily</strong> go <strong>offline</strong>. Note that <strong>durability</strong> comes with cost of performance, and should be determined by need of application.</li></ol><p><strong>Direct messaging methods</strong>:</p><ol><li><strong>UDP multicast</strong>: low latency, unreliable, retransmit on demand</li><li>Brokerless messaging libraries such as <strong>ZeroMQ</strong> and <strong>nanomsg</strong> take a similar approach, implementing publish&#x2F;subscribe messaging over <strong>TCP</strong> or <strong>IP</strong> multicast.</li><li>Or we can use unreliable UDP messaging for collecting metrics (like <strong>counter</strong>) from all machines on the network and monitoring them.</li><li>Consumer can expose service on network, producers can make direct <strong>HTTP</strong> or <strong>PRC</strong> request to push messages to the consumer.  A <strong>callback URL</strong> of one service is registered with another service, and it makes a request to that URL whenever an event occurs. (The idea of <strong>webhooks</strong>)</li><li>However, we usually assume producer and consumers are constantly <strong>online</strong>. Otherwise, consumer can miss messages from producer if offline.</li></ol><hr><h3 id="Message-Brokers"><a href="#Message-Brokers" class="headerlink" title="Message Brokers"></a>Message Brokers</h3><p>Also known as <strong>message queue</strong>, a kind of database optimized for handling <strong>message streams</strong> (consumers and producers connect as clients, read and write from the broker).</p><p>Clients can come and go, <strong>durability</strong> is handled by the broker. <strong>Unbounded</strong> queueing can be allowed.</p><p>The consuming is also <strong>asynchronous</strong>: producer will not wait till the message got consumed.</p><p><strong>Difference with Database</strong>:</p><ol><li>Delivered message will be deleted.</li><li>Will assume the working set to be small, the queue is <strong>short</strong>, can overflow to <strong>disk</strong>.</li><li>Rather than secondary indexes, brokers usually support <strong>subscribing</strong> to a <strong>subset</strong> of topics matching some pattern.</li><li>Do not support arbitrary queries (based on <strong>snapshot isolation</strong>), will notify clients when data changes.</li></ol><p>When serving <strong>multiple</strong> consumers, we can either do <strong>Load balancing</strong> (each message is delivered to one of the consumers, for them to share the work of processing) or <strong>Fan-out</strong> (each message is delivered to all consumers).<br>These patterns can be combined to be used.</p><p><strong>Acknowledge</strong> from client is needed before deleting a message, otherwise it is <strong>redelivered</strong> to another consumer (<strong>atomic commit protocol</strong> is needed in case message is processed by lost in network).<br>* Note that combination of load balancing with redelivery inevitably leads to messages being <strong>reordered</strong>. If there are causal dependencies between messages, do not use load balancing.</p><p>Messages stored in broker are regarded as temporary, and the acknowledgement is <strong>destructive</strong> as discussed above.</p><hr><p><strong>Log based</strong> message brokers aims at achieving low-latency notification and durability.<br>Producers append message to end of log, consumers read log sequentially, if it reaches the end, waits for the notification of a new message appended. (just like <code>tail -f</code> in unix)<br>Log can be partitioned on different machines by different topics, and achieve higher <strong>throughput</strong>.<br>Within each partition, each message has a <strong>monotonically</strong> increasing sequence number, or <strong>offset</strong> and messages are <strong>totally ordered</strong>. <strong>No</strong> ordering guarantee across different partitions<br>Examples: <strong>Apache Kafka</strong>, <strong>Amazon Kinesis Streams</strong>, and Twitter’s <strong>DistributedLog</strong></p><p>Log based method usually supports <strong>fan-out</strong> messaging, as consumers can independently read the log.<br>Load balancing can be done on partition level (<strong>coarse grained</strong>), and consumer read partition using <strong>single-threaded</strong> manner.</p><p><strong>Limitations</strong>: </p><ol><li>The number of nodes sharing the work of consuming a topic can be <strong>at most</strong> the number of log partitions in that topic.</li><li>Single slow message can hold the processing of subsequent messages in the same partition.</li></ol><p>Consumer can maintain an <strong>offset</strong> to record which messages have been processed. In case of consumer failure, another node and restart from this offset.<br>Also, <strong>“append-only”</strong> log makes <strong>replaying</strong> and <strong>recovering</strong> easier.</p><hr><p><strong>Disk Usage</strong> of Log Based Broker:<br>To reclaim disk space, the log is divided into <strong>segments</strong> and old segments can be deleted or moved to archieve storage.<br>It is possible that a slow consumer fall behind and its consumer offset points to a deleted segments and miss some messages.</p><p>Effectively, the log implements a <strong>bounded-size buffer</strong> that discards old messages when it gets full, also known as a <strong>circular buffer</strong> or <strong>ring buffer</strong> (the size can be large).<br>After the buffer is filled, old messages start to be overwritten.</p><p>Each message will be written to disk anyway, making the throughput more <strong>constant</strong> (and relatively higher) than memory-disk based broker.</p><hr><h3 id="Stream-Data-Systems"><a href="#Stream-Data-Systems" class="headerlink" title="Stream Data Systems"></a>Stream Data Systems</h3><p>In practice, multiple technoligies can be used in applications (e.g., OLTP database, cache, index, data warehouse..).<br>Each has its own copy of data in different representation, so they need to be kept <strong>in sync</strong> with each other.</p><p><strong>Dual writes</strong> (explicitly writes to <strong>each</strong> of the systems when data changes) are used in case periodic full database dumps are too slow.<br>However, it is prone to <strong>race conditions</strong>, the eventual result on each system may not be consistent (due to the order certain message arrives). Also, some write may fail.<br>We can use <strong>2PC</strong> to solve, but with high cost.</p><hr><p><strong>Change Data Capture</strong> has become an interesting topic, where we can take changes made in a database and replicate them to different storage technology like indexes or caches.<br>This can be hard, because there is a lot of internal implementation details of databased exposed in log.</p><p><strong>Implementation</strong>:<br>We can make one database the leader and others as followers using log based message broker (without reordering issue).<br>Log consumers are <strong>derived</strong> data systems, just storing another <strong>view</strong> on the data and we ensure all changes also reflects in the derived data systems.<br>* Database trigger can be used, but with poor performance.</p><p>Keep all the changes log for replay can be expensive, so we can have it <strong>truncated</strong> and create an <strong>initial snapshot</strong>.<br>Snapshot must correspond to a known <strong>position</strong> or <strong>offset</strong> in the change log.</p><p><strong>Log Compaction</strong>: logs can be compacted by discarding duplicates, merge by key and only the latest value is preserved. Deleted keys (indicated by <strong>tombstone</strong>) would also be removed.<br>* This idea can be extended to log-based message brokers and change data capture, where we only keep the <strong>most recent</strong> write for a particular key.<br>When rebuilding data system, we can just start scaning from the beginning of a compacted log, without taking another snapshot of the CDC source database.</p><hr><p><strong>Event Sourcing</strong>: store all changes to the application state as a log of change events.</p><p>For <strong>Change Data Capture</strong>, the application uses the database in a <strong>mutable</strong> way (also a low level way, like parsing change logs), and application writing to DB does not need to be aware of it.</p><p>For Event Sourcing, we expect <strong>immutable</strong> events are written to an event log (append-only) to reflect things happened at application level, rather than on lower level.<br>This helps us better understand why things happen, like </p><p>For example, storing the event “student cancelled their course enrollment” clearly expresses the intent of a single action in a neutral fashion, whereas the side effects “one entry was deleted from the enrollments table, and one cancellation reason was added to the student feedback table” embed a lot of assumptions about the way the data is later going to be used.</p><p>New side effect to easily be <strong>chained</strong> off the existing event (like a place can be offered to next person).</p><hr><p>*It shoud be noted that user usually prefer seeing current states of system, rather than history of modifications.<br><strong>Change Data Capture</strong> can discard former events and only keep the latest value, but <strong>Event Sourcing</strong> usually needs the entire history as later event do not override prior events.<br>This is a <strong>limition of immutability</strong>, and can be optimized using <strong>snapshot</strong>.</p><p>The philosophy of event sourcing is to distinguish <strong>events</strong> and <strong>commands</strong>.<br>User’s request is initially a command, and may fail due to violation of integrity condition. However, if it is accepted, it becomes a <strong>durable</strong> and <strong>immutable</strong> event. It cannot be rejected by consumer of the event stream.<br>Any validation must happen before a command is executed.</p><p>It should be noted that <strong>mutable</strong> state (come with <strong>accoutability</strong>, and is good for analyzing) and append-only log of <strong>immutable</strong> events do not contradict each other.<br>Database is a cache of a subset of the log, containing the latest values.<br>* append only log is more friendly for achieving <strong>atomicity</strong> and removing <strong>non-determinism</strong>.</p><p><strong>Command Query Responsibility Segregation</strong> (CQRS): Derive serval (<strong>read-oriented</strong>) views from the same event log. We can have separate read-optimized view for the new feature, without changing the existing system.<br>This breaks the <strong>design fallacy</strong> that data must be written in the same form as it will be queried. This can also help resolving the debate around normalization and <strong>denormalization</strong> (such view can be translated from event log).</p><p>Limitation of <strong>event sourcing</strong> and <strong>change data capture</strong> is that the event log are usually <strong>asynchronous</strong>, so the read view may not be synced. We can either synchronously update the read view or use a <strong>total order broadcast</strong>.</p><hr><ul><li>For small dataset with high rate of updates and deletes, Immutability may result in large history and performance of <strong>compaction</strong> and <strong>garbage collection</strong> becomes crucial for operational robustness </li><li>For <strong>privacy &#x2F; compliance</strong> concern, we may want to remove erroneous ifnormation or contain accidental leakage. We may need to <strong>rewrite</strong> history and pretend that data was <strong>never written</strong> (called <strong>excision</strong> or <strong>shunning</strong>).<br>* Note that truly delete data can be challenging, because copies can live in many places rather than overwritten in place, we just try to make retrival of data harder.</li></ul><hr><h3 id="Processing-Streams"><a href="#Processing-Streams" class="headerlink" title="Processing Streams"></a>Processing Streams</h3><p>A piece of code that processes streams to produce derived streams is known as an <strong>operator</strong> or a <strong>job</strong>.<br>The input is consumed in <strong>read-only</strong> fashion and writes to a different location in <strong>append-only</strong> fashion.</p><p>Since stream is endless, we can <strong>NOT</strong> use mapping operations such as transforming and filtering (sort-merge) records. Also, it is impossible to restart from beginning after a crash.</p><p>Stream is widely used for <strong>monitoring</strong> purpose requesting pattern matching and correlations.<br>Other applications include: </p><ul><li><strong>Complex event processing</strong> (search for certain patterns of events in a stream, such pattern may described using high-level declarative query language or GUI, then maintain a state machine. If detected, a complex event is emitted) In such cases, queries become <em>long-term</em>.</li><li><strong>Stream analytics</strong>: towards <strong>aggregations</strong> and <strong>statistical</strong> metrics, usually over fixed time intervals (e.g., averaged request counts and latency in different percentiles). The time interval over which you aggregate is known as a <strong>window</strong>, and probabilistic algorithms are sometimes used like <strong>Bloom filters</strong> and <strong>HyperLogLog</strong></li><li><strong>Maintaining materialized views</strong>: deriving an alternative view onto some dataset for query purpose, and update it whenever data changes. However, we may need to maintain all evenets forever, which is against the assumption of a time window.</li><li><strong>Search on streams</strong>: we may also need to search individual events based on complex criteria (<strong>Elasticsearch</strong>‘s <strong>percolator</strong> can support this). We can store (potentially index) queries and ducuments to dynamically search individual events.</li></ul><p><strong>Clock</strong> can be a big challenge in stream processing, as we do not have a unified reliable clock to rely on, and we need timestamp to detemine window.</p><p>One mitigation solution is to log three timestamps:<br>the time an event occured (<strong>device clock</strong>), the <strong>device clock</strong> when it was sent to the server and the <strong>server clock</strong> when it is received.<br>By subtracting the second timestamp from the third, you can estimate the <strong>offset</strong> between the device clock and the server clock </p><hr><p>Some different types of windows which got commonly used:</p><ul><li><strong>Tumbling window</strong>: fixed-lengthed, every event belongs to exactly one window. Can be implemented by taking each event timestamp and <strong>rounding</strong> it down to the nearest minute.</li><li><strong>Hopping window</strong>: fixed-lengthed, allow windows to <strong>overlap</strong> to provide smoothing.</li><li><strong>Sliding window</strong>: Contains all the events that occur within some interval of each other. Can be implemented by a <strong>buffer</strong> of events, and remove old events when they expire from the window.</li><li><strong>Session window</strong>: No fixed duration, just group all events for the same user occur closely together in time. Ends when user become <strong>inactive</strong>.</li></ul><hr><h3 id="Stream-Joins"><a href="#Stream-Joins" class="headerlink" title="Stream Joins"></a>Stream Joins</h3><ul><li><strong>Stream-stream join</strong> (window join): We may need to link search and click activity (within certain time window) to let them sharing the same session ID (note that you need to learn accurate click-through rate, to learn searches which did not bring clicks events). We can use a stream processor to merge such activities by ID.</li><li><strong>Stream-table join</strong> (stream enrichment): Enrich the activity events with information from the table. If the database querying is slow, consider loading the database copy (may need to be updated by <strong>change data capture</strong>) to the stream processor.</li><li><strong>Table-table join</strong> (materialized view maintenance): Cache maintenance similar to the example provided at the beginning of this book. When celebrity tweets, sync it too all followers’ tiemlines. We can maintain a <strong>materialized view</strong> for a query that joins two tables</li></ul><p>For the aboce cases, <strong>time dependency</strong> is important, as data in certain DB can change (e.g., tax rate), which prevents us from getting the same result rerunning the same job.<br>In data warehouse, it is called <strong>Slowly Changing Dimension</strong> (SCD). We can use a <strong>unique</strong> identifier for a particular version of the joined record (e.g., whenever tax rate changes). This makes the join deterministic, but will also make log compaction impossible.</p><hr><h1 id="Fault-Tolerance"><a href="#Fault-Tolerance" class="headerlink" title="Fault Tolerance"></a>Fault Tolerance</h1><p><strong>Exactly-once Semantics &#x2F; Effectively-Once</strong>: though some tasks &#x2F; records may failed and processed multiple times, the visible effect is as if they have only been processed once.</p><p>However, stream is <strong>infinite</strong>, so we cannot make output until finished.</p><p><strong>Microbatching</strong>: break the stream into small blocks, and treat each block like a miniature batch process (in a implicit tumbling window).<br>Can generate <strong>rolling checkpoints</strong> of state and write them to durable storage (however, if message is sent to external message broker like email, checkpoint is not sufficient to make it effective once, and will need <strong>2PC</strong>)</p><p><strong>Idempotence</strong>: We can require certain operation which can be performed multiple times, but effect only once.</p>]]>
    </content>
    <id>https://umiao.github.io/2024/05/06/Designing-Data-Intensive-Applications-Note-19/</id>
    <link href="https://umiao.github.io/2024/05/06/Designing-Data-Intensive-Applications-Note-19/"/>
    <published>2024-05-06T21:51:50.000Z</published>
    <summary>Discussion on challenges with stream processing.</summary>
    <title>Designing Data-Intensive-Applications-Note-19 [END]</title>
    <updated>2026-03-10T01:09:07.005Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Designing Data-Intensive-Applications" scheme="https://umiao.github.io/tags/Designing-Data-Intensive-Applications/"/>
    <content>
      <![CDATA[<p>Discussion on challenges with batch processing jobs.</p><img src="/2024/05/05/Designing-Data-Intensive-Applications-Note-18/cover.png" class="" title="ML_note"><span id="more"></span><h3 id="Unix-Tools-Pipe"><a href="#Unix-Tools-Pipe" class="headerlink" title="Unix Tools (Pipe)"></a>Unix Tools (Pipe)</h3><p>We can categorize systems into: <strong>Serivices</strong>(online systems), <strong>Batch Processing Systems</strong> (offline systems) and <strong>Stream processing system</strong> (near real-time).</p><p>The <strong>Unix</strong> philosophy in data processing: concatenate data input and output to form data flows, using <code>pipe</code>.</p><ol><li>Make each program do <strong>one thing</strong> well. To do a new job, build a fresh rather than complicate old programs by adding new “features”.</li><li>Expect the output of every program to become the input to another, as yet unknown, program. Don’t clutter output with extraneous information. Avoid stringently columnar or binary input formats. Don’t insist on interactive input.</li><li>Design and build software, even operating systems, to be tried early, ideally within weeks. Don’t hesitate to throw away the clumsy parts and rebuild them.</li><li>Use tools in preference to unskilled help to lighten a programming task, even if you have to detour to build the tools and expect to throw some of them out after you’ve finished using them.</li></ol><p>This <strong>approach—automation</strong>, <strong>rapid prototyping</strong>, <strong>incremental iteration</strong>, being friendly to <strong>experimentation</strong>, and breaking down large projects into manageable <strong>chunks—</strong> sounds remarkably like the Agile and DevOps movements of today. </p><p>In order to loop programs together, we need all programs to use the same input&#x2F;output interface (<strong>file</strong>, just ordered sequence of bytes).<br>Can be extended to actual files, channels to another process (Unix socket, <code>stdin</code>, <code>stdout</code>),  a device driver (say <code>/dev/audio</code> or <code>/dev/lp0</code>), a socket representing a <code>TCP</code> connection, and so on.</p><p>* Standard protocol may not be trivial, like in <strong>Bulletin Board Systems</strong> (BBSs), each system had its own phone number and baud rate configuration</p><p><strong>Take-aways</strong> of Unix Philosophy</p><ul><li>Separation of <strong>logic</strong> and <strong>wiring</strong>, as a result of loose coupling design. You can by default use <code>stdin</code> and <code>stdout</code> or change to other sources, by it is not a problem for the program. </li><li>The input files are normally treated as <strong>immutable</strong>. You can run the commands as often as you want, trying various command-line options, <strong>without</strong> damaging the input files.</li><li>You can end the pipeline at any point, pipe the output into <code>less</code>, and look at it to see if it has the expected form. This ability to inspect is great for <strong>debugging</strong>.</li><li>You can write the output of one pipeline stage to a file and use that file as input to the next stage. This allows you to restart the later stage without rerunning the entire pipeline.</li></ul><hr><h3 id="MapReduce"><a href="#MapReduce" class="headerlink" title="MapReduce"></a>MapReduce</h3><p>The idea is similar to the Unix tools, but <strong>MapReduce</strong> do I&#x2F;O on distributed filesystem (like <strong>HDFS, Hadoop Distributed File System</strong> and <strong>GFS, Google File System</strong>).<br>So as <strong>object storage services</strong> like Amazon S3, Azure Blob Storage, and OpenStack Swift.</p><p>HDFS follows <strong>shared-nothing</strong> principle, making it not relying on special hardware (store with redundancy for fault tolerance). Consists <strong>daemon process</strong> to expose network service allowing file access. A central server called the <strong>NameNode</strong> keeps track of which file blocks are stored on which machine. </p><hr><p>MapReduce algorithm requires 2 callback functions: <strong>mapper</strong> (called once for every input record, generate k-v pairs from input, no state stored) and <strong>reducer</strong> (takes the k-v pair, iterates to produce output).</p><ol><li>Mapper and reducer can operate only on one record at a time, do not consider related states. </li><li>Usually the application codes need to be moved to machines with task assigned first, like with <strong>k8s</strong>.</li><li><strong>Hash</strong> of key is used, to put k-v paris with the same key to the same reducer.</li><li>The reducers are also (author configurably) partitioned.</li><li>The key-value pairs must be <strong>sorted</strong> (by the mapper, to be performed in stages). After that, the reducers can connect to mappers and download the files of sorted k-v pairs, this is called <strong>shuffling</strong>. For k-v pairs with the same key from different mappers, they will also be merged before parsed by reducers.</li></ol><p>It should be noted that <strong>chained MapReduce</strong> is more like a series of command, where each command’s output is written to a temporary file (Hadoop writes to disk a lot, even unnecessary).</p><hr><p><strong>Reduce-Side Joins and Grouping</strong>: <strong>MapReduce</strong> do not have indexes in usual sense, so it usually do <strong>full table scan</strong> (very expensive). Also, <strong>random-access</strong> request through network is either too slow or too expensive. A better approach may be taking a copy of the database and put it in the same distributed filesystems.</p><p><em><strong>Example</strong></em>: You may want to join user activity table with profile table. With such copies in HDFS, you can use map reduce to extrac <code>(user_id, url)</code> and <code>(user_id, date_of_birth)</code> with different mappers, and reduce them into <code>(user_id, [url, date_of_birth, ...])</code><br>This can be called <strong>Sort-merge joins</strong>, such tuples containing different fields but with same user_id will be <strong>adjacent</strong>.</p><p><strong>Secondary sort</strong>: sort such that the reducer always sees the record from the user database, followed by the activity events (in timestamp order)</p><p>Since the reducer processes all of the records for a particular user ID in one go, it only needs to keep <strong>one</strong> user record in memory at any one time, and it never needs to make any requests over the network.<br>This algorithm is known as a <strong>sort-merge join</strong>, since mapper output is sorted by key, from both sides of the join.</p><p>By bringing related data together, we separated the <strong>physical</strong> network communication aspects of the computation (getting the data to the right machine) from the <strong>application logic</strong> (processing the data once you have it)</p><hr><p><strong>Handling Skew</strong> (e.g., <strong>hotkeys</strong>) can also be important, as MapReduce has to wait till all mappers and reducers to finish in every round.</p><p>We can detect hot keys with <strong>sampling</strong>, then let mappers send hotkey related records to <strong>serveral</strong> reducers (rather than one determined by hashing). Note that other inputs need to be <strong>replicated</strong> to all reducers which handle the hotkey as well.<br>Or if we know the hot keys already, we can store them separately.</p><p>Note that such reducers’ outputs need to be aggregated.</p><hr><h3 id="Map-side-Joins"><a href="#Map-side-Joins" class="headerlink" title="Map-side Joins"></a>Map-side Joins</h3><p>With certain assumption about the input data, <strong>Map-side Joins</strong> may optimize the process (while reduce-side joins need <strong>expensive</strong> sorting, copying and merging)<br>If we can do that, then we can remove the reducer and have mapper writes into certain files.</p><ol><li><strong>Broadcast hash joins</strong>: can be used when joining a large dataset with a small dataset (can fit in memory of each mapper). “Broadcast” reveals that each mapper of a partition of the large input can read the entire small input. An alternative is to store the small join input in a <strong>read-only index</strong> on the <strong>local disk</strong> (so we can handle a bit larger “small” set).</li><li><strong>Partitioned hash joins</strong> (<strong>bucketed map joins</strong>): If the inputs to the map-side join are partitioned in the same way, then the hash join approach can be applied to each partition <strong>independently</strong>. E.g., if we want to join the user profile and activity tables, then we can partition based on the last digit of <code>user_id</code>, because it works for both tables so that we can process on a smaller subset. </li><li><strong>Map-side merge joins</strong>: If input datasets are not only partitioned in the same way, but also <strong>sorted</strong> based on the same key, then the merge can be done by the mapper.</li></ol><hr><p><strong>Application of MapReduce</strong>: build search engine <strong>index</strong> (incremental update will be harder, involves merges segments), <strong>K-V pair</strong> as output (e.g., building dataset or database)</p><p>It may <strong>not</strong> be a good practice Allowing MapReduce to write into database server (one record once) directly. Network request is slower, database’s performance can be impacted, cannot guarantee “atomicity” &#x2F; <strong>“clean all-or-nothing”</strong>.</p><p>Better solution may be building new databases inside the batch jobs, write it as files to output DIR of distributed Filesystem. Once finished, make these files <strong>immutable</strong> and loaded in bulk into servers to serve <strong>read-only</strong> queries.<br>If replication went wrong, we can easily switch back to old files.</p><p><strong>Diversity of storage</strong>: Hadoop would first dump data into HDFS, then figure out how to process that. This can make data available more quickly.<br>This paradigm of worrying about schema later and allow data collection to be speeded up is known as “<strong>data lake</strong>“ or “<strong>enterprise data hub</strong>“. This makes data interpretation a problem just for consumer. Once it is modeled and formated, it can be imported into <strong>MPP (Massively Parallel Processing)</strong> data warehouse.</p><hr><p>However, MapReduce still faces severe performance issue in many scenarios and not used as widely, but more methods can be applied on top of Hadoop.</p><p><strong>Fault Tolerance</strong> of MapReduce</p><p>Batch processes are <strong>less sensitive</strong> to faults than online systems, as tehy do not immediately affect users and can always be rerun.<br>Failure of single map &#x2F; reduce task can be tolerated and can be retried.<br>Data is frequently loaded to <strong>disk</strong>, because the data volume is big and for durable storage.<br>These features makes MapReduce suitable for larger jobs. Retry at the granularity of an <strong>individual task</strong> may help, as rerun the whole task can be expensive.</p><p>* The faults do not happen very frequently, however, batch processing jobs are usually with lower priority and can be terminated (<strong>preempted</strong>). This makes ability to recover valuable so that we can make sue of the “scraps” of computation power.</p><hr><h3 id="Improvements-On-MapReduce"><a href="#Improvements-On-MapReduce" class="headerlink" title="Improvements On MapReduce"></a>Improvements On MapReduce</h3><p><strong>Materialization</strong>: writing intermediate state into files.<br>For complex workflows using a lot of MapReduce jobs, such Materialization can be <strong>wasteful</strong> because we have to wait all preceding jobs to finish, and Mappers are often <strong>redundant</strong> (just read back the file written by Reducers).</p><p><strong>Spark</strong>, <strong>Tez</strong> and <strong>Flink</strong> aim at resolving the above issues, by treating the entire workflow as one job.<br>They are known as <strong>dataflow engines</strong> as they explicitly model the flow of data through several processing stages.</p><ol><li>Work by <strong>repeatedly</strong> calling a user-defined function (<strong>operators</strong>) to process one record at a time on a single thread. </li><li>They <strong>parallelize</strong> work by partitioning inputs</li><li>They copy the output of one function over the network to become the input to another function.</li><li>More <strong>flexible</strong>, not required to take strict roles of map and reduce.</li></ol><p><strong>How Operator Connect To Inputs</strong>: </p><ul><li><strong>Repartition and sort</strong> records by key</li><li>Take several inputs and to partition them in the same way, but <strong>skip</strong> the sorting.</li><li>For <strong>broadcast hash joins</strong>, the same output from one operator can be sent to all partitions of the join operator.</li></ul><p><strong>Advantages</strong>: </p><ol><li>Expensive work like <strong>sorting</strong> only performed in places where it is actually required.</li><li>No unnecessary map tasks.</li><li>All joins and data dependencies in a workflow are explicitly declared, making <strong>locality optimization</strong> possible (e.g., place data producer and consumer tasks in the same node).</li><li>Can keep intermediate state between operators in <strong>memory</strong> or <strong>local disk</strong>, rather than write to <strong>HDFS</strong>.</li><li>Operators can start as soon as their input is ready, rather than waiting for the entire preceding stage.</li><li>Existing JVM can be reused to run new operators.</li></ol><hr><p><strong>Fault Tolerance</strong>:</p><p><strong>Spark</strong>, <strong>Flink</strong>, and <strong>Tez</strong> avoid writing intermediate state to HDFS, so they take a different approach to tolerating faults:</p><p>If a machine fails and the intermediate state on that machine is lost, it is <strong>recomputed</strong> from other data that is still available<br>(a prior <strong>intermediary stage</strong> if possible, or otherwise the <strong>original input data</strong>, which is normally on HDFS).</p><p>Framework should track how a given piece of data was computed—which input partitions it used, and which operators were applied to it.<br><strong>Spark</strong>: use <strong>Resilient Distributed Dataset (RDD)</strong> to track the ancestor of data.<br><strong>Flink</strong>: checkpoints operator state.</p><p>* It is important that the computation is <strong>deterministic</strong>. It matters especially when some lost data is sent to the downstream operators (so that we ensure there is no <strong>contradictions</strong> between the old and new data).<br>If it is <strong>non-deterministic</strong>, then downstream operators should be killed as well and rerun. Note that certain programming language may not guarantee any order when iterating over elements, and we should try to use pseudorandom numbers using a <strong>fixed</strong> seed.</p><p>** For expensive computation or small output, we can still <strong>materialize</strong> it as a file.</p><p>We can tell that the tradeoff is like whether you want to store results in a temporary file, or pass it like a Unix pipe.<br>It should be noted that <strong>Sorting</strong> operation must be first completed before passing to next operator (Flink tends to <strong>incrementally</strong> pass the output of an operator).</p><hr><h3 id="Graphs-and-Iterative-Processing"><a href="#Graphs-and-Iterative-Processing" class="headerlink" title="Graphs and Iterative Processing"></a>Graphs and Iterative Processing</h3><p>We may need to look at graphs in batch processing context, e.g. <strong>PageRank</strong> in recommendation engine.</p><p>Dataflow engines (Spark, Flink, and Tez) typically arrange the operators in a job as a <strong>directed acyclic graph (DAG)</strong>.<br>Many graph algorithms are expressed by <strong>traversing</strong> one edge at a time, joining one vertex with an adjacent vertex in order to propagate information, repeat till some condition is met (<strong>transitive closure</strong>).</p><p>Graph can be stored in a distributed filesystem, but the “repeative until done” cannot be expressed in plain <strong>MapReduce</strong>, since it only performs a single pass over the data, but should be implemented in an iterative way (but with very low efficiency, as the entire input dataset will be read though small part of the graph changed).</p><hr><p>The <strong>Pregel</strong> processing model has been introduced as an optimization of the above method, using <strong>Bulk Synchronous Parallel (BSP).</strong></p><p>One vertex can “send a message” to another vertex, and typically those messages are sent along the edges in a graph.<br>In each iteration, a function is called for each vertex, passing it <strong>all</strong> the messages that were sent to it (via all the adjacent edges, much like a call to the reducer).<br>In the Pregel model, a vertex remembers its state in memory from one iteration to the next, so the function only needs to process new incoming messages. </p><p>* If <strong>no</strong> messages are being sent in some part of the graph, <strong>no</strong> work needs to be done.</p><p>** It should be noted that it is hard to partition that vertices are colocated on the same machine, a lot of <strong>cross-machine communication overhead</strong>. Thus we should place such algorithm on <strong>single</strong> machine if possible.</p>]]>
    </content>
    <id>https://umiao.github.io/2024/05/05/Designing-Data-Intensive-Applications-Note-18/</id>
    <link href="https://umiao.github.io/2024/05/05/Designing-Data-Intensive-Applications-Note-18/"/>
    <published>2024-05-05T19:00:33.000Z</published>
    <summary>Discussion on challenges with batch processing jobs.</summary>
    <title>Designing Data-Intensive-Applications-Note-18</title>
    <updated>2026-03-10T01:09:07.004Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Designing Data-Intensive-Applications" scheme="https://umiao.github.io/tags/Designing-Data-Intensive-Applications/"/>
    <content>
      <![CDATA[<p>Discussion on challenges with consensus.</p><img src="/2024/04/27/Designing-Data-Intensive-Applications-Note-17/cover.png" class="" title="ML_note"><span id="more"></span><p><strong>Consensus</strong>: Get all nodes to agree on something, e.g., the election of new leader. If two nodes both believe that they are the leader, that situation is called <strong>split brain</strong> which can be prevented by consensus.</p><p><strong>Eventually consistency</strong> &#x2F; <strong>coverage</strong> is a <strong>weak</strong> guarantee.<br><strong>Stronger</strong> consistency guarantee may be possible, but can be less fault-tolerant or with worse performance. </p><h3 id="Linearizability"><a href="#Linearizability" class="headerlink" title="Linearizability"></a>Linearizability</h3><p>Database works as if there is only one replica, no replication lag.<br>Also known as <strong>atomic consistency</strong>, <strong>strong consistency</strong>, <strong>immediate consistency</strong>, or <strong>external consistency</strong>.<br>It is a <strong>recency guarantee</strong>, means that we always able to see the latest value written to the database.</p><p>Considering that we have a write request to the database, while there are other client requesting the database to get the latest value.<br>Any read operations that <strong>overlap</strong> in time with the write operation might return either old or new value, because we don’t know whether or not the write has taken effect at the time when the read operation is processed.<br>These operations are <strong>concurrent</strong> with the write.</p><p>However, that is not yet sufficient to fully describe linearizability: if reads that are concurrent with a write can return either the old or the new value, then readers could see a value <strong>flip</strong> back and forth between the old and the new value several times while a write is going on. That is not what we expect of a system that emulates a <strong>“single”</strong> copy of the data.</p><p>In order to enforce <strong>Linearizability</strong>, we have to imagine that for some point in time, the above value <strong>flips</strong> from old value to new value. All subsequent reads must also return the new value, even if the write operations <strong>has not</strong> yet completed.<br>Also, we can add <strong>compare-and-set</strong> <code>cas</code> atomic operator, to make the read-write cycle works correctly. (Before write the value $v_{new}$, we can read the value $v_{old}$ which is previously read). Otherwise, return an <strong>error</strong>.</p><hr><p><strong>Solution</strong>: We assume each operator to happen at certain <strong>moment</strong> of time. We place <strong>markers</strong> on these moments, and we require such markers are joined up in a <strong>sequential</strong> order, and the result must be a valid sequence of reads and writes for a register (every read must return the value set by the <strong>most recent</strong> write) (new value should take effect, even the write is <strong>not completed</strong>).</p><p>The requirement of <strong>linearizability</strong> is that the lines joining up the operation markers always move forward in time (from left to right), never backward. This requirement ensures the <strong>recency guarantee</strong> we discussed earlier: once a new value has been written or read, all subsequent reads see the value that was written, until it is overwritten again.</p><p>It is possible (though computationally <strong>expensive</strong>) to test whether a system’s behavior is linearizable by recording the timings of all requests and responses, and checking whether they can be arranged into a <strong>valid sequential order</strong> </p><hr><p><strong>Linearizability</strong> Versus <strong>Serializability</strong>:</p><p><strong>Serializability</strong> is an isolation property of <strong>transactions</strong>, each may read &#x2F; write multiple objects. It guarantees that transactions behave the same as if they had executed in some serial order (OK to be different from the order which transactions were actually run)</p><p><strong>Linearizability</strong> is a <strong>recency guarantee</strong> on reads and writes of a register &#x2F; single object. </p><p>When the above two are provided, it is called <strong>strict serializability</strong> or <strong>strong one-copy serializability</strong> <strong>(strong-1SR)</strong>. </p><p>* Implementations of <strong>serializability</strong> based on <strong>two-phase locking</strong> (2PL) or <strong>actual serial execution</strong> are typically <strong>linearizable</strong>.<br>**  Serializable snapshot isolation is not linearizable, because read is made from consistent snapshot, does not include writes that are more recent than the snapshot.</p><hr><p>One way of electing a leader is to use a <strong>lock</strong>: every node that starts up tries to acquire the lock, and the one that succeeds becomes the leader. The lock must be <strong>linearizable</strong> to reach consensus.</p><p>Distributed locking can be used in more granular level, e.g., in  <strong>Oracle Real Application Clusters (RAC)</strong>, lock is on disk page level, with multiple nodes sharing access to the same disk storage system.</p><p><strong>Uniqueness guarantees</strong> is similar to lock (or <strong>compare-and-set</strong>), e.g., to solve naming conflicts. This can also be used to enforce <strong>constraints</strong>, like make sure bank account balance being non-negative, or inventory not below zero.<br>Such constraints do require linearizability. While foreign key or attribute constraints do not need such property.</p><hr><p><strong>Cross-channel timing dependencies</strong>: </p><p>Race conditions may be possible between different channels, without linearizability.<br>E.g., a server would first store the full size picture and send message to generate a resized picture (<strong>thumbnail</strong>). If the latter is faster, it may receive NULL result, or a old-versioned picture.<br>This is due to me have two different communication channels between the web server and the resizer: the file storage and the message queue.</p><hr><h3 id="Implementations-of-Linearizability"><a href="#Implementations-of-Linearizability" class="headerlink" title="Implementations of Linearizability"></a>Implementations of Linearizability</h3><ul><li><strong>Single Replica</strong>: linearizable, but vulnearable to data loss and failure</li><li><strong>Single-leader replication</strong>: potentially linearizable if you read from the leader or synced followers. However, may not be linearizable due to design (snapshot isolation) or concurrency bugs. However, we need the assumption that everyone knows who is the leader. Otherwise we may have <strong>delusional leader</strong> continues to serve requests.</li><li><strong>Consensus algorithms</strong>: Linearizable.</li><li><strong>Multi-leader replication</strong>: Not Linearizable. Because usually the replication is done asynchronously, may need conflict resolution.</li><li><strong>Leaderless replication</strong>: probably not linearizable. Such <strong>Dynamo style</strong> thing sometimes claimed that you can obtain <strong>strong consistency</strong> by requiring quorum reads and writes ($w + r &gt; n$) but this might not be true.</li><li><strong>Last write wins</strong> conflict resolution methods based on <strong>time-of-day</strong> clocks are almost certainly <strong>nonlinearizable</strong>, because clock timestamps cannot be guaranteed to be consistent with actual event ordering due to <strong>clock skew</strong>.</li></ul><p><strong>Sloppy quorums</strong> also ruin any chance of linearizability.</p><hr><p>We can construct samples that even with quorum reads and writes ($w + r &gt; n$) met, the execution can be <strong>non-linearizable</strong>:</p><p>Consider that $n&#x3D;3, w&#x3D;3, r&#x3D;2$, and we have replica A, B and C. First new value is written to replica A, then client X reads A, B to get the new value; Then client Y reads B, C to get the old value.</p><p>This is non-linearizable as Y reads after X, but get the older value.</p><p>Safest to assume a leaderless system with Dynamo-style replication does <strong>not</strong> provide linearizability.<br>With reduced performance, we can make it linearizable: a reader must perform <strong>read repair</strong> synchronously, before returning results to the application; a writer must read the latest state of a quorum of nodes before sending its writes.<br>Also, a linearizable <strong>compare-and-set</strong> operation cannot, because it requires a <strong>consensus</strong> algorithm</p><hr><p><strong>The CAP theorem</strong>： If your application requires <strong>linearizability</strong>, and some replicas are disconnected from others, then they must <strong>wait</strong> until the network is fixed, or return an error.<br>Otherwise (no linearizability needed), we can allow each replica to process requests indenpendently even disconnected (e.g., in a <strong>multi-leader</strong> setting)</p><p>* <strong>CAP</strong> is sometimes presented as <strong>Consistency</strong>, <strong>Availability</strong>, <strong>Partition tolerance</strong>: pick 2 out of 3. </p><p>However, network partition just happens as a kind of fault, and you do not have a choice. When this happens, we can provide either <strong>linearizability</strong> or <strong>total availability</strong>.<br>CAP cannot help us best understand the system (does not consider network delays, dead nodes, or other trade-offs) and better avoided.</p><hr><p>Few systems are actually <strong>linearizable</strong> in practice, not even RAM on a modern multi-core CPU, because each core has its own memory <strong>cache</strong> and store <strong>buffer</strong>. Memory access goes to cache by default, then written out to memory <strong>asynchronously</strong>.</p><p>Thus, many systems give up providing linearizability, due to performance concern.<br>It is prove that if you want linearizability, the response time of read and write requests is at least <strong>proportional</strong> to the uncertainty of delays in the network. However, delays can be long in a network with highly variable delays.</p><hr><h3 id="Ordering"><a href="#Ordering" class="headerlink" title="Ordering"></a>Ordering</h3><p><strong>Causality</strong> imposes an ordering on events: cause comes before effect.<br>Something has to be created before updated, A and B should not have causal link between them to become concurrent. “Consistent” read also means that snapshot should contains answer as well as the question it trying to answer.</p><p>If a system obeys the ordering imposed by causality, we say that it is <strong>causally consistent</strong>.</p><p>The causal order is not a <strong>total order</strong> (arbitrary two elements can be compared). $\{a, b\}$ cannot be compared with $\{b, c\}$, being <strong>incomparable</strong> (unless one is subset of another)</p><p>In a linearizable system, we have a <strong>total order</strong> of operations.<br>Therefore, no concurrent operation exists in such system. If there is any, we can only have a <strong>partial order</strong> such that they are incomparable (not causally related).<br>Linearizability is <strong>stronger</strong> than preserving causality.</p><p><strong>Version control system</strong> like <strong>Git</strong> works as a graph of causal dependencies, where timeline <strong>branches</strong> and <strong>merges</strong>.</p><p><strong>Conclusion</strong>: Causal consistency is the <strong>strongest</strong> possible consistency model that does not slow down due to network delays, and remains available in the face of network failures.<br>Implementation <strong>causality consistency</strong> requires tracking <strong>causal dependencies</strong> across the entire database, can be done with <strong>version vectors</strong> &#x2F; <strong>logical clock</strong>.</p><hr><h3 id="Noncausal-sequence-number-generator"><a href="#Noncausal-sequence-number-generator" class="headerlink" title="Noncausal sequence number generator"></a>Noncausal sequence number generator</h3><p>If there is not a single leader (due to multi-leader &#x2F; leaderless database), it is less clear about how to generate sequence numbers for operations. Potential solutions:</p><ul><li>Each node generate its own <strong>independent set</strong> of sequence numbers, e.g., only generate multiple of $k$ as version number, or reserve bits in binary representation as unique identifier</li><li>Attach a <strong>timestamp</strong> from a time-of-day clock (e.g., last write win).</li><li><strong>Preallocate</strong> blocks of sequence numbers, e.g. (1-1000) just for node A.</li></ul><p>However, these methods <strong>cannot</strong> capture the ordering of operations correctly.</p><h3 id="Lamport-timestamps"><a href="#Lamport-timestamps" class="headerlink" title="Lamport timestamps"></a>Lamport timestamps</h3><p>Generating sequence numbers that is consistent with <strong>causality</strong><br>Represent timestamp as  <em><strong>(counter, node ID)</strong></em> so that each timestamp is unique. <code>counter</code> is used to determine order, and <code>node ID</code> work as tie-breaker.<br>Every node and every client keeps track of the <strong>maximum</strong> counter value it has seen so far, and includes that maximum on every request.<br>When a node receives a request or response with a maximum counter value greater than its own counter value, it immediately <strong>increases</strong> its own counter to that maximum (enforcing the timestamp to be in consistent with the causality).</p><p>Difference between <strong>version vectors</strong>: Lamport time‐stamps are more <strong>compact</strong>, but cannot tell whether two operations are concurrent or whether they are causally dependent (<strong>total ordering</strong> is always enforced).</p><hr><h3 id="Total-order-broadcast"><a href="#Total-order-broadcast" class="headerlink" title="Total order broadcast"></a>Total order broadcast</h3><p>It should be noted that Lamport timestamps is not sufficient in some tasks like username creation, because we cannot determine whether concurrent requests are asking for the same name, and which one should win (need knowledge from other nodes). We need not only total ordering of operations, but also when that order is <strong>finalized</strong> (maybe with total <strong>order broadcast</strong>). </p><p>Partitioned databases with a single leader per partition often maintain ordering only <strong>per partition</strong>, which means they cannot offer consistency guarantees (e.g., consistent snapshots, foreign key references) <strong>across partitions</strong>. </p><p><strong>Total order broadcast</strong> or <strong>Atomic Broadcast</strong>: used to scale the system if the throughput is greater than a single leader can handle, and also how to handle failover if the leader fails.<br>Usually described as a <strong>protocol</strong> for exchanging messages between nodes.<br>Requires: <strong>Reliable delivery</strong> (No messages are lost: if a message is delivered to one node, it is delivered to all nodes.) and <strong>Totally ordered delivery</strong> (Messages are delivered to every node in the same order).</p><p><strong>State machine replication</strong>: If every message represents a write to the database, and every replica processes the same writes in the same order, then the replicas will remain consistent with each other (aside from any temporary replication lag). </p><hr><p>An important aspect of total order broadcast is that the order is <strong>fixed</strong> at the time the messages are delivered: a node is not allowed to retroactively insert a message into an earlier position in the order if subsequent messages have already been delivered.<br>This fact makes total order broadcast <strong>stronger</strong> than timestamp ordering </p><p>We can view it as a way of creating a log, delivering a message is like appending to the log.<br>Since all nodes must deliver the same messages in the same order, all nodes can read the log and see the same sequence of messages.</p><p>Total order broadcast is also useful for implementing a <strong>lock</strong> service that provides <strong>fencing tokens</strong>. Every request to acquire the lock is appended as a message to the log, and all messages are sequentially numbered in the order they appear in the log.<br>The sequence number can then serve as a fencing<br>token, because it is monotonically increasing. In ZooKeeper, this sequence number is called <strong>zxid</strong></p><hr><p><strong>Total order broadcast</strong>: <strong>asynchronous</strong>: messages are guaranteed to be delivered reliably in a <strong>fixed order</strong>, but there is no guarantee about <strong>when</strong> a message will be delivered<br><strong>Equivalent</strong> to <strong>consensus</strong>.</p><p><strong>linearizability</strong>: <strong>Recency guarantee</strong>: a read is guaranteed to see the latest value written.</p><p>We can try to use <strong>Total order broadcast</strong> to implement a <strong>linearizable storage</strong> to resolve the username setting task:</p><ol><li>Append a message to the log, decalring the name you want to claim</li><li>Read the log, wait for the message you appended to be delivered back to you.</li><li>Check for any messages claiming the user name you want; If the first is your own message, then it is succesful, otherwise it is claimed by others.</li></ol><p>This is like a compare-and-set, each name initially with value <code>NULL</code> and you want to assigny your userID as value of the name. It is only legal to overwrite name with value <code>NULL</code></p><hr><ol><li>Because log entries are delivered to all nodes in the same order, if there are several <strong>concurrent</strong> writes, all nodes will <strong>agree</strong> on which one came first. </li><li>Choosing the first of the conflicting writes as the winner and aborting later ones ensures that all nodes agree on whether a write was committed or aborted. A similar approach can be used to implement <strong>serializable multi-object transactions</strong> on top of a log.</li><li>Ensures linearizable writes, not <strong>linearizable reads</strong>. Data read from a store which is asynchronously updated from the log can be stale (this procedure provides <strong>sequential consistency</strong> &#x2F; <strong>timeline consistency</strong>, slightly weaker than linearizability).</li></ol><p>To make reads linearizable:</p><ol><li>Append a message to a log, read the log, performing the actual read when the message is delivered back to you so reads can be sequenced.</li><li>If the log allows you to fetch the position of the latest log message in a linearizable way, you can query that position, wait for all entries up to that position to be delivered to you, and then perform the read. (ZooKeepter’s <code>sync()</code> operation)</li><li>Read from a replica that is <strong>synchronously</strong> updated on writes,<br>and is thus sure to be up to date.</li></ol><hr><p>We can also build <strong>total order broadcast</strong> from <strong>linearizable storage</strong>.<br>With a <strong>linearizable register</strong> (or atomic compare-and-set), you can increment and get the linearizable integer, and attach it in the message as sequence number. You can then send the message to all nodes (resend lost messages) and the recipents will deliver the messages consecutively.<br>Unlinke Lamport timestamps, the numbers must form sequence with <strong>no gaps</strong>, making it total order (you will know that you should wait for No.5 messages even you received No.4 and No.6). </p><p>In order to restore the register value when network connections fails, you inevitably get a <strong>consensus</strong> algorithm.<br>Linearizable <strong>compare-and-set</strong> (or <strong>increment-and-get</strong>) register and <strong>total order broadcast</strong> are both equivalent to <strong>consensus</strong></p><hr><ul><li><strong>Consesus</strong> is important in <strong>leader election</strong> (of database with single-leader replication) and <strong>Atomic commit</strong> (in database cross nodes or partitions).</li></ul><p><strong>FPL</strong> result: No algorithm can reach consensus with risk that a node may crash, in async system model and no clock and timeout is allowed.</p><p>With <strong>timeouts</strong> or other methods (even use of random numbers) to detect crashed nodes, then consensus is solvable.</p><h3 id="Atomic-Commit-and-Two-Phase-Commit-2PC"><a href="#Atomic-Commit-and-Two-Phase-Commit-2PC" class="headerlink" title="Atomic Commit and Two-Phase Commit (2PC)"></a>Atomic Commit and Two-Phase Commit (2PC)</h3><p>Atomicity prevents failed transactions from littering the database, and makes sure <strong>secondary index</strong> stays consistent with the primary data.</p><p>For <strong>single</strong> node, a database usually makes the transaction’s write durable (writeahead log, e.g.) then append a commit record to the log on disk (2 phase commit). If node is crashed before commit record is written, then the writes are <strong>rolled back</strong>.</p><p>For <strong>multiple</strong> nodes, we need to ensure all nodes either commit or abort. It is <strong>not</strong> allowed that some commit succeed and some fail.<br>Once committed, a commit is <strong>irrevocable</strong>.<br>However, we can undo a commit by another independent one: <strong>compensating transaction</strong>.</p><hr><p><strong>Implementation</strong>:<br><strong>2PC</strong> introduces a <strong>coordinator</strong> (also known as <strong>transaction manager</strong>) to arrange the commit on all nodes.<br>In 1st phase: Coordinator send request to all nodes and ask if they are able to commit. If anyone responses “no”, then coordinator sends an <strong>abort</strong> request to all nodes.<br>In 2nd phase: Coordinator send commit request, and commit actually takes place (and broadcasted).</p><hr><p><strong>Details</strong> of 2PC implementation:</p><ol><li>Before begin a distributed transaction, application needs to request a <strong>globally unique</strong> transaction ID.</li><li>The application begins a <strong>single-node</strong> transaction on each of the participants (to do all reads and writes), and attaches the globally unique transaction ID. If anything go wrong, coordinator or any participant can abort.</li><li>The coordinator sends a <strong>prepare request</strong> to all participants, tagged with the global transaction ID, when the application is ready to commit. If any request fails &#x2F; times out, the coordinator sends an abort request for that transaction ID to all nodes.</li><li>When a participant receives the prepare request, it makes sure that it can definitely commit the transaction under <strong>all</strong> circumstances. This includes writing all transaction data to disk (a crash, a power failure, or running out of disk space is <strong>not</strong> an acceptable excuse for refusing to commit later), and checking for any conflicts or constraint violations. By replying “yes” to the coordinator, the node promises to commit the transaction without error if requested. In other words, the participant <strong>surrenders the right to abort</strong> the transaction, but without actually<br>committing it.</li><li>When the coordinator has received responses to all prepare requests, it makes a <strong>definitive decision</strong> on whether to commit or abort the transaction (committing only if all participants voted “yes”). The coordinator must write that decision to its transaction log on disk so that it knows which way it decided in case it subsequently crashes. This is called the <strong>commit point</strong>.</li><li>Once the coordinator’s decision has been written to disk, the commit or abort request is sent to all participants. If this request fails or times out, the coordinator <strong>must retry forever</strong> until it succeeds. There is no more going back: if the decision was to commit, that decision must be enforced, no matter how many retries it takes. If a participant has crashed in the meantime, the transaction will be committed when it recovers—since the participant voted “yes,” it cannot refuse to commit when it recovers.</li></ol><p>Thus, the protocol contains two crucial “<strong>points of no return</strong>”: when a participant votes “yes,” it promises that it will definitely be able to commit later (although the coordinator may still choose to abort); and once the coordinator decides, that decision is <strong>irrevocable</strong>.<br>Those promises ensure the <strong>atomicity of 2PC</strong>.<br>(Single-node atomic commit lumps these two events into one: writing the commit record to the transaction log.)</p><hr><p><strong>Coordinator failure</strong>:<br>If one of participant’s <strong>prepare requests</strong> fail or time out, coordinator aborts the transaction.<br>if any of the <strong>commit</strong> or <strong>abort</strong> requests fail, the coordinator retries them <strong>indefinitely</strong> (that is why <strong>2PC</strong> is called <strong>blocking</strong> atomic commit protocol).</p><p>However, if coordinator failed, participants’s state is called <strong>in doubt</strong> or <strong>uncertain</strong>, as they have to wait forever (cannot abort).<br>In <strong>2PC</strong> protocol, the only way is to wait coordinator to <strong>recover</strong>.</p><p>The commit point of 2PC comes down to a regular <strong>single-node atomic commit</strong> on the coordinator.</p><hr><p><strong>3PC</strong> is proposed to make an atomic commit protocol <strong>nonblocking</strong>, but we need assumption that having a network with <strong>bounded</strong> delay and nodes with <strong>bounded</strong> response times.<br>However, in a system without such assumption, <strong>3PC</strong> cannot guarantee atomicity (without a <strong>perfect</strong> failure detector)<br>That is why <strong>2PC</strong> is still in use</p><hr><h3 id="Heterogeneous-distributed-transactions"><a href="#Heterogeneous-distributed-transactions" class="headerlink" title="Heterogeneous distributed transactions"></a>Heterogeneous distributed transactions</h3><p><strong>Database-internal</strong> distributed transactions are easier to deal with, like distributed databases. All nodes (participants) run the <strong>same</strong> database software. They do not have to be compatible with other system, able to use any protocol and do specific optimization.</p><p><strong>Heterogeneous</strong> distributed transactions are harder, e.g., message brokers. Atomic commit <strong>must</strong> be ensured.</p><p><strong>Exactly-once message processing</strong>:  A message from a message queue can be acknowledged as processed if and only if the database transaction for processing the message was successfully committed<br>(<strong>atomically</strong> commit the message acknowledgment and database writes in a <strong>single</strong> transaction).<br>E.g., when a message processing is associlated with sending an email. If either one failed, we can roll back both and safely retry.</p><h3 id="XA-transactions"><a href="#XA-transactions" class="headerlink" title="XA transactions"></a>XA transactions</h3><p><strong>X&#x2F;Open XA</strong> (short for <strong>eXtended Architecture</strong>) is a standard for implementing twophase commit across heterogeneous technologies。<br>The essence is <strong>C API</strong> for interfacing with a transaction coordinator.<br>Assumes that your application uses a network driver or client library to communicate with other participants &#x2F; services. If driver supports XA, then it calls the XA API to figure out if an operation is part of a distributed transaction, if so, sends necessary information to the database server. Driver also exposes <strong>callbacks</strong> through which the coordinatro can ask the participant to prepare, commit or abort.</p><p>The transaction <strong>coordinator</strong> implements the XA API.<br>However, corrdinator is usually just a <strong>library</strong> loaded into the same process as the application issuing the transaction rather than a seperate service.<br>This is bit tricky as coordinator can go with died server.</p><hr><p>The reason that we really care about transaction in <strong>doubt</strong> is that they might be holding locks (if any <strong>exlusive</strong> lock or <strong>2PL</strong> is used).<br>These locks will be holded until coordinator recovers.</p><p>In practice, <strong>orphaned in-doubt transactions</strong> do occur.<br>Coordinator cannot decide their outcome for whatever reason (transaction log may be lost or corrupted). They will sit forever in the database, hold locks and block other transactions.</p><p>Only solution is for an administrator to <strong>manually</strong> decide whether to commit or roll back the transactions (a lot of work, and breaks the <strong>2PC</strong> constraints)</p><p>XA implementation may have emergency escape hatch called <strong>heuristic decisions</strong>: allowing a participant to <strong>unilaterally</strong> decide to abort or commit an in-doubt transaction without a definitive decision from the coordinator. </p><p>Heuristic here is a euphemism for probably breaking atomicity, since it violates the system of promises in two-phase commit.<br>Thus, heuristic decisions are intended only for getting out of <strong>catastrophic situations</strong>, and not for regular use.</p><hr><h3 id="Limitations-of-distributed-transactions"><a href="#Limitations-of-distributed-transactions" class="headerlink" title="Limitations of distributed transactions"></a>Limitations of distributed transactions</h3><ol><li>Limitation of <strong>XA</strong>: transaction coordinator is itself a kind of database (in which transaction outcomes are stored)<br>If it runs on only one single machine, it is a <strong>single point</strong> of failure and do not even have high availability.</li><li>Many server-side applications are developed in a <strong>stateless</strong> model. Coordinator’s logs will break such model.</li><li>XA is a <strong>lowest common denominator</strong>, to be compatible with a wide range of data system. Cannot detect deadlocks across different systems (requiring knowledge on locks), does not work with <strong>Serializable Snapshot Isolation</strong>, SSI (require protocol to identify conflicts across different systems).</li></ol><p>Even for <strong>internal</strong> distributed transactions, where we can implement SSI, failures can still be amplified by <strong>2PC</strong> as we require all participants to respond.</p><hr><h3 id="Fault-Tolerant-Consensus"><a href="#Fault-Tolerant-Consensus" class="headerlink" title="Fault-Tolerant Consensus"></a>Fault-Tolerant Consensus</h3><p>Some properties we need consensus to have:</p><ol><li><strong>Uniform agreement</strong>: No two nodes decide differently.</li><li><strong>Integrity</strong>: No node decides twice.</li><li><strong>Validity</strong>: If a node decides value v, then v was proposed by some node.</li><li><strong>Termination</strong>: Every node that does not crash eventually decides some value (usually, a majority is required as the <strong>quorum</strong>).</li></ol><p>The idea is that everyone decides on the same outcome, and once you have decided, you cannot change your mind.<br>Having a dictator can resolve most properties (except termination), but lacks <strong>fault tolerance</strong>.<br>In fact, the termination property requires that a consensus algorithm must <strong>make progress</strong>.<br>Any algorithm that has to wait for a node (like <strong>2PC</strong>) will NOT have the termination property.</p><p>* It is possible to make consensus robust against <strong>Byzantine faults</strong> as long as fewer than <strong>one-third</strong> of the nodes are Byzantine-faulty</p><hr><p>The best-known fault-tolerant consensus algorithms are <strong>Viewstamped Replication</strong> (VSR), <strong>Paxos</strong>, <strong>Raft</strong> and <strong>Zab</strong>.</p><p>They decide on a sequence of values, which makes them <strong>total order broadcast</strong> algorithms.<br>Remember that total order broadcast requires messages to be delivered <strong>exactly once</strong>, in the <strong>same order</strong>, to <strong>all</strong> nodes. This is equivalent to repeated rounds of <strong>consensus</strong> (decide what to send next, and decide on the next message to be delivered in total order)<br>Total order broadcast is more efficient than doing multiple <strong>value-at-a-time consensus</strong>.</p><hr><h3 id="Epoch-numbering-and-quorums"><a href="#Epoch-numbering-and-quorums" class="headerlink" title="Epoch numbering and quorums"></a>Epoch numbering and quorums</h3><p>Consensus protocols internally uses a leader, but leader is a result of <strong>consensus</strong>. Such protocols do not guarantee that the leader is unique.</p><p>We can make a weaker guarantee with <strong>epoch numbering</strong> (<strong>ballot number &#x2F; view number &#x2F; term number</strong>), and guarantee that within each epoch, the leader is unique. Every time the current leader is thought to be dead, a vote is started among the nodes to elect a new leader.</p><p> This election is given an <strong>incremented</strong> epoch number, and thus<br>epoch numbers are <strong>totally ordered</strong> and <strong>monotonically increasing</strong>. </p><ol><li>If there is a conflict between two different leaders in two different epochs (perhaps because the previous leader actually wasn’t dead after all), then the leader with the <strong>higher</strong> epoch number prevails (so we remove the previous leader even it recovers).</li><li>For every decision that a leader wants to make, it must send the proposed value to the other nodes and wait for a <strong>quorum</strong> (usually a majority of nodes) of nodes to respond in favor of the proposal.</li><li>A node votes in favor of a proposal only if it is <strong>not aware</strong> of any other leader with a higher epoch.</li><li>Have two rounds of voting: once to choose a <strong>leader</strong>, and a second time to vote on a leader’s <strong>proposal</strong>. The key insight is that the quorums for those two votes must <em>overlap</em>: if a vote on a proposal succeeds, at least one of the nodes that voted for it must have also participated in the most recent leader election. </li><li>The leader got to know if it is still holds the leadership.</li><li>Consensus algorithms define a <strong>recovery process</strong> by which nodes can get into a consistent state after a new leader is elected, ensuring that the safety properties are always met.</li></ol><p><strong>Limitations of Consensus</strong>: </p><ul><li>Vote on proposal is actually <strong>synchronous</strong> replication, and can result in data loss.</li><li>Needs <strong>strict majority</strong> to operate. Most consensus algorithms assume a <strong>fixed</strong> set of nodes participate in voting, means you cannot just add &#x2F; remove nodes in the cluster (may be resolved by dynamic membership).</li><li><strong>Sensitive</strong> to network problems, relies on timeout for failure detection.</li></ul><hr><p>Consesus Algorithm Projects like ZooKeepter are very useful for <strong>distributed coordination</strong>. It is limited as it can only hold small amount of data able to fit in memory, and replicated across all node using <strong>fault tolerant total order broadcast</strong> (also <strong>Linearizable atomic operations</strong> are implemented to implement lock).</p><p><strong>Failure detection</strong>: Clients maintain and long-lived session on ZooKeeper servers to exchange <strong>heartbeats</strong>, if it cease for longer than the session timeout, session is declared to be dead and locks are released (ZooKeeper calls these <strong>ephemeral nodes</strong>).</p><p><strong>Change notifications</strong>:  One client read locks and values that were created by another client, and can also watch them for changes. Then it can know if a client joins the cluster or fails, without frequently <strong>poll</strong> to find out.</p><p>Only the linearizable atomic operations really require consensus.</p><p><strong>Use Cases</strong>: <strong>ZooKeeper</strong> can be used for <strong>leader election</strong>, <strong>partition assigning</strong>, <strong>rebalancing</strong> and taking over <strong>failed</strong> node’s work (maybe even service discovery). It runs on fixed number of nodes, performs its majority votes among them and supports potentially large number of clients. Normally, the kind of data managed by ZooKeeper is quite <strong>slow-changing</strong> (not suitable to store runtime state of application)</p>]]>
    </content>
    <id>https://umiao.github.io/2024/04/27/Designing-Data-Intensive-Applications-Note-17/</id>
    <link href="https://umiao.github.io/2024/04/27/Designing-Data-Intensive-Applications-Note-17/"/>
    <published>2024-04-27T18:27:31.000Z</published>
    <summary>Discussion on challenges with consensus.</summary>
    <title>Designing Data-Intensive-Applications-Note-17</title>
    <updated>2026-03-10T01:09:07.003Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Designing Data-Intensive-Applications" scheme="https://umiao.github.io/tags/Designing-Data-Intensive-Applications/"/>
    <content>
      <![CDATA[<p>Discussion on challenges with distributed systems.</p><img src="/2024/04/21/Designing-Data-Intensive-Applications-Note-16/cover.png" class="" title="ML_note"><span id="more"></span><h3 id="Faults-and-Partial-Failure"><a href="#Faults-and-Partial-Failure" class="headerlink" title="Faults and Partial Failure"></a>Faults and Partial Failure</h3><p>We expect outcomes of program running on a single computer either returns correct result, or crash (without returning wrong results)<br><strong>Idealized system model</strong>: operates with perfection, <strong>CPU instruction</strong> does the same thing, data in <strong>memory and disk</strong> remains intact and not randomly corrupted.</p><p>However, in distributed system, we can easily find <strong>partial failure</strong> that some parts of the system that are broken in unpredicatable ways but remaining parts working fine.<br>You may not even know if something succeeded or not, as the time it takes for message traveling across netowrk is <strong>nondeterministic</strong></p><p>Large-scale computing systems’ design logic is between <strong>High-performance Computing (HPC)</strong> and <strong>Cloud Computing</strong>. </p><p>The prior is more like a single node computer (if failure happens, fix it and restart entire cluster workload from checkpoint [can be expensive]]). Also, it has node communication through shared memory and <strong>Remote Direct Momoery Acess (RDMA)</strong>, use specialized hardware and more reliable. Also, having its nodes close together.</p><p>The latter focuses on high availability, based on IP and Ethernet, arranged in Clos topologies to provide high bisection bandwidth.<br>System with fault tolerance can do things like <strong>rolling upgrade</strong>, or kill-n-restart virtual node.</p><hr><p><strong>Fault Tolerance</strong>: It is important to consider wide range of possible faults, even fairly unlikely ones. Fault handling mech should be part of the software design.</p><p>You can construct a more reoliable system from a less reliable underlying base. E.g., using <strong>error-correcting</strong> codes to detect random bit error during transmission, and use <strong>Transmission Control Protocol</strong> (TCP) to form more reliable transport layter on top of IP (<strong>Internet Protocol</strong>)<br>However, these methods have their own limit of how much error can be detected and handled, and TCP cannot resolve network latency issue.</p><hr><p><strong>Unreliable Network</strong>: Usually, one node can send a message &#x2F; packet to another node, but there is <strong>no guarantee</strong> when it will arrive, or whether it will arrive at all.<br>Sender cannot tell if the packet was delivered, because recipent’s response message can be lost. Usual way of handling this is setting a <strong>timeout</strong>.<br>* Redundancy in data center setting cannot prevent <strong>human error</strong>, which is a main reason for outage.</p><p><strong>Detecting Faults</strong>: Many systems need to detect faulty nodes, e.g., load balancer stop sending request to dead node, and new leader may need to be elected in single-leader setting database.<br>However, it is still hard to get response, or get to know how much data is processed before failure happened. If node’s operating system is still running, another node may be able to take over.</p><hr><h3 id="Timeout-and-Unbounded-Delays"><a href="#Timeout-and-Unbounded-Delays" class="headerlink" title="Timeout and Unbounded Delays"></a>Timeout and Unbounded Delays</h3><ol><li><strong>Timeout</strong> can be the only sure way of detecting a fault, but the limit is hard to set.</li><li>Falsely declaring a <strong>node dead</strong> is problematic, may ended up performing actions twice. Also, extra load would be placed on other nodes, and cause <strong>cascading failure</strong> (extremely all nodes can be declared dead and stop working).</li><li>If delivery time is bounded by $d$ and request processing time is bounded by $r$, then we can set timeout limit as $2d+r$. Unfortunately, most systems have <strong>unbounded delays</strong>.</li></ol><p><strong>Network Congestion</strong>: Multiple different nodes <strong>simultaneously</strong> try to send packets to the <strong>same destination</strong>, the network switch must queue them up and feed them into the destination network link one by one. Then packet may need to wait for a slot.<br>* Packet may be dropped if switch queue is full.</p><p>Also, even a packet reaches the destination machine, we may still need to be queued by the <strong>operating system</strong> if the system is busy, or need to wait to get a CPU core in a <strong>virtualized</strong> environment.<br><strong>TCP</strong> performs <strong>flow control</strong> (also known as <strong>congestion avoidance</strong> or <strong>backpressure</strong>), in which a node limits its own rate of sending in order to avoid overloading a network link or the receiving node (additional queueing will be needed). </p><p>* <strong>TCP</strong> would consider a packet to be lost if it is not acknowledged within the timeout. Application will not see the loss and retreansmission, but will see the latency.</p><p><strong>TCP v.s. UDP</strong>: In <strong>latency-sensitive</strong> application, like video conferencing, use UDP rather than TCP because delayed data will be worthless. UDP does <strong>not</strong> perform flow control and does not retransmit loss packets. Human can ask for retransmission instead.</p><p><strong>Determine timeout</strong>: Queueing delays have especially wide range when a system is close to its max capacity and long queue can be built.<br>You can only choose timeouts <strong>experimentally</strong> to learn the distribution of expected <strong>variability of delays</strong>.</p><p>More ideally, systems can continually measure response times and their variability (<strong>jitter</strong>), and automatically <strong>adjust</strong> timeouts according to the observed response time distribution.<br>This can be done with a <strong>Phi Accrual failure detector</strong></p><h3 id="Phi-Accural-Failure-Detector"><a href="#Phi-Accural-Failure-Detector" class="headerlink" title="Phi Accural Failure Detector"></a>Phi Accural Failure Detector</h3><p>Traditionally, we can use <strong>heartbeat</strong> for failure detector. Rather than setting the heartbeat timeout to a <strong>constant</strong> value, we assume the heartbeat message matches certain type of probability model. E.g., the interval obeys the normal distribution.<br>Then, we can use the history data in the <strong>sliding window</strong>, to estimate the probability parameters with <strong>maximum likelihood</strong> estimation and calculates the probability of receiving heartbeat message at this moment.</p><ul><li>If the node is invalid, we want the $\lim_{t \rightarrow \infty}\phi(t)&#x3D;\infty$</li><li>$\exists t_0$ such that $\forall x_1 \ge x_2 \ge t_0, \phi(x_1) \ge \phi(x_2)$.</li><li>If the node is valid, then $\phi(t)$ is bounded.</li><li>If the node is valid, then $\exists t_0$ such that $\forall x \ge t_0, \phi(x) &#x3D;0$.</li></ul><p>$\phi(t_{now})$ is defined as $-\log_{10}(P_{later} (t_{now} - T_{last}) )$<br>$P_{later} (t) &#x3D; \frac{1}{\sigma \sqrt{2 \pi}} \int_t^{+\infty} e^{-\frac{(x-\mu)^2}{2\sigma ^2}}dx $</p><p>Here, $\mu$ and $\sigma$ can be estimated with the data in the <strong>sampling window</strong>.</p><p>Each node &#x2F; subprocess can use their own threshold to compare with (other node’s) $\phi(t)$ shared by the failure detector.</p><hr><p>* When you make a call over the <strong>telephone network</strong>, it would establish a <strong>fixed</strong>, <strong>guaranteed</strong> amount of bandwidth through the entire route between callers.<br> This kind of network is <strong>synchronous</strong>: even as data passes through several routers, it does not suffer from queueing, because the 16 bits of space for the call have already been reserved in the <strong>next hop</strong> of the network. And because there is no queueing, the maximum end-to-end latency of the network is fixed. We call this a <strong>bounded delay</strong>.</p><p>The packets of a TCP connection <strong>opportunistically</strong> use whatever network bandwidth is available.<br>They are <strong>packet switching</strong>, instead of <strong>circuit-switched</strong> networks, as they are optimized for <strong>bursty traffic</strong>. It can be hard to <strong>guess</strong> a proper bandwidth allocation. TCP can adapt the rate <strong>dynamically</strong>.</p><p><strong>Static</strong> resource allocation can provide <strong>latency guarantees</strong>, but at the cost of <strong>reduced utilization</strong>.</p><hr><h3 id="Unreliable-Clocks"><a href="#Unreliable-Clocks" class="headerlink" title="Unreliable Clocks"></a>Unreliable Clocks</h3><p>Usually each machine has its own clock, which makes determining the <strong>order</strong> difficult in case of concurrency.<br>This makes figuring request <strong>timeout</strong> &#x2F; getting <strong>timestamps</strong> &#x2F; scheduling services… more challenging.</p><p>We can <strong>synchronize</strong> clocks to some degree with <strong>Network Time Protocol (NTP)</strong>.<br>NTP uses <strong>hierarchical</strong>, <strong>semi-layered</strong> system of time sources. Each level of hierachy is termerd a <strong>stratum</strong>, assigned with a number starting from 0. A server synchronized to a stratum $n$ server runs at stratum $n+1$.<br>* Stratum is <strong>not</strong> always an indication of quality or reliability</p><ul><li>Stratum 0: High-precision timekeeping devices such as <strong>atomic clocks</strong>, <strong>GNSS</strong> (including GPS) or other radio clocks,</li><li>Stratum 1: Synced within a few microseconds of stratum 0 devices. </li><li>Stratum 2: Synced with the above level.</li></ul><p>The NTP algorithms on each computer interact to construct a <strong>Bellman–Ford</strong> shortest-path spanning tree, to minimize the accumulated round-trip delay to the stratum 1 servers for all the clients.</p><p>The <strong>upper limit</strong> for stratum is 15; stratum 16 is used to indicate that a device is <strong>unsynchronized</strong></p><p><strong>Example</strong>: We will have 4 timestamps $T_1, … T_4$. Here, client send a request on $T_1$, it reaches the server on $T_2$, response is sent on $T_3$ and arrives on $T_4$.<br>We can estimate $delay &#x3D; \frac{T_4 - T_1 - (T_3 - T_2)}{2}$, and the offset satisfies: $\text{offset} + T_4 &#x3D; T_3 + \text{delay}$, so we can figure out the offset and adjust the clock.</p><hr><p>Modern computers have at least 2 kinds of clocks: a <strong>time-of-day clock</strong> (returns date and time according to some calendar, known as <strong>wall-clock time</strong>, like seconds since the epoch:midnight on 1st Jan 1970, might jump back) and a <strong>monotonic clock</strong> (for measuring a <strong>duration</strong>, never jumps back, the unit might be in nano-seconds).</p><p><strong>NTP</strong> may adjust the frequency at which the monotonic clock moves forward, if the local quartz is moving faster or slower (allow error up to 0.05%). Such error is tolerable in distributed systems.</p><p>There lies some challenges in getting the clock synchronized:</p><ol><li>Quartz clock in a computer can <strong>drift</strong>, depending on the temperature. It may be a 17 seconds drift for a clock resynchronized once a day.</li><li>Local clock may refure to synchronize if it differs too much from an <strong>NTP</strong> server, or local clock be forcibly reset.</li><li><strong>NTP</strong> synchronization can only be as good as the network delay. Itself may go wrong as well.</li><li><strong>Leap seconds</strong> result in a minute has 59 &#x2F; 61 seconds. NTP server can perform the leap second adjustment gradually over the course of a day (called <strong>smearing</strong>)</li><li>Hardware clock can be virtualized in virtual machines, and each VM can <strong>pause</strong> for tens of miliseconds and clock jumps forward.</li><li>You cannot trust user device’s clock setting.</li></ol><p>Inaccurate clock can casue lost of updates, under setting of <strong>Last Write Wins</strong>.<br>A node with <strong>lagging</strong> clock cannot overwrite values previously written by a node with a <strong>fast</strong> clock, casuing data be <strong>silently</strong> dropped.<br><strong>LWW</strong> cannot distinguish truly <strong>concurrent</strong> writes (writes are not aware of each other) and writes that occurred <strong>sequentially</strong> in quick succession (writers are aware of others).<br>Clock reading should be viewed as a range within a <strong>confidence interval</strong>, rather than a point in time.</p><p>We may need <strong>tie breaker</strong>, if two nodes generate the same timestamp.<br><strong>Logical clocks</strong> based on <strong>incrementing counters</strong> may be a better solution, only measuring the relative ordering of events.</p><hr><h3 id="Synchronized-clocks-for-global-snapshots"><a href="#Synchronized-clocks-for-global-snapshots" class="headerlink" title="Synchronized clocks for global snapshots"></a>Synchronized clocks for global snapshots</h3><p>The most common implementation of snapshot isolation requires a <strong>monotonically increasing</strong> transaction ID.<br>On single node, this can be generated by a counter.<br>However, it is challenging for database distributed across many machines &#x2F; data centers, because transaction ID must reflect <strong>causality</strong>, but we have <strong>clock accuracy</strong> concern. </p><ol><li><strong>TrueTime API</strong> can report clock’s <strong>confidence interval</strong>, and we can use it to determine if two time reading overlap.</li><li>We can wait the length of the confidence interval before committing a <strong>read-write</strong> transaction.<br>Above is used in <strong>Google</strong> only</li></ol><hr><h3 id="Process-Pauses"><a href="#Process-Pauses" class="headerlink" title="Process Pauses"></a>Process Pauses</h3><p>In single leader setting database, leader has to know if it is <strong>not</strong> declared by others and safely accept writes.<br>one options is let leader obtain a <strong>lease</strong> from other nodes, which is similar to a lock with a timeout. If the leader dies, it will not renew the lease and another node can take over.</p><p>However, if we want to use a timer in the implementation, we will have the clock issue (if different nodes’ clocks go out-of-sync).</p><p>Even we use a <strong>local monotinic clock</strong>, it is still possible that an <strong>unexpected pause</strong> happens after the leader checked that it still hold the lease. Another node may become the leader during this time, but the old leader is not aware of that.</p><p><strong>Potential Reasons of such pause</strong>:</p><ol><li>(Stop the world) <strong>Garbage Collection</strong>, may stop all running threads. [One emerging idea is to treat <strong>GC</strong> pause like brief planned outages of a node, used in some <strong>latency-sensitive</strong> financial trading systems]</li><li>Suspended <strong>virtual machine</strong> (to migrate to another node, for example).</li><li>In end-user devices like latops, execution can be suspended.</li><li><strong>Context Switch</strong> between threads or virtual machines. CPU time spent in other virtual machines is known as <strong>steal time</strong>.</li><li>Slow <strong>I&#x2F;O</strong>, or delay caused by <strong>paging</strong> (swaping to disk). Servers can disable paging to avoid <strong>thrashing</strong> (spend most of the time swapping pages)</li></ol><p>You can’t assume anything about timing, because arbitrary <strong>context switches</strong> and <strong>parallelism</strong> may occur (just like making multi-threaded code).<br>For distributed systems, we do not have tools like <strong>mutexes</strong>, <strong>semaphores</strong>, <strong>atomic counters</strong>, <strong>lock-free</strong> data structures, <strong>blocking queues</strong>, and so on.</p><hr><p><strong>Response time guarantees</strong>: In some systems (like rocket, called <strong>hard real-time systems</strong>), we have a deadline by which the software <strong>must</strong> respond. </p><p>In <strong>embedded systems</strong>, real-time means that a system is carefully designed and tested to meet specified <strong>timing guarantees</strong> in all circumstances.<br>In <strong>network</strong>, real-time means servers pushing data to clients and stream processing without hard response time constraints</p><p>Providing real-time guarantees in a systems can be challenging, as it requires: a <strong>real-time operating system</strong> (RTOS) that allows processes to be scheduled with a guaranteed allocation of CPU time in specified intervals is needed; library functions must document their <strong>worst-case</strong> execution times; dynamic memory allocation may be <strong>restricted</strong> or disallowed entirely (real-time garbage collectors exist, but the application must still ensure that it doesn’t give the GC too much work to do); and an enormous amount of testing and measurement must be done.</p><hr><p>It should be noted that even if a node believes that it is leader (or hold the lease, etc), that doesn’t necessarily mean a <strong>quorum</strong> of nodes agrees!<br>* We should not do the validation only on application end. We need the resource &#x2F; data itself in checking tokens.</p><p><strong>Fencing</strong>: Every time the lock server grants a lock or lease, it also returns a <strong>fencing token</strong>, which is a number that increases every time a lock is granted (e.g., incremented by the lock service). We can then require that every time a client sends a write request to the storage service, it must <strong>include</strong> its current fencing token.<br>If server ever processed a write with a higher token number, it rejects the write with old token.</p><p>If <strong>ZooKeeper</strong> is used as lock service, the transaction ID <strong>zxid</strong> or the node version <strong>cversion</strong> can be used as fencing token. Since they are guaranteed to be <strong>monotonically increasing</strong></p><h3 id="Byzantine-Faults"><a href="#Byzantine-Faults" class="headerlink" title="Byzantine Faults"></a>Byzantine Faults</h3><p>Fencing tokens can work well (with <strong>honest</strong> but <strong>unreliable</strong> assumption). However, if it is not honest, it could fake such token and cause <strong>Byzantine fault</strong> (reaching <strong>consensus</strong> in this <strong>untrusting</strong> environment).</p><p>The problem needs $n$ generals who need to agree, but we have some traitors among them sending fake messages.<br>A system is <strong>Byzantine fault-tolerant</strong> if it can tolerate nodes which are <strong>malfunctioning</strong> and <strong>not</strong> obeying the protocol, or if <strong>malicious</strong> attackers are interfering with the network.</p><p>We can safely assume there is no Byzantine fault in most datacenters &#x2F; databases. Such solution can be complicated ted and need support from hardware level.<br>Usually, the server has <strong>central authority</strong> to determine what kind of behavior is allowed and do protection with <strong>authentication</strong>, <strong>access control</strong>, <strong>encryption</strong>, <strong>firewalls</strong>, and so on. </p><p>Also, if you deploy same software to all nodes, <strong>Byzantine fault-tolerant algorithm</strong> may not help, because we expect the error to be independent so that we can some nodes functioning correctly.</p><hr><h3 id="Timing-Assumption-Model"><a href="#Timing-Assumption-Model" class="headerlink" title="Timing Assumption Model"></a>Timing Assumption Model</h3><ol><li><strong>Synchronous model</strong>: bounded network delay, bounded process pauses,<br>and bounded clock error. Not very realistic as unbounded delays can occur.</li><li><strong>Partially synchronous model</strong>: Behaves like a synchronous system most of the time, but exceeds the bounds sometimes. </li><li><strong>Asynchronous model</strong>: No any timing assumptions, not even have a clock.</li></ol><p>Common Models of <strong>System (Node) Failure</strong>:</p><ol><li><strong>Crash-stop faults</strong>: Only crash may happen, then the node gone forever.</li><li><strong>Crash-recovery faults</strong>: May recover after sometime if crashed. Nodes are assumed to have <strong>stable storage</strong> (i.e., non-volatile disk storage), while the <strong>in-memory</strong> state is assumed to be lost. Usually most useful.</li><li><strong>Byzantine (arbitrary) faults</strong>: Node can do anything, including being <strong>dishonest</strong></li></ol><hr><p>We may be interested in the following properties under the above failure models:</p><ol><li><strong>Correctness</strong>: We can write down the properties we want a distributed algorithm to have, to define correctness.</li><li><strong>Safety and liveness</strong>: Usually we can distinguish <strong>liveness</strong> properties from <strong>safety</strong> properties, as prior usually include the word “eventually” in their definition. Safety means nothing bad happens, but liveness means something good eventually happens.</li><li><strong>Formal Definition</strong>: If <strong>safety</strong> property is viloated, we can point out a particular time at which it was broken, and such violation cannot be undone. For <strong>liveness</strong> property, it may not hold at some point in time, but there is always hope that it may be satisfied in the future.</li></ol><p>For distributed algorithms, it is common to require safety properties always hold (to ensure it does not return a <strong>wrong</strong> result in terms of failure). For liveness properties, we can make caveats and state that only if majority of nodes have not crashed, we can say a request needs to receive a response.</p>]]>
    </content>
    <id>https://umiao.github.io/2024/04/21/Designing-Data-Intensive-Applications-Note-16/</id>
    <link href="https://umiao.github.io/2024/04/21/Designing-Data-Intensive-Applications-Note-16/"/>
    <published>2024-04-21T16:54:34.000Z</published>
    <summary>Discussion on challenges with distributed systems.</summary>
    <title>Designing Data-Intensive-Applications-Note-16</title>
    <updated>2026-03-10T01:09:07.003Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Designing Data-Intensive-Applications" scheme="https://umiao.github.io/tags/Designing-Data-Intensive-Applications/"/>
    <content>
      <![CDATA[<p>Discussion on transaction related topics.</p><img src="/2024/03/29/Designing-Data-Intensive-Applications-Note-15/cover.png" class="" title="ML_note"><span id="more"></span><p>A lot of things may go wrong (database software and hardware &#x2F; application &#x2F; network &#x2F; clients racing &#x2F; dirty data &#x2F; concurrency), and that is why we need <strong>transactions</strong>: To group reads and writes together into a logical unit(<strong>commit</strong>), succeeds or fails (<strong>abort</strong>, <strong>rollback</strong>) as one.</p><p>Transactions are created for simplifying <strong>programming model</strong> for applications accessing a database, to ignore certain error &#x2F; concurrency issues, as database has <strong>safety guarantees</strong>.</p><p>Transactions on <strong>relational</strong> databases are similar.<br>As <strong>NoSQL</strong> becomes famous, many databases abandoned transactions or describe a much weaker set of guarantees (due to scalability and availability concern).</p><h3 id="Atomicity-Consistency-Isolation-and-Durability-ACID"><a href="#Atomicity-Consistency-Isolation-and-Durability-ACID" class="headerlink" title="Atomicity, Consistency, Isolation, and Durability (ACID)"></a>Atomicity, Consistency, Isolation, and Durability (ACID)</h3><p>Systems that do not meet the ACID criteria are sometimes called <strong>BASE</strong>, which stands for <strong>Basically Available</strong>, <strong>Soft state</strong>, and <strong>Eventual consistency</strong> (also very vague concept).</p><ul><li><p><strong>Atomicity</strong>:  <strong>atomic</strong> refers to something that cannot be broken down into smaller parts, means no way a thread can see half-finished result of a operation.<br>System can either be in state of <strong>before</strong> operation or <strong>after</strong> opertion.<br>If a transaction was aborted, the application can be sure that it didn’t change anything, so it can safely be <strong>retried</strong>.</p></li><li><p><strong>Consistency</strong>: Certain statements about your data (invariants) that must always be <strong>true</strong>. E.g., credits and debits across all accounts must be balanced.<br>Application is responsible for defining transactions correctly to preserve consistency, not up to the database alone.</p></li><li><p><strong>Isolation</strong>: Multiple clients operating on the same record may run into <strong>concurrency</strong> problems &#x2F; <strong>race conditions</strong>.<br>Then we need to isolate transactions.<br>However, <strong>serializability</strong> is too expensive to be used, so we often use looser isolation level, e.g., <strong>snapshot isolation</strong>. </p></li><li><p><strong>Durability</strong>: Data written to <strong>non-volatile</strong> storage (single-node) or copied to some number of nodes (multi-node).</p></li></ul><p>There may <strong>NOT</strong> be perfect solution when choosing between replication and write to SSD &#x2F; disk.</p><hr><p><strong>Multi-Object Transaction</strong><br>If multiple objects are to be operated in a transation, then we need to determine which read &#x2F; write belong to the same transaction.</p><p><strong>In relational DB</strong>: on any particular TCP connection &#x2F; everything between <code>BEGIN TRANSACTION</code> and <code>COMMIT</code> is considered to be of the same transaction.</p><p><strong>For non-relational DB</strong>, we may not have such guarantees and ended up with partially updated state.</p><p><strong>Examples</strong> about why multi-object transaction is needed:</p><ol><li>Keep <strong>foreign key</strong> reference up-to-date.</li><li>When updating <strong>denormalized</strong> information and keep it in sync (rather than normalized document as a whole).</li><li>In databases with <strong>secondary indexes</strong>, the indexes also need to be updated.</li></ol><hr><p><strong>Single-Object Transaction</strong><br>Storage engines universally provide atomicity (can be implemented by <strong>log</strong> for crash recovery) and isolation (use <strong>lock</strong> on each object) on the level of single object (like <strong>key-value pair</strong>).</p><p>More complex atomic operations like <strong>increment</strong> operation can be used to remove the need for a <strong>read-modify-write</strong> cycle<br>Similarly popular is a <strong>compare-and-set</strong> operation, which allows a write to happen only if the value has not been concurrently changed by someone else.</p><p>Note that single-object transaction is <strong>NOT</strong> transactions in the usual sense (as it did not group multiple operations on multiple objects into one unit of execution).</p><hr><p>Retrying failed transaction is not perfect solution:</p><ol><li>Transaction succeeded but <strong>network</strong> failed, then retrying transaction may cause it perform twice (if without additional <strong>application-level deduplication mechanism</strong>).</li><li>If the error is due to overload, retrying the transaction will make the problem worse, not better. To avoid such feedback cycles, you can limit the number of retries, use <strong>exponential backoff</strong>, and handle overload-related errors differently (if possible).</li><li>It is only worth retrying after transient errors (for example due to <strong>deadlock</strong>, <strong>isolation violation</strong>, temporary network interruptions, and <strong>failover</strong>); after a permanent error (e.g., <strong>constraint violation</strong>) a retry would be pointless.</li><li>If the transaction also has <strong>side effects</strong> outside of the database, those side effects may happen even if the transaction is aborted (like sending emails). This can be solved by <strong>two-phase commit</strong>.</li><li>If the client process fails while retrying, any data it was trying to write to the database is <strong>lost</strong>.</li></ol><hr><h1 id="Weak-Isolation-Levels"><a href="#Weak-Isolation-Levels" class="headerlink" title="Weak Isolation Levels"></a>Weak Isolation Levels</h1><p>As mentioned before, Serializable isolation is too expensive so people would use <strong>weaker level of isolations</strong> to protect against some concurrency issues, but not all.</p><h3 id="Read-Committed"><a href="#Read-Committed" class="headerlink" title="Read Committed"></a>Read Committed</h3><p>The most basic level of transaction isolation is read committed. It makes two guarantees:</p><ol><li>When reading from the database, you will only see data that has been committed (no <strong>dirty reads</strong>).</li><li>When writing to the database, you will only overwrite data that has been committed (no <strong>dirty writes</strong>).</li></ol><p>If a transaction needs to update several objects, read committed can guarantee that database is not partially updated (keep <strong>consistency</strong>);<br>Also database will not see <strong>later rolled back</strong> data (never actually committed to the database.)</p><p>Usually later writes will be delayed until the first write’s transaction has committed or aborted.<br>However, it does not prevent the <strong>race condition</strong> between two counter increments.</p><p>It is most common to use <strong>row-level locks</strong> to implement read committed. This is fine for requiring read locks to read, but may have performance issues if such lock is required for write purpose, as it can block a lot of read transactions, bad for <strong>operability</strong> and have <strong>knock-on effect</strong>.</p><p>An alternative is, for every object that is written, the database remembers <strong>both</strong> the old committed value and the new value set by the transaction that currently holds the write lock.<br>While the transaction is ongoing, any other transactions that read the object are simply given the old value. Only when the new value is committed do transactions, switch over to reading the new value.</p><h3 id="Snapshot-Isolation-and-Repeatable-Read"><a href="#Snapshot-Isolation-and-Repeatable-Read" class="headerlink" title="Snapshot Isolation and Repeatable Read"></a>Snapshot Isolation and Repeatable Read</h3><p>Read Committed may not be sufficient.<br><strong>Non-repeatable read</strong> &#x2F; <strong>read skew</strong>: Before &#x2F; after a commit, a different value would be seen. E.g., if you transfer from an account to another, then at certain point, the sum of two account balances may not be accurate.</p><p>Some situations can <strong>NOT</strong> tolerate this:</p><ul><li><strong>Back Up</strong>: During maybe hours of backing up, writes will still be made to the database. Thus, you could end up with some parts of the backup containing an older version of the data, and other parts containing a newer version. If you need to restore from such a backup, the inconsistencies (such as disappearing money) become <strong>permanent</strong>.</li><li><strong>Analytic queries</strong> and <strong>integrity checks</strong>: Sometimes, you may want to run a query that <strong>scans over</strong> large parts of the database or may be part of a periodic <strong>integrity check</strong> that everything is in order (monitoring for data corruption).  These queries are likely to return nonsensical results if they observe parts of the database at different points in time.</li></ul><p><strong>Snapshot isolation</strong>: each transaction reads from a consistent snapshot of the database, and see the data was committed in the database at the start of the transaction (will not see changes happen after that).</p><p><strong>Implementation</strong>: Use write locks to prevent dirty write;<br>a transaction makes a write can block other transactions writing to the same object;<br>read is not blocked.<br>There is <strong>NO lock contention</strong> between reader and writer.</p><p><strong>Multi-Version Concurrency Control</strong> (MVCC): potentially keep<br>several different committed versions of an object, because various in-progress transactions may need to see the state of the database at different points in time. </p><p>If database only need read committed isolation, then only 2 versions is needed (committed version, and overwritten but not yet committed version). However, MVCC is more frequently used.</p><h4 id="Implementation"><a href="#Implementation" class="headerlink" title="Implementation"></a>Implementation</h4><ul><li>When a transaction is started, it is given a <strong>unique</strong>, <strong>always-increasing</strong> transaction ID (<strong>txid</strong>).  (You cannot see changes made by a larger transaction ID)</li><li>Whenever a transaction writes anything to the database, the data it writes is tagged with the transaction ID of the <strong>writer</strong>. </li><li>Each row in a table has a <code>created_by</code> field, containing the ID of the transaction that inserted this row into the table.</li><li>Each row has a <code>deleted_by</code> field, which is initially empty. If a transaction deletes a row, the row isn’t actually deleted from the database, but it is marked for deletion by setting the <code>deleted_by</code> field to the ID of the transaction that requested the deletion. </li><li>At some later time, when it is certain that no transaction can any longer access the deleted data, a <strong>garbage collection</strong> process in the database removes any rows marked for deletion and frees their space.</li></ul><p><strong>Visibility Rules</strong>:</p><ol><li>List all other transactions are <strong>in progress</strong> (not committed or aborted), ignore their changes. </li><li>Ignore the <strong>aborted</strong> transactions.</li><li>Ignore writes made by transactions with a <strong>later</strong> transaction ID, regardless of whether they are committed.</li><li>All other writes are visible.</li></ol><p>By never updating values in place but instead creating a new version every time a value is changed, the database can provide a <strong>consistent snapshot</strong> while incurring only a small overhead.</p><hr><h3 id="Indexes-and-snapshot-isolation"><a href="#Indexes-and-snapshot-isolation" class="headerlink" title="Indexes and snapshot isolation"></a>Indexes and snapshot isolation</h3><ol><li>Have the index point to <strong>all</strong> versions of an object, require an <strong>index query</strong> to filter out any object versions that are not visible to the current transaction. When garbage collection removes old object versions no longer visible to any transaction, corresponding index entries can be removed.</li><li>Like <strong>B-Tree</strong>, we can use an append-only &#x2F; copy-on-write variant which does not overwrite pages of the tree when they are updated, but instead creates a new copy of each modified page. The parent pages up to the root of the tree, are copied and updated to point to the new versions of the child pages.</li><li>With append-only B-trees, every write transaction creates a <strong>new B-tree root</strong>, and a particular root is a consistent snapshot of the database at the point in time it was created. (no need to filter on transaction IDs because subsequent writes cannot modify an existing tree, but can only create new roots [needs <strong>compaction</strong> and <strong>garbage collection</strong>])</li></ol><p>* For the 1st method, we can try to fit different versions of the same object on the same page.<br>** Snapshot Isolation has many names like <strong>serializable</strong> &#x2F; <strong>repeatable read</strong>.</p><h3 id="Prevent-Lost-Updates"><a href="#Prevent-Lost-Updates" class="headerlink" title="Prevent Lost Updates"></a>Prevent Lost Updates</h3><p>Updates may lost, if concurrent applications are in a <strong>read-modify-write cycle</strong>, because the write back value may not contain other application’s updates (later write clobbers the earlier write).</p><p>This can be resolved by <strong>atomic write</strong> operations, implemented by an <strong>exclusive lock</strong> on the object when it is read so no other transaction can read it until the update has been applied.<br>Sometimes it is called <strong>cursor stability</strong>.</p><p>Or we can force all atomic operations to be executed on a <strong>single thread</strong>.</p><p>Object-relational mapping frameworks make it easy to accidentally<br>write code that performs <strong>unsafe</strong> read-modify-write cycles instead of using atomic operations provided by the database.</p><hr><p><strong>Explicitly Lock</strong>: Also, we can require application to <strong>explicitly lock</strong> objects to be updated, and implement more complex validation logic (like checking whether move of piece of chess is legal)</p><p><strong>Automatically detecting lost updates</strong>: We canc allow writes to execute in parallel, and if the transaction manager detects a lost update, abort the transaction and force it to <strong>retry</strong> its read-modify-write cycle. This is good as it does not require application code to use any special database features.</p><p><strong>Compare-and-set</strong>: The atomic <strong>compare-and-set</strong> opeartion to avoid lost updates by allowing an update to happen only if the value has not changed since you last read it. Otherwise, retry the read-modify-write loop.</p><p><strong>Conflict resolution and replication</strong>: Replicated database has multiple copies on different nodes, make prevention of lost of updates more complex. Lock based &#x2F; compare-and-set will <strong>NOT</strong> apply as we cannot guarantee that there is a single up-to-date copy of the data, due to asynchrounous write nature. We can allow concurrent writes to create conflicting versions of values (<strong>siblings</strong>) and resolve &amp; merge versions after the fact.<br>Atomic operations works well with replicated context, especially when they are <strong>commutative</strong>. <strong>Last Write Wins</strong> (LWW) can easily lost updates, but it is widely used.</p><hr><h3 id="Write-Skew-and-Phantoms"><a href="#Write-Skew-and-Phantoms" class="headerlink" title="Write Skew and Phantoms"></a>Write Skew and Phantoms</h3><p>Some constraints (e.g. checking the inventory before purchase) can be violated, if it depends on certain database data under snapshot isolation, then write happens. After the writes are finished, constraints are violated. This is called <strong>Write Skew</strong>.</p><p>Write skew can occur if two transactions read <strong>the same</strong> objects, and then update some of those objects (different transactions may update different objects).<br>In the special case where different transactions update the same object, you get a <strong>dirty write</strong> or <strong>lost update</strong> anomaly (depending on the timing).</p><p>Atomic operations (because multiple objects involved) and automatic detection will not help. We need <strong>true serializable</strong> isolation to prevent Write Skew.<br>Otherwise, we can explicitly lock the rows that the transaction dependes on. We can use <code>FOR UPDATE</code> to lock all the read rows.</p><hr><p>The effect, where a write in one transaction changes the result of a search query in another transaction, is called a <strong>phantom</strong><br><strong>Snapshot isolation</strong> avoids phantoms in <strong>read-only</strong> queries, but not in <strong>read-write</strong> transactions.</p><p>Using <code>FOR UPDATE</code> to lock rows will not work, if no row is returned. Then, it cannot resolve conference room booking problem, because we want to create a meeting if there does not lie one.</p><p><strong>Materializing Conflicts</strong>:<br>We can try to introduce a lock object into the database to resolve the issue of no object we can attach locks on. However, this can be hard and error-prone, and ugly to let concurrency control mechanism leak into the application data model.<br>A <strong>serializable</strong> isolation level is much preferable in most cases.</p><hr><h2 id="Serializability"><a href="#Serializability" class="headerlink" title="Serializability"></a>Serializability</h2><p>We have different ways of implementing serializability:</p><h3 id="Actual-Serial-Execution"><a href="#Actual-Serial-Execution" class="headerlink" title="Actual Serial Execution"></a>Actual Serial Execution</h3><p>Remove concurrency entirely, but this is only recognizied as feasible only until recently.<br>This is because: RAM became <strong>cheap</strong> enough to keep entire active dataset in memory; OLTP transactions are usually <strong>short</strong> and only make a small number of read and writes.</p><p><strong>Multi-stage</strong> process (e.g., search and book for flight) is hard to be encapsulated as a transaction. However, humans are very slow to respond, and this will result in huge number of concurrent transactions and mostly <strong>idle</strong>.</p><p>Most OLTP applications <strong>avoid</strong> waiting for a user within a transaction.<br>This means, a transaction is committed within the <strong>same</strong> HTTP request—a transaction does not span <strong>multiple</strong> requests. A new HTTP request starts a new transaction.<br>* Even though the human has been taken out of the critical path, transactions have continued to be executed in an interactive client &#x2F; server style, one statement at a time.<br>In such case, queries, application codes and database run on different machines and resulted in expensive <strong>communication cost</strong>.<br>It is <strong>impossible</strong> to disallow concurrency, because database will spend most of the time waiting the application to issue the next query for the current transaction. </p><p>Systems with <strong>single-threaded serial</strong> transaction processing don’t allow <strong>interactive multi-statement transactions</strong>.<br>Application must submit the <strong>entire</strong> transaction code to the database as a <strong>stored procedure</strong> (must have all needed data by hand in memory, No network &#x2F; disk IO).</p><hr><p>Stored Procedure has kind of bad repuatation because they are implemented with <strong>different langauges</strong> (PL&#x2F;SQL, e.g.), ugly and archaic, lack <strong>ecosystem</strong> of libraris, harder to <strong>debug</strong>, <strong>version control</strong> , <strong>deploy</strong> and <strong>test</strong>, as well as <strong>monitoring</strong>. Badly written stored procedure may impact the performance more. </p><p><strong>Solution</strong>: Use exisiting general-purpose programming langauges.<br>With stored procedures and in-memory data, executing all transactions on a <strong>single thread</strong> becomes feasible</p><p><strong>Partition</strong> may be needed for single thread procedure.<br>However, procedures may need to be performed in <strong>lock-step</strong> across all partitions, if we need to access multiple partitions (to ensure serializability, may impact performance). </p><p>Data with multiple <strong>secondary indexes</strong> may require a lot of <strong>crosspartition coordination</strong>. </p><h3 id="Summary-of-Stored-Procedure"><a href="#Summary-of-Stored-Procedure" class="headerlink" title="Summary of Stored Procedure"></a>Summary of Stored Procedure</h3><ul><li>Transaction must be <strong>small</strong> and <strong>fast</strong></li><li>One slow transaction can stall all transaction processing</li><li>Apply if active dataset can fit in <strong>memory</strong>. It can be slow if we need to access disk.</li><li>Write throughput must be low enough, to be handled on <strong>single CPU</strong> &#x2F; partitioned <strong>without</strong> requiring cross-partition coordination (possible, but highly limited).</li></ul><p><strong>Two-Phase Locking (2PL)</strong>: Several transactions are allowed to concurrently read the same object as long as nobody is <strong>writing</strong> to it. But as soon as anyone wants to write (modify or delete) an object,<br><strong>exclusive access</strong> is required (this lock will block <strong>readers</strong> as well)</p><p>2PL is used for <strong>serializable isolation</strong> level in <strong>MySQL</strong> (<strong>InnoDB</strong>) and <strong>SQL Server</strong>, and the <strong>repeatable read isolation</strong> level in <strong>DB2</strong>. </p><p><strong>Implementation</strong>: <strong>Readers</strong> must first acquire lock in <strong>share mode</strong>; <strong>Writers</strong> must first acquire lock in <strong>exclusive mode</strong>.<br>Transaction must hold the lock until the end of the transaction (commit or abort), and that is the meaning of two-phase.<br><strong>First Phase</strong>: When locks are acquired while the transaction is executing;<br><strong>Second Phase</strong>: Locks are release when transaction is finished.</p><p><strong>Deadlock</strong> may happen, and needs to be detected and resolved (by aborting one transaction). So as <strong>contention</strong>. Thus, <strong>2PL</strong> databases may experience severe performance issue, and we may prefer weaker lock in that case.</p><hr><p><strong>Predicate locks</strong>: in order to  prevent <strong>phantoms</strong>.<br>Does <strong>NOT</strong> belong to a particular object (e.g., one row in a table), it belongs to all objects that match some search condition (e.g. certain inventory &#x2F; conference room availability)</p><p>If transaction A wants to read objects matching some condition, it must acquire a <strong>shared-mode</strong> predicate lock on the conditions of the query.<br>If another transaction B currently has an <strong>exclusive lock</strong> on <strong>any</strong> object matching those conditions, A must wait until B releases its lock before it is allowed to make its query.<br>If transaction A wants to insert, update, or delete any object, it must first check whether either the <strong>old</strong> or the <strong>new</strong> value matches any existing predicate lock. </p><p>The key idea here is that a predicate lock applies even to objects that <strong>do not yet exist</strong> in the database, but which might be added in the future (<strong>phantoms</strong>).<br>If <strong>two-phase locking</strong> includes <strong>predicate locks</strong>, the database prevents all forms of write skew and other race conditions, and so its isolation becomes <strong>serializable</strong>.</p><hr><h3 id="Index-range-locks"><a href="#Index-range-locks" class="headerlink" title="Index-range locks"></a>Index-range locks</h3><p><strong>Index-range locks</strong> &#x2F; <strong>nextkey locking</strong>, which is a simplified approximation of <strong>predicate locking</strong>, might be the reason of poor performance (as it place locks on <strong>more</strong> objects than needed).</p><p>E.g., it is safe to block all rooms, or block one room’s all slots, if you just want to book a meeting in room A between 1-2PM.<br>Index-range locks might be easier to implement, with existing indexes.</p><p>Index-range lock is still a good compromise to implement serializability. </p><hr><h3 id="Serializable-Snapshot-Isolation-SSI"><a href="#Serializable-Snapshot-Isolation-SSI" class="headerlink" title="Serializable Snapshot Isolation (SSI)"></a>Serializable Snapshot Isolation (SSI)</h3><p>Now used in both single-node and distributed databases.</p><p><strong>Two-phase locking</strong> is a so-called <strong>pessimistic</strong> concurrency control mechanism: if things can go wrong (lock is possessed), we should wait till the lock is released, like <strong>mutual exclusion</strong>.</p><p><em>Serial execution is extremely pessimistic, equals to each transaction having an exclusive lock during execution.</em></p><p><strong>serializable snapshot isolation</strong> is an <strong>optimistic</strong> concurrency control technique.<br>We hope everything to turn out alright, just check whether isolation was violated before a transaction commits. If so, we have to retry.</p><p>If there lies <strong>high contention</strong>, optimistic concurrency control will have <strong>poor</strong> performance. However, if we have enough spare capacity and reasonable contention, it can perform better. Contention can be reduced with <strong>commutative atomic</strong> operations.</p><p><strong>SSI</strong> is based on <strong>snapshot isolation</strong>, and adds an algorithm for <strong>detecting serialization conflicts</strong> among writes and dtermines which transaction to abort.</p><hr><p>Under snapshot isolation, the result from original query may not be up-to-date, because data may be modified during transaction execution, and the premise (that this transaction is doable and correct) may no longer be true (means that the transaction needs to be aborted).</p><p>We need to detect reads of a <strong>stale MVCC object version</strong> (uncommitted write occurred before the read), and writes that affect prior reads (the write occurs <strong>after the read</strong>).</p><ol><li>For detecting <strong>stale MVCC object version</strong>: Before commit a transaction, database checks if any previously ignored transaction (changing the same data field) is committed. If so, current transaction is aborted as its premise no longer exists.</li><li>For detecting <strong>writes that affect prior reads</strong>: Can be implemented using a technique similar to <strong>index-range lock</strong>, but do not block other transactions.<br>We can track in index or table level, when a transaction commits, it detects whether other transactions read the affected data (let such transactions know their read is not up-to-date).</li></ol>]]>
    </content>
    <id>https://umiao.github.io/2024/03/29/Designing-Data-Intensive-Applications-Note-15/</id>
    <link href="https://umiao.github.io/2024/03/29/Designing-Data-Intensive-Applications-Note-15/"/>
    <published>2024-03-29T20:52:54.000Z</published>
    <summary>Discussion on transaction related topics.</summary>
    <title>Designing Data-Intensive-Applications-Note-15</title>
    <updated>2026-03-10T01:09:07.002Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Designing Data-Intensive-Applications" scheme="https://umiao.github.io/tags/Designing-Data-Intensive-Applications/"/>
    <content>
      <![CDATA[<p>Discussion on partition related topics.</p><img src="/2024/03/24/Designing-Data-Intensive-Applications-Note-14/cover.png" class="" title="ML_note"><span id="more"></span><p><strong>Partition</strong>: Known as <strong>Shard</strong> in MongoDB, Elasticsearch, and SolrCloud. Known as a <strong>region</strong> in HBase, a <strong>tablet</strong> in Bigtable, a <strong>vnode</strong> in Cassandra and Riak, and a <strong>vBucket</strong> in Couchbase. </p><p>Partition aims to act as independent small, <strong>non-overlapping</strong> databases for better <strong>scalability</strong>.<br>Different partitions can be placed on different nodes for load balancing (on processor) and large dataset distribution. Each node should be able to independently querying their own partition, able to <strong>parallelize</strong> complex queries.</p><p>Old partition databases: <strong>Teradata</strong>, <strong>Tandem NonStop SQL</strong>; Recently: <strong>Hadoop</strong> and <strong>NoSQL</strong> databases.</p><p>Partition is usually combined with replication to have each partition stored on multiple nodes. If leader based replication setting is used, then leader and follower of the same partition should belong to different nodes.</p><hr><ul><li>We expect partition to be spreading data and query <strong>evenly</strong>, so that our throughput and data volume being able to handle can grow linearly as we have more nodes (if we ignore the replication).</li><li><strong>Skewed</strong>: the partitioning is unfair and some partitions have more data. Reduce the effectiveness of partition. A partition with high load is named a <strong>hot spot</strong>.</li><li>We should try to assign records to node randomly, preferrably using <strong>primary key</strong>.</li></ul><p><strong>Partition by Key Range</strong>: </p><ol><li>assign a <strong>continuous</strong> range of keys (from some minimum to some maximum) to each partition (like encyclopedia)</li><li>The range of keys are <strong>NOT</strong> guaranteed to be evenly spaced, need to adapt to the data. Some access pattern may lead to <strong>hot spots</strong>.</li><li>We can keep the keys sorted in each partition to make scan easier and treat the keys as a <strong>concatenated index</strong>.</li></ol><p><strong>Partition by Hash of Key</strong>: </p><ol><li>Due to the risk of <strong>skew</strong> and <strong>hot spots</strong>, we can use hash function to determine partition (assign each partition a range of <strong>hashes</strong>).</li><li>Good hash function can takes skewed data but still makes it <strong>uniformly</strong> distributed. </li><li>Such function not have to be <strong>cryptographically</strong> strong if it is for partition purpose. E.g., <strong>MD5</strong> and <strong>Fowler–Noll–Vo</strong>. </li><li>Some hash function (used for hash tables) are not suitable for partitioning, like <code>Object.hashCode()</code> in <strong>Java</strong> and <code>Object#hash</code> in <strong>Ruby</strong>, where same key may have different hash values across processes.</li><li>The partition boundaries can be evenly spaced, or they can be chosen <strong>pseudorandomly</strong> (<strong>consistent hashing</strong> &#x2F; <strong>hash partitioning</strong>, randomly choose partition boundaries to avoid the need for <strong>central control</strong> &#x2F; <strong>distributed consensus</strong>. That is to say, also map nodes &#x2F; virtual nodes to a ring, then direct traffic to the next node on the ring).</li><li>We can <strong>NOT</strong> do efficient range queries by hashing the keys. No primary key based range queries as queries have to be sent to all partitions.<br>* We can make a tradeoff by using <strong>compound primary key</strong> but just hash the first part to determine the partition, other columns used as <strong>concatenated index</strong>.<br>E.g., if you use compound key of <code>(user_id, updated_timestamp)</code>, you can effectively query posts of one user, in a certain time range. Each user’s data is guranteed to be stored in a single partition.</li></ol><p><strong>Relieving Hot Spots</strong>: </p><ol><li>Exactly the same keys would still be directed into the <strong>same</strong> partition, even with the partition on hash of keys (e.g., visits to certain celebrity).</li><li>This is hard to be compensated by data systems, so should be done on the <strong>application level</strong>. The hotspots can be relieved by appending random numbers to the beginning &#x2F; end of the key. </li><li>This only makes sense when you are adding <strong>salt</strong> for small number of hotspots.</li></ol><hr><p><strong>Partition on Secondary Indexes</strong></p><ol><li><strong>Secondary Indexes</strong> usually cannot uniquely identify a record, more like searching occurences of a particular value. It is more <strong>complex</strong> and does not map neatly into partitions.</li><li><strong>Document-based partitioning</strong> (also named as <strong>Local Index</strong>): Each partition is <strong>sepearate</strong> and owns its own secondary indexes covering the documents in that partition. E.g., you can add a qualified red car to each partition’s seondary index’s list of <code>color:red</code>. During query time, you should send query to all partitions and combine the result back (<strong>scatter &#x2F; gather</strong>, may make the secondary index more expensive and prone to <strong>tail latency amplification</strong>). It is widely used and ideally you can construct your schema to have secondary index queries served from a <strong>single</strong> partition.</li><li><strong>Term-based partitioning</strong>: In this case, we are having <strong>global</strong> secondary indexes which cover data in all partitions. Such index can be stored in partitions in a <strong>non-overlap</strong> manner. E.g., <code>color:red</code> contains <strong>all</strong> indexes of qualified cars across partitions, but only got stored in <strong>single</strong> partition (called <strong>term-partitioned</strong> as the term we are looking for determines the partition of index).</li><li><strong>Term &#x2F; Global secondary index</strong> is more efficient in <strong>read</strong>; <strong>write</strong> is slower and more complex (a write to single document may affect multiple partitions of the index).  Global secondary index also require a <strong>distributed transaction</strong> across all partitions<br>affected by a write, which is not supported in all databases. It is often <strong>asynchronous</strong> and the index may not be updated in real-time.</li></ol><hr><h3 id="Rebalance-Partitions"><a href="#Rebalance-Partitions" class="headerlink" title="Rebalance Partitions"></a>Rebalance Partitions</h3><p><strong>Rebalancing</strong> (move data and requests from one node to another) may be needed due to increase of <strong>query throughput</strong> (to add more CPUs), increase of <strong>dataset size</strong> or <strong>machine failure</strong>.</p><p>After rebalancing, we want:  </p><ol><li>Load (data storage, read and write requests) should be shared <strong>fairly</strong> between the nodes in the cluster.</li><li>While rebalancing is happening, the database should continue accepting reads and writes.</li><li>No more data than necessary should be moved between nodes, to make rebalancing fast and to <strong>minimize</strong> the network and disk I&#x2F;O load.</li></ol><p><strong>Strategies of rebalancing</strong>:</p><ol><li><strong>NOT</strong> recommended: <strong>hash mod N</strong> (N is number of new partitions). This is expensive as it over-moves the data. Instead, better divide hash into <strong>ranges</strong> in partitioning.</li><li><strong>Fixed number of partitons</strong>: creating <strong>more</strong> partitions than number of nodes, assign several partitions to each node (maybe unevenly). Then partitions can be easily moved between nodes for node extension &#x2F; delete purpose. Number of partitions nor assignment of keys will chagne. Also, old partitions can still handle requests when transferring is in progress.</li></ol><hr><h3 id="Dynamic-Partitioning"><a href="#Dynamic-Partitioning" class="headerlink" title="Dynamic Partitioning"></a>Dynamic Partitioning</h3><p>A <strong>fixed number</strong> of partitions with <strong>fixed boundaries</strong> may end up with all data in one partition, if you did not configure the boundaries properly.</p><p><strong>Dynamic Partitioning</strong> (used in <strong>HBase</strong> &#x2F; <strong>RethinkDB</strong>): When a partition <strong>grows</strong> to exceed the configured size (e.g. 10GB), it is split into two partitions approximately half of the original size. If data is delted below same threshold, it can be merged with <strong>adjacent</strong> partition.</p><p>It makes the number of partitions adapats to the total data volume to potentially save the overhead.</p><p>An initial set of partitions can be configured on an empty database (this is called <strong>pre-splitting</strong>) to prevent the number of partitions to grow from 1, but it requires you to know the <strong>key distribution</strong>.</p><hr><h3 id="Partitioning-proportionally-to-nodes"><a href="#Partitioning-proportionally-to-nodes" class="headerlink" title="Partitioning proportionally to nodes"></a>Partitioning proportionally to nodes</h3><p><strong>Dynamic partitioning</strong>: number of partitions is proportional to the size of the dataset<br><strong>Fixed num‐ber of partitioning</strong>: the size of each partition is proportional to the size of the dataset.</p><p><strong>Proportional Partitioning</strong>: Make each node having a fixed number of partitions, and the number of partitions proportional to the number of nodes.</p><p>When a new node joins the cluster, it <strong>randomly</strong> (hash-based partitioning is required) chooses a fixed number of existing partitions to split, and then takes ownership of one <strong>half</strong> of each of those split partitions while leaving the other half of each partition in place. </p><p>The randomization can produce <strong>unfair</strong> splits, but when averaged over a larger number of partitions (in <strong>Cassandra</strong>, 256 partitions per node by default), the new node ends up taking a fair share of the load from the existing nodes. </p><hr><p>* It may be good to loop human in partitioning to reach a <strong>tradeoff</strong> between automatic &#x2F; manual partitoning, e.g., data system suggests partition assignment but requires commit from admin.<br>Fully automated partitionings can be unpredictable and expensive, may cause <strong>overload</strong> of network &#x2F; nodes and <strong>cascading</strong> failure.</p><hr><h3 id="Request-Routing"><a href="#Request-Routing" class="headerlink" title="Request Routing"></a>Request Routing</h3><p><strong>Request Routing</strong> is an instance of <strong>service discovery</strong>, which is about how we route request to certain node &#x2F; port.</p><ol><li>Allow clients to contact <strong>any</strong> node (e.g., via a <strong>round-robin load balancer</strong>). If that node coincidentally owns the partition to which the request applies, it can handle the request directly; otherwise, it forwards the request to the appropriate node, receives the reply, and passes the reply along to the client.</li><li>Send all requests from clients to a <strong>routing tier</strong> first, which determines the node that should handle each request and forwards it accordingly. This routing tier does <strong>NOT</strong> itself handle any requests; it only acts as a <strong>partition-aware load balancer</strong>.</li><li>Require that clients be aware of the partitioning and the assignment of partitions to nodes. In this case, a client can connect <strong>directly</strong> to the appropriate node, without any intermediary.</li></ol><p>Coordination service like <strong>ZooKeeper</strong> can help in this process (as routing tier), but you can also have any node take &amp; forward requests.</p><p>*  <strong>Massively Parallel Processing (MPP)</strong> relational database can be a lot more complex, as query needs to be broken into executive stages and partitions.</p>]]>
    </content>
    <id>https://umiao.github.io/2024/03/24/Designing-Data-Intensive-Applications-Note-14/</id>
    <link href="https://umiao.github.io/2024/03/24/Designing-Data-Intensive-Applications-Note-14/"/>
    <published>2024-03-24T21:21:49.000Z</published>
    <summary>Discussion on partition related topics.</summary>
    <title>Designing Data-Intensive-Applications-Note-14</title>
    <updated>2026-03-10T01:09:07.001Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Designing Data-Intensive-Applications" scheme="https://umiao.github.io/tags/Designing-Data-Intensive-Applications/"/>
    <content>
      <![CDATA[<p>Discussion on leaderless replication.</p><img src="/2024/03/24/Designing-Data-Intensive-Applications-Note-13/cover.png" class="" title="ML_note"><span id="more"></span><p>Some data storage systems take a different approach, abandoning the concept of a leader and allowing <strong>any</strong> replica to directly accept writes from clients.<br>Become fashion after Amazon used it for <strong>Dynamo</strong> system.<br><strong>vi Riak</strong>, <strong>Cassandra</strong>, and <strong>Voldemort</strong> are open source datastores with leaderless replication models inspired by Dynamo, so this kind of database is also known as <strong>Dynamo-style</strong>.</p><p>In some leaderless implementations, the client directly sends its writes to several replicas, while in others, a <strong>coordinator node</strong> does this on behalf of the client.<br>Difference with leader database: coordinator does not enforce <strong>ordering</strong> of writes.</p><p>There is no <strong>failover</strong> in leaderless configuration, we mark write as successful if received enough ack &#x2F; OK response.<br>Where failed node return online, it may have stale data due to lost of writes. Thus, a client can send <strong>multiple</strong> read requests and use latest version.</p><h3 id="Read-repair-and-anti-entropy"><a href="#Read-repair-and-anti-entropy" class="headerlink" title="Read repair and anti-entropy"></a>Read repair and anti-entropy</h3><p>We want to gain eventual consistency, and catch-up mechanism for replicas in leaderless setting.</p><p><strong>Read Repair</strong>: When a client makes a read to serval nodes in parallel, it can write new values back to the stale replica (works well for data being frequently read).</p><p><strong>Anti-entropy process</strong>: have a background process to check difference in data between replicas and copies missing values.<br>This process does <strong>NOT</strong> copy data in particular order and my have big <strong>latency</strong> (this helps recovering the less read values).</p><h3 id="Quorums-for-reading-and-writing"><a href="#Quorums-for-reading-and-writing" class="headerlink" title="Quorums for reading and writing"></a>Quorums for reading and writing</h3><p>We need to determine how many successful read &#x2F; writes are needed to make sure the changes take effect on enough replicas (do not care why some nodes failed).<br>Genearlly, we need changes be confirmed by $w$ nodes out of $n$ replicas, and we read $r$ nodes every time. If $w+r \gt n$, then at least one read value is latest.</p><p>If $w \lt n$, if node is unavailable, we can still process writes.<br>If $r \lt n$, if node is unavailable, we can still process reads.<br>Usually, reads and writes are sent to all $n$ replicas.</p><p>* Note that we can have more than $n$ nodes, but ensure for any given value it is only stored on $n$ nodes. This allows us to partition and store dataset too big to fit in single node.</p><p>Often $r$ and $w$ are chosen to be majority ($\gt \frac{n}{2}$) but it is not necessary, it only matters that the sets of nodes used by the read and write operations overlaps in at least one nodes.</p><p>If <strong>quorum condition</strong> is not satified (use smaller $r$ and $w$), you may read stale values but allows better performance and availability.<br>Even <strong>quorum condition</strong> is satisfied, you may still read stale value if <strong>sloppy quorum</strong> is used ($w$ reads and $r$ writes ended up on different nodes so there is no guarantee for overlap).</p><p>If two writes occur concurrently, we cannot tell which one happened first. We can <strong>merge</strong> the concurrent writes.<br>If a write and read occur concurrently, writes may be reflected on only some of the replicas and stale values may be read.<br>If a write succeeded on some replicas ($\lt w$), it is not rolled back on the replicas where it succeeded. Also, a node carrying a new value may fail and restored with an older value.</p><p>Better <strong>Not</strong> view quorum condition as a strong guarantee for data consistency.</p><p><strong>Staleness</strong> is <strong>hard</strong> to monitor on leaderless setting, as we cannot easily track the replication lag (no fixed order) for metrics analysis.</p><hr><h3 id="Sloppy-Quorums-and-Hinted-Handoff"><a href="#Sloppy-Quorums-and-Hinted-Handoff" class="headerlink" title="Sloppy Quorums and Hinted Handoff"></a>Sloppy Quorums and Hinted Handoff</h3><p>Network problem may prevent clients from connecting with active nodes, so the client can no longer reach a quorum.</p><p><strong>Sloppy quorum</strong>: writes and reads still require $w$ and $r$ successful responses, but thoese may include nodes that are not among the designated n “home” nodes for a value.<br>Default on for <strong>Riak</strong> and default off for <strong>Cassandra</strong> and <strong>Voldermort</strong>. </p><p><strong>Hinted handoff</strong>: Once the network interruption is fixed, writes which are temporarily accepted on behalf of another node got sent to the “home” nodes.<br>Tradeoff is that latest values maybe written to other nodes.<br>Only durability is guaranteed that data is stored somewhere in $w$ nodes, but may not be visible by $r$ writes until <strong>hinted handoff</strong> is completed.</p><hr><h3 id="Concurrent-Writes"><a href="#Concurrent-Writes" class="headerlink" title="Concurrent Writes"></a>Concurrent Writes</h3><p><strong>Dynamo-style</strong> databases allow several clients to concurrently write to <strong>the same</strong> key, which means that conflicts will occur even if strict quorums are used. </p><p>To achieve eventually consistency, we need to detect and resolve the conflicts (e.g., cause by multiple clients’ concurrent writes).</p><p><strong>Last Write Wins (LWW)</strong>: Keep only the most recent value, discard concurrent writes to achieve eventual convergence.</p><p>However, for concurrent writes, the order is undefined. By applying and using the LWW, we achieve the eventual convergence at the cost of <strong>durability</strong>. Non-recurrent write may be possible to be dropped.<br>In some cases like aching, lost writes are maybe acceptable, otherwise <strong>LWW</strong> is not suitable.</p><p>The only safe way of using a database with <strong>LWW</strong> is to ensure that a key is only written <strong>once</strong> and thereafter treated as immutable, thus avoiding any concurrent updates to the same key.<br>For example, a recommended way of using <strong>Cassandra</strong> is to use a<br><strong>UUID</strong> as the key, thus giving each write operation a unique key</p><p> Whether one operation happens <strong>before</strong> another operation is the key to defining what concurrency means.<br> In fact, we can simply say that two operations are concurrent if neither happens before the other (i.e., neither knows about the other, no <strong>causally relationship</strong>) </p><h3 id="Handling-of-Concurrent-Writes"><a href="#Handling-of-Concurrent-Writes" class="headerlink" title="Handling of Concurrent Writes"></a>Handling of Concurrent Writes</h3><ol><li>The server maintains a version number for <strong>every</strong> key, <strong>increments</strong> the version number every time that key is written, and stores the new <strong>version number</strong> along with the <strong>value</strong> written. </li><li>When a client reads a key, the server returns all values that have not been overwritten, as well as the <strong>latest</strong> version number. A client must read a key before writing.</li><li>When a client writes a key, it must include the version number from the prior read, and it must <strong>merge</strong> together all values that it received in the prior read. (The response from a write request can be like a read, returning all current values, which allows us to chain several writes like in the shopping cart example.)</li><li>When the server receives a write with a particular version number, it can <strong>overwrite</strong> all values with that version number or below (since it knows that they have been merged into the new value), but it must keep all values with a higher version number (because those values are <strong>concurrent</strong> with the incoming write).</li><li>When a write includes the version number from a prior read, that tells us which previous state the write is based on. If you make a write without including a version number, it is concurrent with all other writes, so it will not overwrite anything—it will just be returned as one of the values on subsequent reads (as an new independent copy)</li></ol><p>If you want to do a bit more, you can try to merge the above multiple versions into one. However, note that delted items may appear again as the result.<br>System must leave a <strong>marker</strong> with an appropriate version number to indicate that the item has been removed when merging siblings.<br>Such a deletion marker is known as a <strong>tombstone</strong>.</p><hr><h3 id="Version-Vectors"><a href="#Version-Vectors" class="headerlink" title="Version Vectors"></a>Version Vectors</h3><p>When we have multiple replicas accepting writes concurrently, we need to use a version number <strong>per replica</strong> as well as <strong>per key</strong>.</p><ol><li>Each replica <strong>increments</strong> its own version number when processing a write</li><li>Also keeps track of the version numbers it has seen from each of the other replicas. </li><li>This information indicates which values to <strong>overwrite</strong> and which values to keep as <strong>siblings</strong>.</li></ol><p><strong>Version Vector</strong>: The collection of version numbers from all the replicas. Sometimes called a <strong>Vector Clock</strong>.<br>The version vector structure ensures that it is safe to read from one replica and subsequently write back to another replica.<br>Doing so may result in siblings being created, but no data is lost as long as siblings are merged correctly.</p><h3 id="Dotted-Version-Vector"><a href="#Dotted-Version-Vector" class="headerlink" title="Dotted Version Vector"></a>Dotted Version Vector</h3><p>In case of conflict, instead of Last Write Wins, we can store all the versions, their values and the <strong>Causal Hisotry</strong> (e.g., version 3 declares it is <code>from: [v_1, v_2]</code>, means that it overwrites value of version 1 and 2)</p><p>However, the causal history may be too expensive to store.</p><p>Data structure of <strong>Dotted Version Vector</strong> (DVV) looks like this:<br>$((i_1, n), [(i_1, m), (i_2, l), (i_3, k) …])$<br>Here, $i_1, ..i_3$ are IDs of nodes, and the number $n, m, l, k$ are version numbers (incremental) of corresponding node.</p><p>The first part is the version of certain node (called a <strong>dot</strong>), and the second part (list) is the <strong>version vector</strong>, stores the state before the event which added $(i_1, n)$ happens.</p><p>Then we can determine the timing &#x2F; causality. We can use the version vector to determine which event &#x2F; version comes after the other.<br>Otherwise, they are <strong>concurrent</strong>. </p>]]>
    </content>
    <id>https://umiao.github.io/2024/03/24/Designing-Data-Intensive-Applications-Note-13/</id>
    <link href="https://umiao.github.io/2024/03/24/Designing-Data-Intensive-Applications-Note-13/"/>
    <published>2024-03-24T16:31:49.000Z</published>
    <summary>Discussion on leaderless replication.</summary>
    <title>Designing Data-Intensive-Applications-Note-13</title>
    <updated>2026-03-10T01:09:07.001Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Designing Data-Intensive-Applications" scheme="https://umiao.github.io/tags/Designing-Data-Intensive-Applications/"/>
    <content>
      <![CDATA[<p>Discussion on replication lag problems and multi-leader replication.</p><img src="/2024/03/21/Designing-Data-Intensive-Applications-Note-12/cover.png" class="" title="ML_note"><span id="more"></span><h3 id="Problems-with-Replication-Lag"><a href="#Problems-with-Replication-Lag" class="headerlink" title="Problems with Replication Lag"></a>Problems with Replication Lag</h3><ol><li>Only Asynchronus Replication is realistic, otherwise single node &#x2F; network failure will turn down the entire system.</li><li>We may see out-dated data because follower falling behind.</li><li><strong>Eventual Consistency</strong>: If writes stopped and wait for followers, followers can catch up and be consistent eventually. </li><li>In real world, such latency can be seconds or minutes, bringing real problems.</li></ol><h3 id="Reading-Your-Own-Writes"><a href="#Reading-Your-Own-Writes" class="headerlink" title="Reading Your Own Writes"></a>Reading Your Own Writes</h3><p>In certain scenarios, users want to view the changes they submitted <strong>immediately</strong> (post &#x2F; comment). If we just write to leader and read from follower, the recent changes may not be replicated yet, looks as if they are lost.</p><p>Then, we need this <strong>read-after-write</strong> consistency &#x2F; <strong>read-your-writes</strong> consistency to guarantee that user will always see their own updates after reloading the page (<strong>No</strong> promise for other users, their changes may be shown later).</p><p><strong>Potential Implementations</strong>:</p><ol><li>Read from <strong>leader</strong> if it is something user may have modified. For something like user profile (only user themself can edit), we can always read from leader.</li><li>Reading from leader too often can negating the benefit of scaling. Thus we can use some criteria to determine if we read from leader, like: <strong>Track time of last update</strong>, then in the following 1 minute, read from leader; <strong>Monitor replication lag</strong> and do not read from followers left too far behind (&gt; 1 min); Record <strong>Timestamp</strong> of user’s update to ensure the served replica reflects the updates, otherwise wait or switch replica. Timestamp can be <strong>logical</strong> (e.g., log sequence number indicating order of writes) or <strong>system clock</strong> (needs <strong>clock synchronization</strong>).</li><li>If your replica distribute across multiple datacenters, then request must be first routed to the one containing the leader.</li><li><strong>Cross-device</strong> read-after-write consistency may be needed (across desktop and mobile platforms.) This makes recording timestamps and routing to correct data centers more difficult, may need single user’s devices be routed to the same datacenter.</li></ol><h3 id="Monotonic-Reads"><a href="#Monotonic-Reads" class="headerlink" title="Monotonic Reads"></a>Monotonic Reads</h3><p>This consistency prevents user from reading data from <strong>the past</strong>.<br>E.g., a comment was added to a page. During the replication to followers, a user made 2 exactly the same reads. One read returned the comment (from a follower with small lag) and the later one returned <strong>nothing</strong> (due to another follower’s greater lag).</p><p>This can be implemented by always routing to <strong>the same replica</strong> (map by user ID’s hash). Rerouting is needed if the replica failed.</p><h3 id="Consistent-Prefix-Reads"><a href="#Consistent-Prefix-Reads" class="headerlink" title="Consistent Prefix Reads"></a>Consistent Prefix Reads</h3><p>Replication lag anomalies concerns violation of <strong>causality</strong> (e.g., the answer is shown prior to a question being asked). This is a particular problem in <strong>partitioned &#x2F; sharded</strong> database.<br>If different partitions operate independently, then there is no <strong>global ordering of writes</strong> (some part may be new and some may be old).</p><p>This requires <strong>consistent prefix reads</strong>, if a sequence of writes happens in a certain order, then anyone reading those writes will see them appear in <strong>the same order</strong>.</p><p>We want to have writes which are causally related to each other written to <strong>the same partition</strong>.<br>This may be hard to achieve in some applications.</p><h3 id="Summary"><a href="#Summary" class="headerlink" title="Summary"></a>Summary</h3><p>Root cause of replication lag derived issues is, the replication pretends to be synchronous but in fact is <strong>asynchronous</strong>.<br>Such issue can be mitigated by conduct some reads on the <strong>leader</strong>.<br>Many distributed (replicated and partitioned) databases have abandoned the <strong>transactions</strong> as it is expensive in terms of performance and availability.<br><strong>Eventual consistency</strong> may be inevitable in scalable system (may be right but over-simplified)</p><hr><h1 id="Multi-Leader-Replication"><a href="#Multi-Leader-Replication" class="headerlink" title="Multi-Leader Replication"></a>Multi-Leader Replication</h1><p>Having only one leader means we may encounter single point failure on leader, then no writes can be done to the database.</p><p>A natural solution is to allow <strong>multiple</strong> nodes to accept writes.<br>E.g., <strong>Tungsten Replicator</strong> for <strong>MySQL</strong>, <strong>BDR</strong> for <strong>PostgreSQL</strong>, and <strong>GoldenGate</strong> for <strong>Oracle</strong>.</p><p>Replication still happens in the same way: each node that processes<br>a write must forward that data change to all the other nodes.<br>We call this a <strong>multi-leader</strong> configuration (also known as <strong>master–master</strong> or <strong>active&#x2F;active</strong> replication).<br>In this setup, each leader simultaneously acts as a follower to the other leaders.</p><p>If you have a database with replicas in multiple datacenters, then the leader-based setup will require all writes go into the datacenter containing that leader.</p><ul><li>Among data centers, each leader replicates changes to other leaders (in different datacenter). </li><li>Within a datacenter, it is regular leader-follower replication.</li><li>Interdatacenter latency can be <strong>hidden</strong> from user, means better preceived latency.</li><li>Allow each datacenter to operate <strong>independently</strong>.</li><li>Have better tolerance of <strong>network problems</strong> (more common as inter-datacenter link is usually on public internet)</li><li><strong>Challenges</strong>: same data modified in different datacenters must have <strong>write conflicts</strong> resolved. <strong>Autoincrementing keys</strong>, <strong>triggers</strong>, and <strong>integrity constraints</strong> can be problematic. Avoid multi-leader setting if possible.</li></ul><hr><p><strong>Clients with offline operation</strong>: Situation where you need multi-leader replication, as you still want to have certain app like calendar to accept read (see meetings) and write (add meetings) even <strong>without</strong> network connection.<br>For your devices with the calendar app, they need to be synced when next time online. Every device has a local database that acts as a leader (it accepts write requests) and has a multi-leader replication process.<br><strong>CouchDB</strong> is designed for this mode of operation.</p><p><strong>Collaborative editing</strong>: <strong>Etherpad</strong> and <strong>Google Docs</strong> allow multiple people to concurrently edit a text document or spreadsheet.<br>The changes need to be applied to local replica instantly, and applied to other users’ replicas asynchronously.<br><strong>Lock mechanism</strong> is needed to avoid editing conflicts. Some tradeoff is needed to make collabration faster, like avoid locking and use <strong>conflict resolution</strong> instead.</p><h3 id="Conflict-Detection"><a href="#Conflict-Detection" class="headerlink" title="Conflict Detection"></a>Conflict Detection</h3><p>In multi-leader setting, conflicts cannot be avoided by synchronous conflict detection (wait for writes to be replicated before return success) as it negated the idea of multi-leader.</p><p><strong>Conflict Avoidance</strong>: Try to ensure all writes of certain record go through the same leader, making it <strong>single-leader</strong> from user’s view. It is suggested as conflicts handling is more difficult.<br>The avoidance can fail if certain datacenter is failed, or the user moved to another physical location and closer to a different datacenter.</p><p><strong>Converging toward a consistent state</strong>:<br>In single-leader database, the last write determines the value of a field. However, this order is not guaranteed in multi-leader setting, ended up with <strong>inconsistent</strong> result.<br>Data must be eventually the same in all replicas, thus conflicts must be resolved in a <strong>convergent</strong> way.</p><p><strong>Convergent conflict resolution</strong></p><ol><li>Give each write a <strong>unique ID</strong> (a timestamp, a long random number, a UUID, or a hash of the key and value) and pick the write with highest ID as winner and <strong>throw away</strong> other writes.</li><li>If we use timestamp, then it is <strong>Last Write Wins</strong> (LWW), may cause data loss.</li><li>Instead, we can give each replica a ID and keep higher numbered replica always take precedence over writes from lower numbered replica. This will also cause <strong>data loss</strong>.</li><li>Somehow merge the conflict values together (like concatenating)</li><li>Record the conflict in an explicit data structure that preserves all information, and write application code that resolves the conflict at some later time (perhaps by <strong>prompting the user</strong>).</li></ol><hr><p>The conflicts resolving logic can be customized, as the most appropriate way may depend on the application.<br><strong>On read</strong>: If conflict detected in log of replicated changes, then call the conflict handler. This is usually backend faced and <strong>NOT</strong> interative with user.<br><strong>On write</strong>: Store all the conflicting writes and return to application for resolving. (how <strong>CouchDB</strong> works)</p><p>Conflict resolution usually applies at the level of individual row &#x2F; document, not transaction.<br>(we will count multiple writes of single transaction)</p><hr><h2 id="Automatic-Conflict-Resolution"><a href="#Automatic-Conflict-Resolution" class="headerlink" title="Automatic Conflict Resolution"></a>Automatic Conflict Resolution</h2><p>* Conflict resolution rules can quickly become complicated, and custom code can be <strong>error-prone</strong>. (e.g., if Amazon only records items in the cart but not items got deleted, then deleted items would be recovered.)</p><ul><li><strong>Conflict-free replicated datatypes</strong> (CRDTs):<br>a family of data structures for sets, maps, ordered lists, counters, etc. that can be concurrently edited by multiple users, and which automatically resolve conflicts in sensible ways. Some CRDTs have been implemented in Riak 2.0</li><li><strong>Mergeable persistent data structures</strong>: track history explicitly, similarly to the Git version control system, and use a <strong>three-way</strong> merge function (whereas CRDTs use two-way merges).</li><li><strong>Operational transformation</strong>: conflict resolution algorithm behind collaborative editing applications such as <strong>Etherpad</strong> and <strong>Google Docs</strong>. It was designed particularly for concurrent editing of an ordered list of items, such as the list of characters that constitute a text document.</li></ul><p>Note that some conflicts can be <strong>harder</strong> to detect, e.g., make sure each person does not appear in two meeting rooms.</p><hr><h2 id="Multi-Replication-Topologies"><a href="#Multi-Replication-Topologies" class="headerlink" title="Multi-Replication Topologies"></a>Multi-Replication Topologies</h2><p>A problem to consider if you have more then <strong>two</strong> leaders.<br>Some examples: <strong>circular</strong> (n leaders having n-1 directional edges), <strong>star</strong> (each edge leader has directional link with the central &#x2F; root leader) or <strong>all-to-all</strong> (each leader has link to each other, most <strong>general</strong> one).<br><strong>MySQL</strong> by default only supports <strong>circular topology</strong>.</p><p>For star and circular (they may prone to <strong>single point of failure</strong>), a write may need to pass through multiple nodes, so we need each node to place a unique identifer on the replication log to prevent <strong>infinite replication loops</strong>.<br>For all-to-all topologies, some network links may be faster, the change log may not be delivered in order.<br>E.g., dependent update arrives before the insert.<br>Then we need to resolve this problem of <strong>causality</strong>, to have a node process the insert first by having a timestamp.</p><p>It should noted that conflict detection techniques are <strong>poorly</strong> implemented in many multi-leader replication systems.</p>]]>
    </content>
    <id>https://umiao.github.io/2024/03/21/Designing-Data-Intensive-Applications-Note-12/</id>
    <link href="https://umiao.github.io/2024/03/21/Designing-Data-Intensive-Applications-Note-12/"/>
    <published>2024-03-21T23:27:44.000Z</published>
    <summary>Discussion on replication lag problems and multi-leader replication.</summary>
    <title>Designing Data-Intensive-Applications-Note-12</title>
    <updated>2026-03-10T01:09:07.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Designing Data-Intensive-Applications" scheme="https://umiao.github.io/tags/Designing-Data-Intensive-Applications/"/>
    <content>
      <![CDATA[<p>Discussion on implementation and optimization of replication log.</p><img src="/2024/02/26/Designing-Data-Intensive-Applications-Note-11/cover.png" class="" title="ML_note"><span id="more"></span><h3 id="Statement-based-Replication"><a href="#Statement-based-Replication" class="headerlink" title="Statement-based Replication"></a>Statement-based Replication</h3><ul><li>Simplest case, just forward all leader’s write request to its followers (<code>INSERT</code>, <code>UPDATE</code>, <code>DELETE</code>).</li><li>Statement calls a <strong>non-determinsitic</strong> function like <code>RAND()</code> can get a different value.</li><li>For <strong>auto-incrementing</strong> column &#x2F; operators relying on the <strong>existing</strong> data in the database, they have to be executed in exactly <strong>the same order</strong> on each replica. This can be limiting in multiple concurrently executing transactions case.</li><li>Stataments that have side effects (<strong>triggers</strong>, <strong>stored procedures</strong>, <strong>user-defined functions</strong>) may result in different side effect occuring on each replica, unless they are absolutely <strong>determinsitic</strong>.</li></ul><p>These issues can be resolved by replacing non-deterministic function calls with a fixed value. However, there are too many <strong>edge cases</strong> so other replication methods are preferred.</p><p><strong>Statement-based</strong> replication was used in <strong>MySQL</strong> before version 5.1. It is still sometimes used today, as it is quite compact, but by default MySQL now switches to <strong>rowbased replication</strong> if there is any <strong>nondeterminism</strong> in a statement.<br><strong>VoltDB</strong> uses statement-based replication, and makes it safe by requiring transactions to be <strong>deterministic</strong></p><h3 id="Write-ahead-log-WAL-shipping"><a href="#Write-ahead-log-WAL-shipping" class="headerlink" title="Write-ahead log (WAL) shipping"></a>Write-ahead log (WAL) shipping</h3><p>In storage engines, for data on disk, we usually every write is appended to a log:</p><ul><li>In the case of a <strong>log-structured</strong> storage engine,this log is the main place for storage. Log segments are <strong>compacted</strong> and <strong>garbage-collected</strong> in the background.</li><li>In the case of a B-tree, which overwrites individual disk blocks, every modification is first written to a <strong>write-ahead log</strong> so that the index can be restored to a consistent state after a crash.</li><li>In either case, the log is an <strong>append-only</strong> sequence of bytes containing all writes to the database. </li><li>We can use the exact same log to build a <strong>replica</strong> on another node: besides writing the log to disk, the leader also sends it across the network to its followers to build a copy of the exact same data structures as on the leader (used in <strong>PostgreSQL</strong> and <strong>Oracle</strong>).</li><li><strong>Disadvantage</strong>: log describes the data on a very low level, contains details of which bytes were changed in which disk blocks. This makes replication closely <strong>coupled</strong> to the storage engine.<br>If the database changes its storage format from one version to another, it is typically not possible to run different versions of the database software on the leader and the followers.</li><li>If the replication protocol allows the follower to use a newer software version than the leader, you can perform a <strong>zero-downtime</strong> upgrade of the database software by first upgrading the followers and then performing a failover to make one of the upgraded nodes the new leader. If the replication protocol does not allow this version mismatch, as is often the case with <strong>WAL shipping</strong>, such upgrades require <strong>downtime</strong>.</li></ul><h3 id="Logical-row-based-log-replication"><a href="#Logical-row-based-log-replication" class="headerlink" title="Logical (row-based) log replication"></a>Logical (row-based) log replication</h3><p>An alternative is to use <strong>different</strong> log formats for replication and for the storage engine, which allows the replication log to be <strong>decoupled</strong> from the storage engine internals.<br>This kind of replication log is called a <strong>logical log</strong>, to distinguish it from the storage engine’s (<strong>physical</strong>) data representation.</p><ul><li>A logical log for a relational database is usually a <strong>sequence of records</strong> describing writes to database tables at the granularity of a <strong>row</strong>:</li><li>For an <strong>inserted</strong> row, the log contains the new values of all columns.</li><li>For a <strong>deleted</strong> row, the log contains enough information to uniquely identify the row that was deleted. Typically this would be the <em>primary key</em>, but if there is no primary key on the table, the old values of all columns need to be logged.</li><li>For an <strong>updated</strong> row, the log contains enough information to uniquely <strong>identify</strong> the updated row, and the new values of all columns (or at least the new values of all columns that changed).</li></ul><p>A transaction that modifies several rows generates several such log records, followed by a record indicating that the transaction was committed.<br>MySQL’s <strong>binlog</strong> (when configured to use row-based replication) uses this approach.</p><p>Since a logical log is <strong>decoupled</strong> from the storage engine internals, it can more easily be kept <strong>backward compatible</strong>, allowing the leader and the follower to run different versions of the database software, or even different storage engines.</p><p>A logical log format is also easier for external applications to parse. This aspect is useful if you want to send the contents of a database to an external system, such as a <strong>data warehouse</strong> for offline analysis, or for building <strong>custom indexes</strong> and <strong>caches</strong>.<br>This technique is called <strong>change data capture</strong>.</p><h3 id="Trigger-based-replication"><a href="#Trigger-based-replication" class="headerlink" title="Trigger-based replication"></a>Trigger-based replication</h3><p>Above replication approaches are implemented by the database system, without involving any <strong>application code</strong>. </p><ul><li>More flexibility may be needed. </li><li>For example, if you want to only replicate a <strong>subset</strong> of the data, or want to replicate from one kind of database to another, or if you need <strong>conflict</strong> resolution logic, </li><li>You may need to move replication up to the <strong>application layer</strong>.</li></ul><p><strong>Oracle GoldenGate</strong>, can make data changes available to an application by reading the database log. </p><p>An alternative is to use features that are available in many relational databases: <strong>triggers</strong> and <strong>stored procedures</strong>.</p><ol><li>A trigger lets you register custom application code that is automatically executed when a data change (<strong>write transaction</strong>) occurs in a database system. </li><li>Able to log this change into a <strong>separate</strong> table, from which it can be read by an external process. </li><li>That external process can then apply <strong>any</strong> necessary application logic and replicate the data change to another system.</li></ol><p><strong>Databus</strong> for <strong>Oracle</strong> and <strong>Bucardo</strong> for <strong>Postgres</strong> work like this, for example.</p><h4 id="Trigger-based-replication-v-s-built-in-replication"><a href="#Trigger-based-replication-v-s-built-in-replication" class="headerlink" title="Trigger-based replication v.s. built-in replication"></a>Trigger-based replication v.s. built-in replication</h4><ul><li>Greater overheads</li><li>More prone to bugs and limitations</li><li>More flexibility.</li></ul>]]>
    </content>
    <id>https://umiao.github.io/2024/02/26/Designing-Data-Intensive-Applications-Note-11/</id>
    <link href="https://umiao.github.io/2024/02/26/Designing-Data-Intensive-Applications-Note-11/"/>
    <published>2024-02-27T04:01:56.000Z</published>
    <summary>Discussion on implementation and optimization of replication log.</summary>
    <title>Designing Data-Intensive-Applications-Note-11</title>
    <updated>2026-03-10T01:09:07.000Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Designing Data-Intensive-Applications" scheme="https://umiao.github.io/tags/Designing-Data-Intensive-Applications/"/>
    <content>
      <![CDATA[<p>Discussion on distributed system goal and Replication implementations.</p><img src="/2024/02/25/Designing-Data-Intensive-Applications-Note-10/cover.png" class="" title="ML_note"><span id="more"></span><p>Distributed System:</p><ul><li><strong>Motivations</strong>:</li></ul><ol><li><strong>Scalability</strong></li><li><strong>Fault tolerance</strong> &#x2F; <strong>High availability</strong></li><li><strong>Latency</strong>.</li></ol><h3 id="Scale-to-Higher-Load"><a href="#Scale-to-Higher-Load" class="headerlink" title="Scale to Higher Load"></a>Scale to Higher Load</h3><ul><li><strong>Scale up</strong>: under single OS, integrate more CPUs, RAM and disks, under Shared memory architecture. </li><li>However, single machine solution’s cost grows faster than <strong>linearly</strong>, restricted to single <strong>geographic</strong> location, has <strong>limited</strong> fault tolerance and subject to <strong>bottlenecks</strong> (like IO and memory)</li></ul><h4 id="Shared-disk-architecture"><a href="#Shared-disk-architecture" class="headerlink" title="Shared-disk architecture"></a>Shared-disk architecture</h4><ul><li>Serval machines with <strong>independent</strong> CPU and RMA, but stored data on array of <strong>disks</strong> is shared between the machines</li><li>Connected via a fast network (used in some data warehouse). </li><li>However, this method’s scalability is also limited by contention and overhead of <strong>locking</strong>.</li></ul><h4 id="Scale-Out"><a href="#Scale-Out" class="headerlink" title="Scale Out"></a>Scale Out</h4><ul><li>A <strong>Shared-Nothing</strong> Architecture, also named <strong>horizontal scaling</strong> is more popular. </li><li>Each (virtual) machine is called a <strong>node</strong>. </li><li>Each node uses its CPUs, RAM and disks <strong>independently</strong>. </li><li>Coordination between nodes is done at <strong>software level</strong> with conventional network. </li><li>No special hardware is reuiqred, so you can use whatever machines for best <strong>price &#x2F; performance ratio</strong>. </li><li>Data can be potentially distributed across multiple geographic regions to reduce <strong>latency</strong> and <strong>survive</strong> the loss of an entire data center, also increase <strong>throughput</strong>. </li><li>With cloud deployment of virtual machines, small companies can still have a <strong>multi-region</strong> distributed architecture.<br>*Such solution requires developer to be most cautious about the constraints and tradeoffs. It also introduces additional <strong>complexity</strong> for applications and limits the <strong>expressiveness</strong> of data model you are using.</li></ul><hr><h3 id="Replication-v-s-Partitioning"><a href="#Replication-v-s-Partitioning" class="headerlink" title="Replication v.s. Partitioning:"></a>Replication v.s. Partitioning:</h3><ul><li>Common ways of distributing data across multiple nodes are replication and partition.</li></ul><p><strong>Replication</strong>:<br>Keeping a copy of <strong>the same</strong> data on several different nodes, potentially in different locations, provides <strong>redundancy</strong>.</p><p><strong>Partition</strong>:<br>Splitting a big database into smaller <strong>subsets</strong> called partitions.  </p><p>These two ways can go with each other.</p><hr><h3 id="Models-of-Replication"><a href="#Models-of-Replication" class="headerlink" title="Models of Replication:"></a>Models of Replication:</h3><h4 id="Leaders-and-Follower"><a href="#Leaders-and-Follower" class="headerlink" title="Leaders and Follower:"></a>Leaders and Follower:</h4><ul><li>Each node stores a copy of data is called a <strong>replica</strong>. </li><li>We want to ensure that (if our data can fit into arbitrary node, a simplifying assumption) every write ends up on <strong>all</strong> the replicas. </li><li>That is why we need <strong>leader-based</strong> replication (<strong>active-passive</strong> or <strong>master-slave</strong> replication).</li></ul><p><strong>Implementation of Leader-based Replication</strong>:</p><ol><li>One replica is designated the <strong>leader</strong> (<strong>master</strong> &#x2F; <strong>primary</strong>). When clients want to write to the database, they must send their requests to the leader, and leader first write the new data to its <strong>local storage</strong>.</li><li>Other replica known as <strong>followers</strong> (<strong>slaves</strong> &#x2F; <strong>secondaries</strong> &#x2F; <strong>hot standbys</strong>), will receive leader’s change as part of <strong>replication log</strong> &#x2F; <strong>change stream</strong>, updating their local copies by applying all writes in <strong>the same order</strong>.</li><li>When a clinet wants to read from the database, it can query either the leader or any of the followers. However, writes are only accepted on the leader<br>(followers are <strong>read-only</strong> from leader’s view).</li></ol><ul><li><p><strong>Usage as built-in feature of relational DB</strong>: </p></li><li><p>PostgreSQL (since 9.0)</p></li><li><p>MySQL</p></li><li><p>Oracle Data Guard</p></li><li><p>SQL Sever’s AlwaysOn Availability Groups.  </p></li><li><p><strong>Usage as non-relational DB</strong>: </p></li><li><p>MongoDB</p></li><li><p>RethinkDB</p></li><li><p>Espresso.</p></li></ul><p>Also used in distributed message brokers like <strong>Kafka</strong>, <strong>RabbitMQ</strong>’s <strong>highly available queues</strong>.<br>Also used by some network file systems and replicated block devices.</p><h3 id="Synchronous-v-s-Asynchrounous-Replication"><a href="#Synchronous-v-s-Asynchrounous-Replication" class="headerlink" title="Synchronous v.s. Asynchrounous Replication:"></a>Synchronous v.s. Asynchrounous Replication:</h3><p>In relational DB, this is usually <strong>configurable</strong>. However, in other systems, it is usually hard-coded.</p><h4 id="Example-of-Synchronous"><a href="#Example-of-Synchronous" class="headerlink" title="Example of Synchronous:"></a>Example of Synchronous:</h4><ul><li>Leader get the request, send write request to one of the follower, waiting for its “OK” before return to the client. </li><li>The leader will continue to wait for the next follower to finish updates.</li></ul><p><strong>Advantages</strong>: follower is guaranteed to have an <strong>up-to-date</strong> &amp; <strong>consistent</strong> copy, which is usable even leader fails.<br><strong>Disadvantages</strong>: the followers might <strong>fall behind</strong> the leader by a long time due to failure, capacity issue or network issues. All follower writes will be <strong>blocked</strong> in this case.<br>Due to the above reason, usually only 1 follower is <strong>synchronous</strong> and the rest are <strong>asynchronous</strong>.<br>If the synchronous follower becomes unavailable, another asynchronous follower will become synchronous.<br>[This guarantees 2 up-to-date copy on at least 2 nodes]<br>This is sometimes called <strong>semi-synchronous</strong>.</p><p>Usually, leader-based replication is configured to be <strong>completely asynchronous</strong>.<br>If leader fails and not recoverable, any write not yet replicated will lost.<br>[<strong>Not</strong> durable write, even confirmed with client]<br>The good side is that leader can continue processing writes even all followers are falling behind (especially when we have a lot of them).</p><hr><h3 id="Chain-Replication"><a href="#Chain-Replication" class="headerlink" title="*Chain Replication"></a>*Chain Replication</h3><ul><li><strong>Chain Replication</strong> is a variant of <strong>synchronous</strong> replication that has been successfully implemented in a few systems such as <strong>Microsoft Azure Storage</strong> (which avoid loss of data)</li></ul><p><strong>Basic components</strong> of CR are:<br>A sequence (chain) of nodes with 2 special nodes — <strong>HEAD</strong> (receives requests from clients) and <strong>TAIL</strong> (end of the chain, provides the guarantee for consistency). </p><p>Such chain has at least following <strong>properties</strong>:</p><ul><li>Tolerates failure of up to $n-1$ nodes.</li><li>Write performance is around write performance of <strong>P&#x2F;B</strong> (<strong>Primary &#x2F; Backup</strong>) approach.</li><li>Cluster reconfiguration happens much faster, in case HEAD failure happens. </li><li>For other nodes — around the same time as for <strong>P&#x2F;B</strong>.<br>It is very important to note that chain replication requires a strong and reliable <strong>FIFO link</strong> between nodes.</li></ul><h4 id="Implementation"><a href="#Implementation" class="headerlink" title="Implementation:"></a>Implementation:</h4><ul><li>Clients send write requests to the head and read requests to the tail. </li><li>When the head receives a request, it calculates the <strong>delta</strong> of the states, applies the change and <strong>propagates</strong> the delta down the chain (to successor). As soon as tail receives the delta, it sends <strong>ACK</strong> back through each node to the head. </li><li>As you can see, if a read request returns some value X, that means it has been saved on <strong>all</strong> nodes.</li></ul><p>In each node, we will save:<br><strong>Pending(i)</strong> — list of received requests by not yet processed by the tail.<br><strong>Sent(i)</strong> — list of not yet processed by the tail requests, sent to the node’s i successor.<br><strong>History(i, key)</strong> — list of changes for the key. It could be either full history or just last value.</p><p>Note that:<br>$History(j, key) \subseteq History(i, key), \forall j \gt i$ and $Sent(i) \subseteq Pending(i), History(i, key) &#x3D; History(i + 1, key) \cup Sent(i)$</p><h4 id="Coping-with-failures"><a href="#Coping-with-failures" class="headerlink" title="Coping with failures:"></a>Coping with failures:</h4><p>We need a special <strong>master process</strong> will: </p><ul><li><strong>Detect</strong> a failed node; </li><li>Notify <strong>predecessor</strong> and <strong>successor</strong> of the failed node; </li><li>Notify clients if the failed node is either HEAD or TAIL.<br>We also assume that our nodes are <strong>fail-stop</strong>, means server stops working in case of its failure, never send an incorrect response. Failure is always <strong>detectable</strong> by the master process.</li></ul><p><strong>Basc Approach</strong>: </p><ul><li>Only write on HEAD and read from TAIL (the only tail, making it a hotspot which is bad; </li><li>If tail is in another data center, it can slow down the entire chain). </li><li>When we need a new node, insert it after tail will be easiest (just <strong>copy</strong> the tail state and ask the previous tail to continue transfer requests).<br>We can tell that, if head fails, we have $Pending(head) – Sent(head)$ lost; If TAIL fails, we can recover by Node TAIL-1. If other node fails, we can recover based on it predecessor and succesor.</li></ul><hr><h3 id="Chain-Replication-with-Apportioned-Queries-（CRAQ）"><a href="#Chain-Replication-with-Apportioned-Queries-（CRAQ）" class="headerlink" title="Chain Replication with Apportioned Queries （CRAQ）:"></a>Chain Replication with Apportioned Queries （CRAQ）:</h3><p>Read only on TAIL can be a <strong>bottleneck</strong>. We want to improve this by allowing read requests to be processed by <strong>all</strong> nodes <strong>but</strong> the TAIL.<br>To preserve consistency, we maintain a <strong>version vector</strong> for write requests and will do requests to the TAIL to get latest committed version in case of ambiguity.</p><p>For <strong>read</strong> request, HEAD will return the response to clients.<br>For <strong>write</strong> request, each node except TAIL can return the value back to the client.</p><p>Each non-tail node can maintain multiple versions of the same key, and those versions are <strong>monotonically increasing</strong>.<br>Each version can be either <strong>“clean”</strong> or <strong>“dirty”</strong>, in the beginning, all versions are <strong>clean</strong>.</p><ul><li>When a node receives a <strong>write</strong> request, it adds the received version to its local list of versions of that key.</li><li>If the node is the TAIL then it marks the version as <strong>clean</strong>, the version now is committed and the tail sends <strong>ACK</strong> back to the head.</li><li>Otherwise — the node marks the version as <strong>dirty</strong> and passes to the next node (<strong>successor</strong>).</li><li>When a node receives ACK from its successor it marks the version as <strong>clean</strong> and removes <strong>all older versions</strong>.</li></ul><hr><ul><li>When a node receives a <strong>read</strong> request:</li><li>If the latest known to the node version is <strong>clean</strong> — return it.</li><li>Otherwise — ask TAIL to get last committed version of the given key, which it sends back to the client. (Such version will always exist on the tail by design).</li></ul><h4 id="Summary"><a href="#Summary" class="headerlink" title="Summary:"></a>Summary:</h4><p>CRAQ’s performance grows linearly with the amount of nodes with <strong>mostly read</strong> requests;<br>In case of mostly write requests, the performance will be close to the basic approach.<br>It can be deployed in <strong>multiple</strong> data center to be physically closer to client. </p><p>CRAQ provides <strong>strong consistency</strong>.<br>But the TAIL might commit the latest version, before a node sends the response to client.<br>We can use <strong>monotonic read</strong> (subsequent read requests don’t go in the past; that is to say, subsequent read should see all changes happened ahead) on the whole chain.</p><p>Other consistency provided:<br><strong>Eventual consistency</strong>: the node doesn’t request the latest committed version from the TAIL.<br>This will still provide monotonic reads but only on <strong>one</strong> node. (subsequent read requests must hit <strong>the same</strong> node).<br>Besides, this allows CRAQ to tolerate <strong>network partitioning</strong> (into independent subnets)<br><strong>Bounded Eventual Consistency</strong>: Allow to return dirty version only under some conditions, like <strong>not older</strong> than N revisions or T minutes.</p><hr><h3 id="Fast-Array-of-Wimpy-Nodes-FAWN"><a href="#Fast-Array-of-Wimpy-Nodes-FAWN" class="headerlink" title="Fast Array of Wimpy Nodes (FAWN):"></a>Fast Array of Wimpy Nodes (FAWN):</h3><p>Similar to the idea of <strong>Amazon Dynamo</strong> and consistent hash, we map a physical server into serveral virtual nodes, each has its own unique VID, forming a <strong>ring</strong>.<br>We have multiple <strong>ranges</strong> formed by virtual nodes, and each VI takes care of the keys <strong>“behind”</strong> it and within the certain range.</p><p>Then, to improve fault tolerance, data is replicated over $R$ next virtual nodes in the <strong>clockwise</strong> direction (for example, if $R&#x3D;2$ then keys from A1 are replicated to B1 and C1, if the chain only has A1-C1). Thus, we get a <strong>chain replication</strong> (basic approach).<br><strong>Read requests</strong> are routed to the TAIL, i.e. read from A1 will be routed to C1.<br><strong>Write requests</strong> are routed to the head and are propagated to the TAIL.</p><hr><h3 id="Setting-Up-Follower-without-downtime"><a href="#Setting-Up-Follower-without-downtime" class="headerlink" title="Setting Up Follower (without downtime):"></a>Setting Up Follower (without downtime):</h3><ul><li>We may need to set up new followers due to <strong>failed nodes</strong> &#x2F; need to <strong>increase replica</strong>. We want the added followers to be accurate. Since the database is changing all the time, copy data from one node to naother is not sufficient.<br>Use lock to make file consistent will against our goal of <strong>availability</strong>.</li></ul><h4 id="Method"><a href="#Method" class="headerlink" title="Method:"></a>Method:</h4><ol><li>Take a consistent <strong>snapshot</strong> of the leader’s database at some point (ideally <strong>without</strong> a lock in the entire databases).<br>This may be supported by database infra, or third party tool like <strong>innobackupex</strong> for <strong>MySQL</strong>.</li><li>Copy the snapshot to the new follower node.</li><li>New follower connects to the leader and requests all the data changes after the snapshot<br>(snapshot should be associated to an exact position in the leader’s <strong>replication log</strong>)<br>[such position named as <strong>log sequence number</strong> in <strong>PostgreSQL</strong>, <strong>binlog coordinates</strong> in <strong>MySQL</strong>].</li><li>After follower processed the backlog of data changes, it is caught up.</li></ol><h4 id="Node-Outage"><a href="#Node-Outage" class="headerlink" title="Node Outage:"></a>Node Outage:</h4><p>Each node can possibly go done, should be able to reboot without downtime of sysTEM. It can be done by <strong>Catch-up</strong> recovery.<br>Each follower keeps a log of data changes received from the leader, and be able to identify the <strong>last</strong> successful transaction before the fault occurred. The follower can request the data changes after the outage and catch up. </p><hr><h3 id="Leader-Failure-Failover"><a href="#Leader-Failure-Failover" class="headerlink" title="Leader Failure: Failover"></a>Leader Failure: Failover</h3><ul><li>In case of the leader failed, one of the followers needs to be <strong>promoted</strong> to the new leader.</li><li>Clients need to be reconfigured to send writes to the new leader </li><li>Other followers start to consume data changes from the new leader.</li><li>This is called <strong>“failover”</strong>, can be either manually done or automatically done.</li></ul><h3 id="Automatic-Failover-Process"><a href="#Automatic-Failover-Process" class="headerlink" title="Automatic Failover Process"></a>Automatic Failover Process</h3><ol><li><strong>Determining the leader failed</strong>. Failure reason could be various (crashes, power outages, network issues, …) but we can simply use <strong>timeout</strong> to detect. If a node doesn’t respond for some period of time—say, 30 seconds—it is assumed to be <strong>dead</strong>.</li><li><strong>Choosing a new leader</strong>. This could be done through an <strong>election process</strong> (where the leader is chosen by a majority of the remaining replicas), or a new leader could be appointed by a previously elected <strong>controller node</strong>.<br>The best candidate for leadership is usually the replica with the most up-to-date data changes from the old leader (to minimize any data loss).<br>Getting all the nodes to agree on a new leader is a <strong>consensus problem</strong>.</li><li><strong>Reconfiguring the system to use the new leader</strong>. Clients now need to send their write requests to the new leader. If the old leader comes back, it might still believe that it is the leader,<br>not realizing that the other replicas have forced it to step down. The system needs to ensure that the old leader becomes a follower and recognizes the new leader.</li></ol><h4 id="Things-Could-Go-Wrong"><a href="#Things-Could-Go-Wrong" class="headerlink" title="Things Could Go Wrong"></a>Things Could Go Wrong</h4><ul><li>If <strong>asynchronous replication</strong> is used, the new leader may not have received all the writes from the old leader before it failed. </li><li>If the former leader rejoins the cluster after a new leader has been chosen, what should happen to those writes? </li><li>The new leader may have received conflicting writes in the meantime. The most common solution is for the old leader’s unreplicated writes to simply be discarded, which may violate clients’ <strong>durability</strong> expectations.</li><li>* Discarding writes is especially dangerous if other storage systems outside of the database need to be coordinated with the database contents. For example, in one<br>incident at <strong>GitHub</strong>, an out-of-date MySQL follower was promoted to leader. The database used an autoincrementing counter to assign primary keys to new rows, but because the new leader’s counter lagged behind the old leader’s, it <strong>reused</strong> some primary keys that were previously assigned by the old leader. These primary keys were also used in a <strong>Redis</strong> store, so the reuse of primary keys resulted in <strong>inconsistency</strong> between <strong>MySQL</strong> and <strong>Redis</strong>, which caused some private data to be disclosed to the wrong users.</li><li>Two nodes coul both believe that they are the leader. This situation is called <strong>split brain</strong><br>if both leaders accept writes, and there is no process for resolving conflicts, data is likely to be lost or corrupted.<br>As a safety catch, some systems have a mechanism to <strong>shut down</strong> one<br>node if two leaders are detected. However, if this mechanism is not carefully designed, you can end up with <strong>both</strong> nodes being shut down.</li></ul><p>A longer timeout means a longer time to recovery in the case where the leader fails. However, if the timeout is too short, there could be unnecessary failovers. (<strong>load spike</strong>, <strong>network glitch</strong> can trigger failovers, and further impact the performance of system)</p><p>Thus, many teams prefer to perform failovers <strong>manually</strong>.</p>]]>
    </content>
    <id>https://umiao.github.io/2024/02/25/Designing-Data-Intensive-Applications-Note-10/</id>
    <link href="https://umiao.github.io/2024/02/25/Designing-Data-Intensive-Applications-Note-10/"/>
    <published>2024-02-26T04:37:34.000Z</published>
    <summary>Discussion on distributed system goal and Replication implementations.</summary>
    <title>Designing Data-Intensive-Applications-Note-10</title>
    <updated>2026-03-10T01:09:06.999Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Designing Data-Intensive-Applications" scheme="https://umiao.github.io/tags/Designing-Data-Intensive-Applications/"/>
    <content>
      <![CDATA[<p>Discussion on Data Flow and Message Passing.</p><img src="/2024/02/25/Designing-Data-Intensive-Applications-Note-9/cover.png" class="" title="ML_note"><span id="more"></span><h3 id="Data-Flow-in-Databases"><a href="#Data-Flow-in-Databases" class="headerlink" title="Data Flow in Databases:"></a>Data Flow in Databases:</h3><ul><li>Data usually has longer life cycle, comparing with codes. </li><li>You need to be aware that if a new version of application wrote the data, it then got read &amp; rewrite by an old ver application, we may face loss of fields &#x2F; <strong>inconsistency</strong>. </li><li>The solution can be easy (like keeping the unknown fields, even when you want to transform it into an object then convert back), you just need to be aware of this.<br>* For archival storage (like creating database’s <strong>snapshot</strong>), it is better to keep them all stored in <strong>latest</strong> version, rather than following the original mixture of formats.</li></ul><h3 id="Data-Flow-Through-Services-REST-and-RPC"><a href="#Data-Flow-Through-Services-REST-and-RPC" class="headerlink" title="Data Flow Through Services: REST and RPC:"></a>Data Flow Through Services: REST and RPC:</h3><ul><li>When arranging communication, you usually need to assign roles like <strong>clients</strong> (access API though network) and <strong>servers</strong> (expose API over network).</li><li>Clients use <strong>get</strong> to dowload <strong>HTML</strong>, <strong>CSS</strong>, <strong>JavaScript</strong>, images, etc. </li><li>Use <strong>POST</strong> requests to submit data to the server. API consissts of a standardized set of protocols and data formats (<strong>HTTP</strong>, <strong>URLs</strong>, <strong>SSL&#x2F;TLS</strong>, <strong>HTML</strong>, etc.)</li><li>A client-side JavaScript application running inside a web browser can use <strong>XMLHttpRequest</strong> to become an HTTP client (this technique is known as <strong>Ajax</strong>), usually transferring JSON.</li></ul><h3 id="Service-Oriented-Architecture-SOA-x2F-microservices-architecture"><a href="#Service-Oriented-Architecture-SOA-x2F-microservices-architecture" class="headerlink" title="Service Oriented Architecture (SOA) &#x2F; microservices architecture"></a>Service Oriented Architecture (SOA) &#x2F; microservices architecture</h3><ul><li>Decompose large application into smaller services by area of <strong>functionality</strong></li><li>One server itself can be the client of other servers by requesting certain sub-services.  </li><li>This helps application easier to change and maintain by making services <strong>independently</strong> deployable and evolvable. This also means that older version and later version servers and clients must be <strong>compatible</strong> across versions of the service API.</li><li>Service can impose <strong>fine-grained</strong> restrictions on what user can do, but limiting the <strong>input</strong> and <strong>output</strong>.</li></ul><hr><h3 id="Web-Services"><a href="#Web-Services" class="headerlink" title="Web Services:"></a>Web Services:</h3><ul><li>Definition: when HTTP is used as the underlying protocol for talking to the service.<br>The context of “web” can be:</li></ul><ol><li>Personal device usage through public internet.</li><li>Same organization’s services talk to each other within the data center as part of a service-oriented structure (software supporting this kind of use case is named as <strong>middleware</strong>). </li><li>Requests between organizations, usually via internet.</li></ol><h4 id="REST"><a href="#REST" class="headerlink" title="REST"></a>REST</h4><ol><li>Design philosophy that builds upon the principles of <strong>HTTP</strong></li><li>Emphasizes <strong>simple</strong> data formats</li><li>Using <strong>URLs</strong> for identifying resources and using HTTP features for <strong>cache control</strong>, <strong>authentication</strong>, and <strong>content type negotiation</strong>.<br>[More famous than SOAP]</li></ol><p>*API designed according to REST principles is called <strong>RESTful</strong>. It prefers less code generation and automated tooling. </p><p>A definition format such as OpenAPI, a.k.a <strong>Swagger</strong>, can be used to describe RESTful APIs and produce documentation.</p><h3 id="SOAP"><a href="#SOAP" class="headerlink" title="SOAP:"></a>SOAP:</h3><ul><li>XML based protocol for making netwrok API requests. </li><li>Aims to be independent from HTTP and <strong>avoids</strong> using most HTTP features</li><li>Instead with sprawling and complex multitude of related standards (the <strong>web service framework</strong>, known as <strong>WS-*</strong>)</li></ul><p>The XML based languaged SOAP API use is called <strong>Web Services Description Language</strong> (WSDL). It is <strong>NOT</strong> human readable and hard to construct mannually, heavily rely on tool support, code generation and IDEs.<br>Client access a remote service using <strong>local classes</strong> and <strong>method calls</strong> (encoded to XML messages and decode by the framework).<br>[Good for statically typed language]<br>Also, the implementation can be different across vendors.</p><hr><h3 id="Remote-Procedure-Calls"><a href="#Remote-Procedure-Calls" class="headerlink" title="Remote Procedure Calls:"></a>Remote Procedure Calls:</h3><ul><li>The <strong>Remote Procedure Calls</strong> (RPC) model tries to make a request to a remote network service look the same as calling a function or method in your programming language, within the same process<br>(this abstraction is called <strong>location transparency</strong>).<h4 id="Problems"><a href="#Problems" class="headerlink" title="Problems"></a>Problems</h4></li><li>Local func call is <strong>predictable</strong> and either succeeds or fails depend on the paran; </li><li>A network request is <strong>unpredictable</strong> due to network problem (for both request and response). </li><li>Network can have completely no result due to a <strong>time-out</strong> (you have no clue what happened). </li><li>If you retry, then the requests may be executed for multiple times, just the response got lost. </li><li>This will interfere with the behavior, unless you build mechanism for deduplication (<strong>idempotence</strong>).</li><li>The time for func call is also <strong>unstable</strong>.</li><li>Larger objects are hard to be encoded and send via network.<br>We may also encounter type issues when the client and server are not implemented using the same language. Like Java has problem dealing number $&gt; 2^{53}$.</li></ul><p><strong>Enterprise JavaBeans</strong> (EJB) and Java’s <strong>Remote Method Invocation</strong> (RMI) are limited to <strong>Java</strong>.<br>The <strong>Distributed Component Object Model</strong> (DCOM) is limited to <strong>Microsoft</strong> platforms.<br>The <strong>Common Object Request Broker Architecture</strong> (CORBA) is excessively complex, and does not provide backward or forward compatibility</p><p>Due to the shorcomings mentioned above, RPC is mainly used in request between services owned by the same organizaiton, typically in the same <strong>data center</strong>.</p><ul><li><strong>Rest</strong>: will not try to hide the fact this is a network protocol, but you can still build RPC libraries on top of that.</li></ul><p><strong>RESTful</strong> APIs are：</p><ol><li>good for experimentation and debugging (easily make request using browser &#x2F; tool curl)</li><li>Supported by <strong>all</strong> mainstream programming languages and platforms</li><li>Has a vast <strong>ecosystem</strong> of tools (servers, caches, load balancers, proxies, firewalls, monitoring, debugging tools, testing tools, etc.).</li></ol><h4 id="Evolvability"><a href="#Evolvability" class="headerlink" title="Evolvability:"></a>Evolvability:</h4><ul><li>For RPC, we can do the simplifying assumption that <strong>all</strong> servers will be first updated, then come the clients.<br>Then, we only have <strong>backward compatibility</strong> on <strong>requests</strong>, and <strong>forward compatibility</strong> on <strong>responses</strong>.<br>If RPC is used for service providing, upgrading can be <strong>hard</strong> on clients.<br>Then compatibility may need to be maintained to long time, maybe forever. </li><li>Service providers often end up maintaining <strong>multiple versions</strong> of the service API side by side.<br>This can be done via specifying the version in <strong>HTTP header</strong> &#x2F; <strong>URL</strong> &#x2F; or use <strong>API Keys</strong>.</li></ul><p>* New generation of RPC frameworks is more explicit about the fact that remote request is different from a local function call. E.g., use <strong>futures</strong> (<strong>promises</strong>) to encapsulate <strong>async</strong> actions that may fail.<br>**Some frameworks also provide <strong>service discovery</strong>, allowing a client to find out at which IP address and port number it can find a particular service.</p><hr><h3 id="Asynchronous-Message-passing-Systems"><a href="#Asynchronous-Message-passing-Systems" class="headerlink" title="Asynchronous Message-passing Systems:"></a>Asynchronous Message-passing Systems:</h3><ul><li>It is between <strong>RPC</strong> and <strong>databases</strong>.<br>A request (message) is delivered to another process with low latency, but got converyed by an intermediate called <strong>message broker</strong> (<strong>message queue</strong> &#x2F; <strong>message-oriented middleware</strong>). </li><li>Usually no data model is enforced, the message is just a <strong>sequence of bytes</strong> with some metadata. </li><li>For consumer, if it wants to republish the message, it should keep all the <strong>unknown fields</strong>.</li><li>Difference with RPC is that the message-passing is usually <strong>one-way</strong>.<br>Sender does not expect replies, just send and move forward.</li><li><strong>Open source implementations</strong>: <strong>RabbitMQ</strong>, <strong>ActiveMQ</strong>, <strong>HornetQ</strong>, <strong>NATS</strong>, <strong>Apache KAFKA</strong></li></ul><h4 id="Advantages"><a href="#Advantages" class="headerlink" title="Advantages:"></a>Advantages:</h4><ul><li>Act as a <strong>buffer</strong> to improve system reliability; </li><li>Redeliver messages to a <strong>crashed</strong> process, avoid it from being lost; </li><li>Avoids the sender needing to know the <strong>IP address</strong> and <strong>port number</strong> of the recipent (useful in a cloud deployment where virtual<br>machines often come and go); </li><li>One message to be send to <strong>serveral</strong> recipents; </li><li>Logically <strong>decoupes</strong> the sender from the recipent (sender does not care who is the message consumer).</li></ul><hr><h3 id="Distributed-actor-frameworks"><a href="#Distributed-actor-frameworks" class="headerlink" title="Distributed actor frameworks:"></a>Distributed actor frameworks:</h3><ul><li>The actor model is a programming model for <strong>concurrency</strong> in a single process. </li><li>Rather than dealing directly with threads (and the associated problems of <strong>race conditions</strong>, <strong>locking</strong>, and <strong>deadlock</strong>), logic is encapsulated in <strong>actors</strong>. </li><li>Each can represent one <strong>client</strong> &#x2F; <strong>entity</strong>, may have some not shared local states.<br>Process one message at a time, can be scheduled <strong>independently</strong> by framework. </li><li>Communicating with each other by sending – receiving <strong>asynchronous</strong> messages. Delivery is not guranteed.</li></ul><p>In distributed actor framework, this programming model is used to scale an application across multiple <strong>nodes</strong>. Same message-passing mechanism is used regardless of sender &amp; recipents in the same node or not.<br>If not, message will be encoded into byte sequence, send over network and decode on the other side.</p><p>It essentially <strong>integrate</strong> message broker and the actor programming model into single framework.<br>But still need to take care of <strong>forward</strong> &amp; <strong>backward</strong> compatibility.</p><ul><li><strong>Location Transparency</strong>: ability to access objects without the knowledge of their location. In actor model, location transparency works better than model in RPC.<br>The message is already assumed to be possibly <strong>lost</strong>, and we have less of a fundamental mismatch between local and remote communication.</li></ul><h4 id="Examples"><a href="#Examples" class="headerlink" title="Examples:"></a>Examples:</h4><ol><li><strong>Akka</strong> uses Java’s built-in serialization by default, which does not provide forward<br>or backward compatibility. However, you can replace it with something like <strong>Protocol Buffers</strong>, and thus gain the ability to do rolling upgrades.</li><li><strong>Orleans</strong> by default uses a custom data encoding format that does not support <strong>rolling upgrade</strong> deployments;<br>To deploy a new version of your application, you need to set up a new cluster, move traffic from the old cluster to the new one, and shut down the old one.<br>Like with Akka, custom <strong>serialization</strong> plug-ins can be used.</li><li>In <strong>Erlang</strong> OTP it is surprisingly hard to make changes to record schemas (despite the system having many features designed for high availability);<br>Rolling upgrades are possible but need to be planned carefully.<br>An experimental new maps datatype (a JSON-like structure, introduced in Erlang R17 in 2014) may make this easier in the future.</li></ol>]]>
    </content>
    <id>https://umiao.github.io/2024/02/25/Designing-Data-Intensive-Applications-Note-9/</id>
    <link href="https://umiao.github.io/2024/02/25/Designing-Data-Intensive-Applications-Note-9/"/>
    <published>2024-02-25T20:49:40.000Z</published>
    <summary>Discussion on Data Flow and Message Passing.</summary>
    <title>Designing Data-Intensive-Applications-Note-9</title>
    <updated>2026-03-10T01:09:07.008Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Designing Data-Intensive-Applications" scheme="https://umiao.github.io/tags/Designing-Data-Intensive-Applications/"/>
    <content>
      <![CDATA[<p>Discussion on Encoding and Schema.</p><img src="/2024/02/25/Designing-Data-Intensive-Applications-Note-8/cover.png" class="" title="ML_note"><span id="more"></span><h3 id="Encoding-and-Evaluation"><a href="#Encoding-and-Evaluation" class="headerlink" title="Encoding and Evaluation:"></a>Encoding and Evaluation:</h3><ul><li>System require <strong>evolvability</strong>, e.g., capture new field or record types.</li><li>In relational database, we usually assume that all data in database follow only <strong>one</strong> valid pattern. However, in <strong>schema-on-read</strong> &#x2F; <strong>schemaless</strong> databases, it is possible that you can read a mixture of old &#x2F; newer data formats written at different times.</li><li>In larger application, you may need <strong>rolling upgrade</strong> &#x2F; <strong>staged rollout</strong>. It deploys the new version into a few nodes at a time, checking whether the new version is running smoothly, gradually working your way through all nodes. This can enable update without <strong>service downtime</strong>, encourage more frequent releases.</li><li>User may <strong>not</strong> update client-side applications in time.<br>Thus we need： <strong>backward compatibility</strong> (newer code can read data that was written by older code) and <strong>forward compatibility</strong> (old code can read data written by new code).<br>The prior is easier but the latter is harder and needs old ver of code to <strong>ignore</strong> additions made by the new codes.</li></ul><p>Programs usually work with data in at least two ways: </p><ul><li><strong>In-memory</strong> (<strong>objects</strong>, <strong>structs</strong>, <strong>lists</strong>, <strong>arrays</strong>, <strong>hash tables</strong>, <strong>trees</strong>, can be effciently accessed and manipulated by CPU via pointers)</li><li><strong>Over network</strong> (<strong>byte sequence</strong> like JSON). By converting between them, we are doing encoding (<strong>serialization</strong>, <strong>marshalling</strong>) and decoding (<strong>deserialization</strong>, <strong>unmarshalling</strong>).</li><li><strong>Examples</strong>: For encoding in-memory objects, <strong>Java</strong>: <code>java.io.Serializable</code> (bad performance, bloated encoding); <strong>Ruby</strong>: <code>Marshal</code>; <strong>Python</strong>: <code>pickle</code>.           <h4 id="Limitations"><a href="#Limitations" class="headerlink" title="Limitations"></a>Limitations</h4></li><li>Reading is restricted to <strong>the same</strong> language</li><li>Hard to switch programming language in the <strong>receive end</strong></li><li>Prevents <strong>integration</strong> of system. </li><li>Decoding process needs to be able to instantiate <strong>arbitrary</strong> classes, usually a source of security problems.  </li><li>Thus, use the language built in encoding is a bad idea.</li></ul><hr><h3 id="JSON-XML-and-binary-variants"><a href="#JSON-XML-and-binary-variants" class="headerlink" title="JSON, XML and binary variants:"></a>JSON, XML and binary variants:</h3><p>JSON and XML are well-known standardized encodings. However, XML is often criticized as too <strong>verbose</strong> and complicated, JSON is benefited from its <strong>built-in support</strong> in browsers and <strong>simplicity</strong>. </p><ul><li>In <strong>XML</strong> &#x2F; <strong>CSV</strong>, you cannot differ number from strings; </li><li>In <strong>JSON</strong>, you can not differ int from float and no precision specified (for number larger than $2^{53}$, it may be an issue).</li><li>Also, they do not support <strong>binary strings</strong>. You can use <strong>base64</strong> to get around but the data size increased by 33%.</li></ul><p>JSON is still less effective than binary encoding. <strong>MessagePack</strong>, is a binary encoding for JSON which sacrifice human readability to make the encoding result size smaller (for 18.5% less size in the given example, as a tradeoff).<br>Meanwhile, csv does not have schema at all, and you may need to deal with complex <strong>escaping</strong> rules.</p><hr><h3 id="Thrift-and-Protocal-Buffers"><a href="#Thrift-and-Protocal-Buffers" class="headerlink" title="Thrift and Protocal Buffers:"></a>Thrift and Protocal Buffers:</h3><ul><li><strong>Apache Thrift</strong> and <strong>Protocol Buffers</strong> (<strong>protobuf</strong>) are binary encoding libraries that are based on the same principle, and a schema is required to define the type, field name and whether is optional.</li></ul><img src="/2024/02/25/Designing-Data-Intensive-Applications-Note-8/1.png" class="" title="ML_note"><ul><li><p>Thrift has 2 binary encoding formats: <strong>BinaryProtocol</strong> and <strong>CompactProtocol</strong>.</p><img src="/2024/02/25/Designing-Data-Intensive-Applications-Note-8/2.png" class="" title="ML_note"><img src="/2024/02/25/Designing-Data-Intensive-Applications-Note-8/3.png" class="" title="ML_note"></li><li><p>Note that if a field is specified as required, this is only checked in <strong>run-time</strong>.</p></li><li><p><strong>Variable-length integers</strong>: rather than using a full eight bytes to store integer, we can instead encode in 2 bytes (units) and use the <strong>top bit</strong> of each byte to indicate if there are more bytes to come.<br>Thrift has a dedicated <strong>list</strong> data type.</p></li></ul><hr><h3 id="Protocol-Buffer"><a href="#Protocol-Buffer" class="headerlink" title="Protocol Buffer:"></a>Protocol Buffer:</h3><ul><li>Is very similar to the Thrift CompactProtocol. </li><li>However, it does not have a list &#x2F; array type, just a <strong>repeated</strong> marker for fields.</li></ul><p><strong>Nice effect</strong>: </p><ul><li>Can change option into repeated; </li><li>New code reading old data sees a list with zero or one elements (depending on whether the field was <strong>present</strong>)</li><li>Old code reading new data sees only the <strong>last</strong> element of the list.</li></ul><p><strong>Compatibility</strong>: </p><ul><li>You can add new fields to the structure, however, just cannot specify it as a <strong>required</strong> field or the runtime check will fail. </li><li>Old codes can simply <strong>ignore</strong> unrecognized fields and new codes can adapt old schema.   </li><li>Thus, added fields should be <strong>optional</strong> with <strong>default values</strong>.</li><li>In terms of delete, you can only delete <strong>optional</strong> fields and can <strong>never</strong> use the same tag number again.</li><li>Changing data type is possible, but may come with risk of losing precision &#x2F; get <strong>truncated</strong>.</li></ul><hr><h3 id="Avro"><a href="#Avro" class="headerlink" title="Avro:"></a>Avro:</h3><ul><li>Based on <strong>Protocol Buffer</strong> and <strong>thrift</strong>.</li><li>2 schema languages: (<strong>Avro IDL</strong>) intended for human editing, the other based on JSON and easier to be read by machine.</li></ul><img src="/2024/02/25/Designing-Data-Intensive-Applications-Note-8/4.png" class="" title="ML_note"><ul><li>No tag number &#x2F; field to identify type. </li><li>Integers based on <strong>variable length encoding</strong>. This means that the binary data can only be decoded correctly if the code reading the data is using exactly the same schema as the code write the data. </li><li>Any mismatch in the schema between the reader and the writer would result in incorrectly decoded data.</li></ul><p><strong>Writer’s schema</strong>: the version of schema an application knows about when it writes (encodes) the data<br><strong>Reader’s schema</strong>: when read &#x2F; decode, the schema it expect to see.   It can cause problem when there is <strong>mismatch</strong> between reader and writer schema.</p><p><strong>Schema evolution</strong>: </p><ul><li><p><strong>Forward compatibility</strong>: have a new version of schema as writer; </p></li><li><p><strong>Backward compatibility</strong>: have a old version of schema as reader.<br>You can only add or delete a field that has a default value.<br>*In avro, if you want to allow a field to be null, you have to use a <strong>union type</strong> like: {null, long, string} </p></li><li><p><strong>Enforcing the writer schema</strong>: the schema can be reused (like declaring in the start of file), e.g., usage in hadoop, storing files with millions of records.<br>You can specify a file format (<strong>object container</strong> files) to do this.<br>We may have multiple authors across different times, so specify the <strong>version</strong> at the beginning of each encoded record and use author’s schema.</p></li><li><p>For network connection case, processes can negotiate the schema version for the <strong>lifetime of the connection</strong>.</p></li><li><p>Version number can be an <strong>incrementing integer</strong> or a <strong>hash</strong> of the schema.</p></li></ul><p>This is in fact, a pattern of <strong>dynamically</strong> generated schema.<br>It can be flexibly configured during every run.<br>Label &#x2F; type can be changed though <strong>name</strong> stay unchanged as the identifier.<br>For statically typed languages like <strong>Java</strong>, <strong>C++</strong> or <strong>C#</strong>, <strong>Thrift</strong> and <strong>Protocol Buffers</strong> are more friendly, allowing <strong>type checking</strong> and <strong>auto-completion</strong>.<br>For dynamically typed programming languages like <strong>Javascript</strong>, <strong>Ruby</strong> or <strong>Python</strong>, there is not much point in generating code, and can become an obstacle to getting data.</p><p>*Avro can support both.</p>]]>
    </content>
    <id>https://umiao.github.io/2024/02/25/Designing-Data-Intensive-Applications-Note-8/</id>
    <link href="https://umiao.github.io/2024/02/25/Designing-Data-Intensive-Applications-Note-8/"/>
    <published>2024-02-25T18:58:40.000Z</published>
    <summary>Discussion on Encoding and Schema.</summary>
    <title>Designing Data-Intensive-Applications-Note-8</title>
    <updated>2026-03-10T01:09:07.008Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Designing Data-Intensive-Applications" scheme="https://umiao.github.io/tags/Designing-Data-Intensive-Applications/"/>
    <content>
      <![CDATA[<p>Discussion on techniques related to data warehouse &#x2F; OLAP.</p><img src="/2024/02/24/Designing-Data-Intensive-Applications-Note-7/cover.png" class="" title="ML_note"><span id="more"></span><h3 id="Transaction"><a href="#Transaction" class="headerlink" title="Transaction:"></a>Transaction:</h3><ul><li>A bunch of operations, not necessarily having <strong>ACID</strong> (<strong>atomicity</strong>, <strong>consistency</strong>, <strong>isolation</strong>, and <strong>durability</strong>).<br>In fact, it just allows low-latency read &#x2F; write comparing with batch processing jobs (this is more widely used by <strong>analytic</strong> purpose, rather than serving customers).<h4 id="Online-Transaction-Processing-Systems-OLTP-System"><a href="#Online-Transaction-Processing-Systems-OLTP-System" class="headerlink" title="Online Transaction Processing Systems (OLTP System)"></a>Online Transaction Processing Systems (OLTP System)</h4></li><li>Small number of records read per query</li><li>Fetch by key</li><li>Random access and low-latency write based on user input</li><li>Serve end-users via web application</li><li>Based on latest state of data, usually have dataset from GB-TB</li></ul><h4 id="Online-Transaction-Analytic-Systems-OLAP-System"><a href="#Online-Transaction-Analytic-Systems-OLAP-System" class="headerlink" title="Online Transaction Analytic Systems (OLAP System)"></a>Online Transaction Analytic Systems (OLAP System)</h4><ul><li>Aggregate read on records</li><li>Bulk or event stream write</li><li>Used by internal analyst for decision support</li><li>Data based on history of events; dataset from PB – TB as it is history data</li></ul><hr><h3 id="Data-Warehouse"><a href="#Data-Warehouse" class="headerlink" title="Data Warehouse"></a>Data Warehouse</h3><ul><li>Company has the trend to stop analyzing on <strong>OLTP</strong> database, instead on separate database (running analytic queries on <strong>OLTP</strong> can be <strong>expensive</strong>, and harm the <strong>performance</strong>, as they should be low-latency and has high-availability).</li><li>Thus we expect data warehouse to be separate from OLTP, read-only, get stream of updates and transform them into an anlysis-friendly schema, clean and load to data warehouse. This is called <strong>Extract-Transform–Load</strong> (ETL).<br>The information source can be from multiple databases.<br>* Indexing optimization discussed above may work well for OLTP, but <strong>NOT</strong> for analytic queries.</li><li>Data warehouses are common to be <strong>relational</strong>, as SQL is generally a good fit for analytic queries.</li></ul><p>There are many graphical data analysis tools that generate SQL queries, visualize the results and explore data like:</p><ul><li><strong>drill-down</strong> (navigate from high-level summary to more detailed level, break down aggregated data into more granular levels) </li><li><strong>slicing</strong> (select a subset of data based on specific criteria or dimensions)  </li><li><strong>dicing</strong> (like slicing, bat invloves selecting and analyzing data based on multiple criteria &#x2F; dim).</li></ul><hr><h3 id="Stars-and-Snowflakes-schema-for-analytics"><a href="#Stars-and-Snowflakes-schema-for-analytics" class="headerlink" title="Stars and Snowflakes: schema for analytics:"></a>Stars and Snowflakes: schema for analytics:</h3><ul><li>Raw data tables are linked to <strong>Fact Table</strong>. Certain row in that table indicates certain events, e.g., when and where an item is sold at which price. Some columns in that table are attributes, and some are foreign key references (called <strong>dimension table</strong>, indicating event’s who, what, where, when, how, and why).</li><li><strong>Star Schema</strong> (dimensional modeling): the fact table is the center and connection to other tables are like the rays of the star.<br>[<strong>Easier</strong> to use]</li><li><strong>Snowflake Schema</strong>: dimensions are further broken into <strong>sub-dimensions</strong>. In some table like product table, each row can reference foreign keys to represent brand &amp; types, rather than stroing them as string.<br>[More <strong>normalized</strong>]</li></ul><hr><h3 id="Column-oriented-storage"><a href="#Column-oriented-storage" class="headerlink" title="Column oriented storage:"></a>Column oriented storage:</h3><ul><li>Fact tables can be very <strong>large</strong> (trillions of rows) but dimension a tables are usually much smaller (millions of rows).<br>Fact table can have a lot of columns, but we usually only access <strong>a few</strong> of them at a time.<br>Based on the above facts, unlike most OLTP databases (including document databases) whose stroage is laid out on <strong>row-oriented</strong> fashion, we can instead all values from each column together.    </li><li>This helps reducing the overhead by <strong>NOT</strong> loading the not requested columns.<br>Note that the column-oriented storage layout relies on each column file containing the rows in <strong>the same order</strong>.</li><li><strong>Parquest</strong> is a <strong>columnar storage</strong> format that applies to non-relational data as well, supporting a document data model.</li><li><strong>Column compression</strong>: The value sequences in many columns are quite <strong>repeatitive</strong>, indicating that compression can be used. An effective technique is <strong>bitmap encoding</strong>. Follows a graph about how bit map works:</li></ul><p>In the following example, we have 18 rows and 6 possible column values, so we can use 1 bit to store the <strong>occurrence</strong> of a certain value. It is possible to be further reduced by <strong>run-length encoding</strong>.   It is especially efficent when we want to do <strong>boolean computation</strong> on a few values.</p><img src="/2024/02/24/Designing-Data-Intensive-Applications-Note-7/1.png" class="" title="ML_note"><ul><li><p><strong>Sort Order</strong>: In column storage, the order may not matter, but we can force certain (meaningful) column to be ordered and use it as <strong>indexing mechanism</strong>.<br>This can also help <strong>compressing</strong> the column, as we can use <strong>run_length</strong> encoding for sorted column.<br>It needs to be noted that such compression has the best performance on the <strong>first</strong> sorted key, e.g., secondary key will be group by the first key so we will not have so many duplicate values.</p></li><li><p>It may be a good idea to store multiple copies of data, sorted in <strong>different</strong> way. In that way we are adding <strong>redundancy</strong> and being able to further <strong>optimize</strong> queries.  </p></li><li><p>The idea is like secondary indexing, but the different is that index is usually stored in <strong>one place</strong>, and only pointer &#x2F; offset is stored, but in this way, the <strong>values</strong> got duplicated as well.</p></li></ul><p>[Note that such optimization makes <strong>write</strong> harder, compression is even impossible for <strong>in-place</strong> methods like B-Tree]<br>This can be optimized by <strong>LSM tree</strong>, which accumulate enough writes (first add to memory and added to sort structure), then to be updated to disk in <strong>batch</strong>.</p><hr><h3 id="Memory-bandwidth-and-vectorized-processing"><a href="#Memory-bandwidth-and-vectorized-processing" class="headerlink" title="Memory bandwidth and vectorized processing"></a>Memory bandwidth and vectorized processing</h3><p>Potential concerns include: </p><ol><li>Memory bandwidth</li><li>The bandwidth from main memory into the CPU cache</li><li><strong>Branch mispredictions</strong> and <strong>bubbles</strong> in the CPU instruction processing pipeline</li><li>Make use of <strong>single-instruction-multi-data</strong> (SIMD) instructions in modern CPUs.</li></ol><ul><li>Column-oriented storage layouts are good for making use of <strong>CPU</strong> cycles, while reducing the volume of data needed to be loaded from <strong>disk</strong>. </li><li>For example, the query engine can take a <strong>chunk</strong> of compressed column data that fits<br>comfortably in the CPU’s <strong>L1 cache</strong> and iterate through it in a <strong>tight loop</strong> (that is, with<br>no function calls). </li><li>A CPU can execute such a loop much faster than code that<br>requires a lot of function calls and conditions for each record that is processed. </li><li>Column compression allows more rows from a column to fit in the same amount of L1 cache.<br>Operators, such as the bitwise <strong>AND</strong> and <strong>OR</strong> described previously, can be<br>designed to operate on such chunks of compressed column data directly.<br>This technique is known as <strong>vectorized processing</strong></li></ul><hr><h3 id="Materialized-aggregate"><a href="#Materialized-aggregate" class="headerlink" title="Materialized aggregate:"></a>Materialized aggregate:</h3><ul><li>Rather than handling raw data every time, we can use <strong>materialized view</strong> to cache some counts &#x2F; sums which are frequently used.</li><li>Difference with <strong>standard (virtual) view</strong> in relational database: materialized view are values of query result written to <strong>disk</strong>, while virtual view is just some stored <strong>queries</strong>.</li><li>When underlying data changes, materialized view needs to be updated as it is a <strong>denormalized copy</strong> of the data.<br>This comes with higher <strong>write cost</strong>, making it not often used in OLTP databases.<br>They are more valuable in <strong>heavy-read</strong> data warehouses.</li><li><strong>Data Cube &#x2F; OLAP Cube</strong>: a grid of aggregates grouped by different dimensions.</li></ul><img src="/2024/02/24/Designing-Data-Intensive-Applications-Note-7/2.png" class="" title="ML_note">]]>
    </content>
    <id>https://umiao.github.io/2024/02/24/Designing-Data-Intensive-Applications-Note-7/</id>
    <link href="https://umiao.github.io/2024/02/24/Designing-Data-Intensive-Applications-Note-7/"/>
    <published>2024-02-24T18:07:04.000Z</published>
    <summary>Discussion on techniques related to data warehouse / OLAP.</summary>
    <title>Designing Data-Intensive-Applications-Note-7</title>
    <updated>2026-03-10T01:09:07.008Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Designing Data-Intensive-Applications" scheme="https://umiao.github.io/tags/Designing-Data-Intensive-Applications/"/>
    <content>
      <![CDATA[<p>Discussion on Indexes and Multi-Column Index solution (R-Tree).</p><img src="/2024/02/24/Designing-Data-Intensive-Applications-Note-6/cover.png" class="" title="ML_note"><span id="more"></span><h3 id="Secondary-Index"><a href="#Secondary-Index" class="headerlink" title="Secondary Index"></a>Secondary Index</h3><ul><li>The mapping may not be unique, i.e., we may have multiple rows mapped to the same key in secondary index. This can be solved by either<br>[1] making each value in that key to be <strong>list</strong> of matching row identifiers, or by<br>[2] making each key <strong>unique</strong> (adding a row identifer to the key).</li><li>May help with <strong>Join</strong>.</li></ul><h3 id="Storing-values-within-the-index"><a href="#Storing-values-within-the-index" class="headerlink" title="Storing values within the index"></a>Storing values within the index</h3><p>In index, the key can just be the search keyword, but the value can be:<br>[1] <strong>Actual row</strong> (or document &#x2F; vertex) in question<br>[2] <strong>Reference</strong> to the row stored elsewhere<br>(That file is called <strong>heap</strong> file, in no particular order).</p><p>Heap file helps: </p><ol><li>Eliminating <strong>duplication</strong>, because if there lies multiple secondary index, only the reference of data is stored; </li><li>Efficient at <strong>overwriting</strong> values, if new value is no larger than the old value.</li></ol><p>Otherwise, the new value needs to be moved to somewhere with enough space. In that case, we can either update all secondary indexes to be updated to point to the new location, or leave a <strong>forwarding pointer</strong> in place.</p><ul><li><strong>Clustered index</strong>: If extra hop from index to heap file is too much of performance penalty for reads, we can directly store <strong>indexed row</strong> within index.<br>E.g., In MySQL’s <strong>InnoDB</strong> engine, the <strong>primary key</strong> of a table is always a clustered index and secondary indexes refer to the primary key. In SQL server, you can specify <strong>one</strong> clustered index per table.</li><li><strong>Compromise between clustered &#x2F; non-clustered index</strong>: covering index or index with included columns, only stores some of the columns within the index.     Both cluster and covering index can speed up the reads but require more storage &#x2F; write overhead.</li></ul><hr><h3 id="Multi-column-indexes"><a href="#Multi-column-indexes" class="headerlink" title="Multi-column indexes:"></a>Multi-column indexes:</h3><ul><li>Can help querying <strong>multiple</strong> columns of a table</li><li>Trivial implementation can be <strong>concatenated index</strong>, which concat multiple columns. This is not very flexible as it is hard coded, you can hardly search by some column’s <strong>sub-pattern</strong>.</li><li>Standard B-Tree &#x2F; LSM-tree index can hardly answer quries which <strong>restrict range</strong> on multiple columns.</li><li><strong>Implementation</strong> 1: To translate a multi-dimensional location into a single number using a <strong>space-filling curve</strong>, and then to use a regular B-tree index</li><li><strong>Implementation</strong> 2: A better choice is using spatial indexes like <strong>R-trees</strong>.</li></ul><h3 id="R-Tree-R-for-Rectangle"><a href="#R-Tree-R-for-Rectangle" class="headerlink" title="R-Tree (R for Rectangle):"></a>R-Tree (R for Rectangle):</h3><ul><li>An generalization of B-tree to <strong>high-dim</strong> space. Used <strong>Minimal Bounding Rectangle</strong> (MBR) for space dividing. That is to say, just like segment tree, we want a spatial “rectangle tree” so that given a node, we can get the sub-nodes &#x2F; leaves belonging to that region. This tree can be <strong>multi-way</strong>.</li></ul><img src="/2024/02/24/Designing-Data-Intensive-Applications-Note-6/1.png" class="" title="ML_note"><ul><li><strong>Structure of leaf nodes</strong>: leaf nodes are saved as tuples of <code>(l, tuple-identifier)</code>. Where <code>tuple-identifer</code> is a n-dim vector (can be realized as a single record &#x2F; data point) and <code>l</code> is a n-dim rectangle which can exactly include all the datapoints belonging to this leaf node.</li><li><strong>Stucture of non-leaf nodes</strong>: <code>(l, child-pointer)</code>, where <code>l</code> is the n-dim rectangle, and <code>child-point points</code> to the child node(s).</li></ul><p><strong>Variants of R-tree</strong>: </p><ol><li><strong>R* tree</strong> (use <strong>re-insertion</strong> to reduce overlap and improve query performance, using a combination of a revised <strong>node split</strong> algorithm and the concept of forced reinsertion at node overflow).<br>R-tree is sensitive to the order of insertion, thus we can improve it by: when a node overflows, a portion of its entries are removed and reinserted, limited to <strong>one</strong> time to avoid indefinite <strong>cascade re-insertion</strong>.)</li><li><strong>R+ Tree</strong>: a tradeoff betweem <strong>R-tree</strong> and <strong>kd-tree</strong>. If needed, insert an object into multiple leaves to avoid overlapping of internal nodes. Minimal coverage reduces the amount of “dead space” (empty area), and reduces the set of search path to the leaves.<br>The difference is that R+ Tree is not guaranteed to be at least <strong>half-filled</strong>, the entries of any internal node do not overlap, and an object ID maybe stored in more than one leaf node.</li></ol><hr><h3 id="Full-text-search-and-fuzzy-indexes"><a href="#Full-text-search-and-fuzzy-indexes" class="headerlink" title="Full-text search and fuzzy indexes:"></a>Full-text search and fuzzy indexes:</h3><ul><li><strong>Motivation</strong>: Instead of search the exact value, we want to allow search for <strong>similar</strong> keys like mis-spelled words.</li><li><strong>Full text search</strong>: Include <strong>synonyms</strong> of the word, to ignore <strong>grammatical variations</strong> of<br>words, and to search for occurrences of words <strong>near</strong> each other in the same document, and support various other features that depend on linguistic analysis of the text.</li><li><strong>Lucene</strong> allows searching of words within a certain <strong>edit distance</strong>, using quasi-SSTable structure to get the <strong>offset</strong> in the sorted file where they need to look for the candidate keys.   </li><li>In <strong>LevelDB</strong>, this in-memory index is a sparse collection of some of the keys, but in <strong>Lucene</strong>, the in-memory index is a <strong>finite state automaton</strong> over the characters in the keys, similar to a <strong>trie</strong>.</li></ul><h3 id="Keep-everything-in-memory-persistent-memory-storage"><a href="#Keep-everything-in-memory-persistent-memory-storage" class="headerlink" title="Keep everything in memory (persistent memory storage?):"></a>Keep everything in memory (persistent memory storage?):</h3><ul><li>Disks are <strong>cheaper</strong> and <strong>durable</strong> (will <strong>NOT</strong> lose data when power off), but needs to be laid out carefully for good performance on reads and writes.</li><li>Now memory is cheaper, and for use cases like <strong>Memcached</strong>, data loss is acceptable. For durability required by database, we can <strong>log</strong> changes to disk, <strong>replicating</strong> in-memory state to other machines, this also helps with back-up &#x2F; inspection and analysis.</li><li>Memory databases are faster because they can avoid the overheads of <strong>encoding</strong> in-mem data to disk form. They can also provide <strong>priority queques</strong> and <strong>sets</strong> (by <strong>Redis</strong>, e.g.) which are hard to implement on disk.</li><li>We can actually support memory database which requires more space than the memory (overlay?) When there is no space, <strong>evict</strong> content to disk, when needed, <strong>load</strong> it back. It is like OS memory management (swap) but in <strong>record granularity</strong>.</li></ul>]]>
    </content>
    <id>https://umiao.github.io/2024/02/24/Designing-Data-Intensive-Applications-Note-6/</id>
    <link href="https://umiao.github.io/2024/02/24/Designing-Data-Intensive-Applications-Note-6/"/>
    <published>2024-02-24T17:34:35.000Z</published>
    <summary>Discussion on Indexes and Multi-Column Index solution (R-Tree).</summary>
    <title>Designing Data-Intensive-Applications-Note-6</title>
    <updated>2026-03-10T01:09:07.007Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Designing Data-Intensive-Applications" scheme="https://umiao.github.io/tags/Designing-Data-Intensive-Applications/"/>
    <content>
      <![CDATA[<p>Discussion on Storage structures.</p><img src="/2024/02/24/Designing-Data-Intensive-Applications-Note-5/cover.png" class="" title="ML_note"><span id="more"></span><h3 id="Storage-and-Retrieval"><a href="#Storage-and-Retrieval" class="headerlink" title="Storage and Retrieval:"></a>Storage and Retrieval:</h3><p>Real life DB may need <strong>concurrency control</strong>, <strong>reclaiming</strong> disk space so that log does not grow forever, handling <strong>errors</strong> and <strong>partially</strong> written records.</p><ul><li><strong>Log</strong>: an append-only sequence of records.</li><li><strong>Index</strong>: additional structure that is derived from the primary data (faster read, slower write)</li><li><strong>Hash Indexes</strong>: basic implementation is to use in memory hash map to store byte offset (then you can generalize memory hash map into disk hash).</li></ul><h3 id="Log-run-out-of-space"><a href="#Log-run-out-of-space" class="headerlink" title="Log run out of space:"></a>Log run out of space:</h3><p>To cope with such risk, we can: </p><ul><li>Break log into <strong>segments</strong> of a certain size, </li><li>Closing a segment file when it reaches certain size</li><li>Make subsequent writes to new segment file.<br>We can do <strong>compaction</strong> on segments to keep only the <strong>most recent</strong> data (this may even allow us to compress <strong>multiple</strong> segments into one).</li></ul><hr><h3 id="Good-Practice-in-storage"><a href="#Good-Practice-in-storage" class="headerlink" title="Good Practice in storage:"></a>Good Practice in storage:</h3><ol><li>Store file as <strong>binary</strong> format, first encode length of a string in bytes, followed by the raw string </li><li>Consider <strong>tombstone</strong> (special delete record) to avoid really deleting records (“delete” execute on read time). </li><li><strong>Crash recovery</strong>: in memory cache will lost, we can consider storing a <strong>snapshot</strong> of each segment’s hash map on <strong>disk</strong>, which can be loaded into memory more quickly. </li><li><strong>Partially written result</strong>: we can keep <strong>checksums</strong> to detect corrupted parts. </li><li><strong>Concurrency control</strong>: we need the log to be strictly <strong>ordered</strong>, so better have <strong>one</strong> writer thread only. </li><li><strong>Reason of Append Only Design</strong>: Appending and segment merging are sequential write operations, which are generally much faster than random writes, especially on <strong>magnetic spinning-disk hard drivesor</strong> even <strong>flash-based solid state drives</strong> (SSDs).<br>Crash recovery will also be easier as values will not be partially overwritten.<br>Also we can avoid <strong>fragmented</strong> data file.</li></ol><p><em><strong>Shortcomings of hash table index</strong></em>: </p><ul><li>It relies on memory, and performs poor on range queries. </li><li>You have to go through <strong>all</strong> keys.</li></ul><hr><h3 id="SSTables-and-LSM-Trees"><a href="#SSTables-and-LSM-Trees" class="headerlink" title="SSTables and LSM-Trees:"></a>SSTables and LSM-Trees:</h3><ul><li>If we further require writes of segment files to be <strong>sequential</strong> (sorted by key), then it is a <strong>Sorted String Table</strong> (SSTable).</li><li><strong>Advantages over hash indexes</strong>: Easier to merge segments as they are ordered (applicable to <strong>merge sort</strong>). </li><li><strong>Tiebreak</strong>: if multiple keys exist, we only keep the latest record. (similar as the idea of using timestamp &#x2F; ItemID as additional rank keyword)</li><li><strong>Sparse Index</strong>: Now that we do not need to keep all keys’ indexes, as we can use similar idea of <strong>interpolation</strong>. As the storage is now ordered, we only need to find out the <strong>interval</strong> where a key might exist. Maybe we can keep an index every few KBs (like paging). Also it is possible to compress multiple keys into a <strong>compressed block</strong>. To save disk space &#x2F; IO cost.</li><li><strong>Maintenance</strong>: it is possible to maintain SSTables in disk (using <strong>B-Trees</strong>) but it is easier using memory (called <strong>memtable</strong>) with <strong>red-black trees</strong> &#x2F; <strong>AVL trees</strong>.</li></ul><img src="/2024/02/24/Designing-Data-Intensive-Applications-Note-5/1.png" class="" title="ML_note"><hr><h3 id="Memtable"><a href="#Memtable" class="headerlink" title="Memtable"></a>Memtable</h3><ul><li>When memtable gets bigger than threshold (a few MBs), write it to disk as an <strong>SSTable</strong> file, and the new SSTable file becomes the most recent segment of database. </li><li>While writing to disk, writes can continue to a <strong>new memtable instance</strong>.   </li><li>When serving reads, try to find key in the memtable, then in <strong>reversed order</strong> to check on-disk segments (check the latest ones first)   </li><li>Run <strong>merging</strong> and <strong>compaction</strong> process from time to time to merge segment files, and remove out-dated values.</li></ul><p><strong>Recovery of memtable</strong>: memory is not persistent, so keep a separate <strong>log</strong> on disk to record every write to restore crashed memtable (<strong>discard</strong> records after such restoration).</p><hr><h3 id="LSM-Tree-details-and-optimization"><a href="#LSM-Tree-details-and-optimization" class="headerlink" title="LSM-Tree: details and optimization:"></a>LSM-Tree: details and optimization:</h3><ol><li>MemTable resides in memory, is <strong>NOT</strong> persistent, and ordered by key. Can enhance reliability by <strong>Write-Ahead Logging</strong> (WAL).</li><li>Memtable turn into <strong>Immutable</strong> Memtable after reaching certain size, and no longer writable. New memtable will handle write request, and Immutable Memtable is <strong>pending transit to SSTable</strong>.</li><li>SSTable is LSM-Tree’s data structure in disk. Can be optimized using <strong>bloom-filter</strong> and <strong>indexes</strong> on key. Note that the updating of disk is journal &#x2F; log like, and the write is also in order. The write performance is improved, however there lies <strong>redundancy</strong> due to out-dated data (that is why we need compact).  Also query needs to be in <strong>reverse order</strong> as that is the latest.</li></ol><h3 id="Compact-Strategy"><a href="#Compact-Strategy" class="headerlink" title="Compact Strategy:"></a>Compact Strategy:</h3><p>Because we have <strong>amplified</strong> read &#x2F; write &#x2F; space, that is to say, we are reading (two level read of MemTable &#x2F; SSTable) &#x2F; writing (Compact may be triggered so we are writing more) &#x2F; storing (due to redundancy) <strong>more</strong> data than actually needed.</p><p><strong>Size-tiered</strong>: Each tier of SSTable has similar size, and <strong>restrict number</strong> (N) of SSTable in each tier. If the number of SSTables reach N, then compact them and pass to next tier.<br>(This will result in <strong>huge</strong> SSTable in deeper level)<br>(Also for same tier SSTables, each key may has multiple records)</p><p><strong>Leveled</strong>: Each level restricts the <strong>gross size</strong> of SSTables, and in the same level, slice data into SSTables with similar sizes. However, the SSTables are guranteed to be <strong>globally ordered</strong> so only stored once.<br><strong>Strategy</strong>: if a level exceeds the limit on size, then take out 1 SSTable, merge it with next level SSTables which have intersection. (This process can be recursive if still exceeds the size limit)</p><hr><h3 id="Make-LSM-tree-out-of-SSTables"><a href="#Make-LSM-tree-out-of-SSTables" class="headerlink" title="Make LSM-tree out of SSTables:"></a>Make LSM-tree out of SSTables:</h3><ul><li>This is used by key-value storage engine like <strong>LevelDB</strong> and <strong>RocksDB</strong>.</li><li>Known as <strong>Log-Structured Merge-Tree</strong> (LSM-tree).</li></ul><p><strong>Instance</strong>: <strong>Lucene</strong> (mapping from term to postings kept in SStable-like sorted files), is an indexing engine for full-text search used by <strong>Elasticsearch</strong> and <strong>Solr</strong>. It uses a similar method for storing its term dictionary.<br>The mentioned full-textr search engine actually stores all links related to a keyword as key, with list of resources as the value.</p><hr><h3 id="B-Tree"><a href="#B-Tree" class="headerlink" title="B-Tree"></a>B-Tree</h3><ul><li>Standard index implementation in <strong>almost all</strong> relational databases</li><li>Instead of break database into variable-size segments and always write sequentially, B-Tree break into <strong>fixed-size</strong> blocks &#x2F; pages (traditionally 4KB) and read or write one page at a time. It is more close to hardware, and each page can be identified using an address, stored on disk.<h4 id="Implementation"><a href="#Implementation" class="headerlink" title="Implementation"></a>Implementation</h4></li><li>Starting from the root, keep going down to narrow the range of indexes and eventually check existence.</li><li>The number of references to child pages in one page of the B-tree is called the <strong>branching factor</strong>, usually a several hundred.</li><li>To update a key, search for leaf page containing that key and write back. </li><li>To add new key, find the page whose range <strong>encompasses</strong> the new key and add it to the page. If there is not enough space, split the page into 2 <strong>half-full</strong> pages. </li><li>This ensures that the tree is <strong>balanced</strong>, has a depth of $O(\log n)$. Most database can fit in a depth of 3 – 4. (A four-level tree of 4 KB pages with a branching factor of 500 can store up to 256 TB)</li></ul><img src="/2024/02/24/Designing-Data-Intensive-Applications-Note-5/2.png" class="" title="ML_note"><ul><li><strong>Comparing with LSM-Tree</strong>: B-Tree’s rewrite is <strong>in-place</strong> and can be viewed as happening at <strong>hardware level</strong>, rather than on log level.<h4 id="Robustness"><a href="#Robustness" class="headerlink" title="Robustness"></a>Robustness</h4></li><li>If you <strong>split</strong> a page because an insertion caused it to be overfull, you need to write the two pages that were split, and also overwrite their <strong>parent page</strong> to update the references to the two child pages. This is a <strong>dangerous</strong> operation, because if the database crashes after only some of the pages have been written, you end up with a corrupted index (e.g., there may be an orphan page that is not a child of any parent).      </li><li><strong>Solution</strong>: Include an additional data structure of <strong>Write-Ahead Log</strong> (WAL) (<strong>redo log</strong>), which is append-only file which records every B-tree modification <strong>before</strong> it applied to the pages. This can restore B-Tree to a consistent state.</li><li>Also,  if multiple threads want to access B-Tree, we will need <strong>lightweight locks</strong> for consistency, which is more complex than log-based structure.</li></ul><h4 id="Optimizations"><a href="#Optimizations" class="headerlink" title="Optimizations"></a>Optimizations</h4><ol><li>Can replace WAL with a <strong>copy-on-write</strong> scheme. That is a modified page is written to a different location, and a new version of the parent pages in the tree is created to <strong>point at</strong> the new location. This is also useful for concurency control. </li><li>We can save space in pages by <strong>abbreviating</strong> the keys, as we only need enough information to act as <strong>boundaries</strong> between key ranges (e.g., <strong>omit</strong> common prefixs).  Thus we can have higher branching factor and fewer levels. </li><li>We can try to layout the tree that leaf pages appear in <strong>sequential</strong> order in disk to save disk seeking time. However, it is difficult. For LSM-trees, such locality is easier as it rewrites large segments during merging.</li></ol><h4 id="Comparison-with-LSM-Tree"><a href="#Comparison-with-LSM-Tree" class="headerlink" title="Comparison with LSM-Tree:"></a>Comparison with LSM-Tree:</h4><ul><li>LSM-tree is faster in <strong>write</strong>, and B-Tree is faster in <strong>read</strong>.</li><li>B-tree must do write twice: once to <strong>WAL</strong> (Write-Ahead Log) and once to the tree page itself (need more when split is needed). Need to change a page at <strong>minimum</strong>. Some storage engines even overwrite the same page twice in order to avoid ending up with a partially updated page in the event of a power failure. </li><li>Log-structured indexes also write data multi-times due to repeated compaction &amp; merging (<strong>write amplification</strong>), impacting SSD; disk bandwidth is also limited and may cause <strong>fragmentation</strong>.</li><li>LSM-tree usually has smaller write amplification as it do not have to overwrite several pages in the tree. Also, <strong>magnetic hard drive</strong>’s sequential writes is much faster than random writes, giving LSM-tree more advantage.<br>However, the compaction can impact read-write performance (due to limited <strong>disk bandwidth</strong>), even storage engines try to perform compaction incrementally and without affecting concurrent access.<br>Initial write and compaction will share the same bandwidth. As database become larger, the compaction is less effective and may be the <strong>bottleneck</strong> of performance &#x2F; use up the disk space (write is usually not restricted in SSTable-based storage).<br>For high-percentile, LSM-tree can have <strong>high response time</strong> and less predictable.</li><li>Many SSD firmware will use log-structured algo to turn random writes into sequential write.</li></ul>]]>
    </content>
    <id>https://umiao.github.io/2024/02/24/Designing-Data-Intensive-Applications-Note-5/</id>
    <link href="https://umiao.github.io/2024/02/24/Designing-Data-Intensive-Applications-Note-5/"/>
    <published>2024-02-24T08:38:40.000Z</published>
    <summary>Discussion on Storage structures.</summary>
    <title>Designing Data-Intensive-Applications-Note-5</title>
    <updated>2026-03-10T01:09:07.007Z</updated>
  </entry>
  <entry>
    <author>
      <name>Blur - Shenghui Xu</name>
    </author>
    <category term="Data Science" scheme="https://umiao.github.io/categories/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/categories/Data-Science/Data-System/"/>
    <category term="Data Science" scheme="https://umiao.github.io/tags/Data-Science/"/>
    <category term="Data System" scheme="https://umiao.github.io/tags/Data-System/"/>
    <category term="Designing Data-Intensive-Applications" scheme="https://umiao.github.io/tags/Designing-Data-Intensive-Applications/"/>
    <content>
      <![CDATA[<p>Discussion on data model and query language.</p><img src="/2024/02/23/Designing-Data-Intensive-Applications-Note-4/cover.png" class="" title="ML_note"><span id="more"></span><h3 id="Concept-of-Data-Model-x2F-Query-Language"><a href="#Concept-of-Data-Model-x2F-Query-Language" class="headerlink" title="Concept of Data Model &#x2F; Query Language:"></a>Concept of Data Model &#x2F; Query Language:</h3><p>We have the questions to answer:<br>-We want to build <strong>abstraction</strong>, one data model layer on top of another, but we need to know how one layer is represented by below layer.<br>-<strong>Data modeling</strong> &#x2F; <strong>API assignment</strong>. How to model the data, <strong>JSON</strong> &#x2F; <strong>XML</strong>? <strong>Table</strong> in DB? <strong>Graph Model</strong>?</p><hr><h3 id="Relational-Model-v-s-Document-Model"><a href="#Relational-Model-v-s-Document-Model" class="headerlink" title="Relational Model v.s. Document Model:"></a>Relational Model v.s. Document Model:</h3><ul><li><strong>Relational</strong>: Data organized into <strong>relations</strong> (called tables in SQL) and each relation is an <strong>unordered collection of tuples</strong> (rows in SQL). Hide details behind cleaner interface.</li><li><strong>Document</strong>: NoSQL, retrieve document by <strong>name</strong>, each stores information of one <strong>object</strong> and any of its related metadata.<br>Examples: <strong>JSON</strong>, <strong>BSON</strong>, <strong>XML</strong>.<br>A <strong>collection</strong> is a group of documents.</li></ul><p>Driving Forces behind NoSQL (Maybe <strong>not only SQL</strong> is more accurate):<br>-Greater <strong>scalability</strong>, like very large dataset or very high write throughput<br>-Preference for <strong>free</strong> and <strong>open source</strong> software over commercial database products<br>-<strong>Specialized</strong> query operations not well supported by relational DB<br>-Frustration with the restrictiveness of <strong>relational schemas</strong>, and a desire for a more dynamic and expressive data model</p><p><strong>Shortcomings</strong> of document model: In document model, you cannot directly refer to some <strong>nested</strong> items (because it is not stored independently). Thus we should not use it when document structure is too complex &#x2F; nesting is too deep. Also it has poor support for <strong>Join</strong>.</p><hr><h3 id="Object-Relational-Mismatch"><a href="#Object-Relational-Mismatch" class="headerlink" title="Object-Relational Mismatch:"></a>Object-Relational Mismatch:</h3><p>Usually, an awkward <em><strong>translation layer</strong></em> is required between the objects in the Relational Model (tables) Versus Document Model (sometimes named as <strong>impedance mismatch</strong>)</p><p><strong>Object-Relational Mapping</strong> (ORM) like <strong>ActiveRecord</strong> and <strong>Hibernate</strong> can reduce the amount of boilerplate code, but the difference still exists (such mismatch)</p><p><strong>ID</strong> can out-perform pure string in <strong>Many-to-One</strong> &#x2F; <strong>Many-to-Many</strong> mappings.<br>Store fields as IDs and retrieve their values can help:<br>1.Consistent <strong>styling</strong><br>2. <strong>Avoid ambiguity</strong><br>3. Ease of <strong>updating</strong><br>4. <strong>Localization</strong> support<br>5. Better <strong>search matching</strong></p><p>Both stroing by ID &#x2F; storing by string has some sort of <strong>redundancy</strong>. </p><ul><li><p>The previous is for <strong>human</strong>, meaningful info is stored in just one place and everything refers to it use an meaningless ID. </p></li><li><p>For string storage, you are <strong>copying</strong> the same values (might consume more storage).<br>However, the benefit using ID is that you do not have to update ID, and can better enforce consistency when update is needed.</p></li><li><p>In document model DB, such unifying can be hard as you may need to query DB for multiple times (Good at one-many, not good at many-one, many-many).</p></li></ul><hr><h3 id="Conference-on-Data-Systems-Languages-CODASYL-and-the-network-model"><a href="#Conference-on-Data-Systems-Languages-CODASYL-and-the-network-model" class="headerlink" title="Conference on Data Systems Languages (CODASYL) and the network model:"></a>Conference on Data Systems Languages (CODASYL) and the network model:</h3><blockquote><p>Disclaimer: This may be something old and just material for fun</p></blockquote><ul><li>Allowing an item to have <strong>multiple parents</strong> to model many-one &#x2F; one-many</li><li>The link is more like <strong>pointers</strong> (static pointers stored on the disk), follow a path to <strong>traverse</strong> (start from root record)  </li><li>[This may be problematic in <strong>many-many</strong> cases, you need to maintain complex paths of data to update]</li></ul><p><strong>Relational Model</strong>:</p><ul><li>Relation (table) is simply a collection of tuples (rows), you can read any of the rows and select those matching your condition. </li><li>You can insert new row into table without thinking of foreign key issues (this is done in <strong>query time</strong> not in insert time)</li></ul><p><strong>Key insight</strong>: For relational DB, you only need to build a query optimizer <strong>once</strong>, and then all applications that use the database can benefit from it.<br>If you don’t have a query optimizer, it’s easier to hand-code the access paths (a.k.a the <strong>network model</strong>) for a particular query than to write a general-purpose optimizer—but the general-purpose solution wins <strong>in the long run</strong>.</p><p><strong>Document model</strong>: </p><ul><li>Different from network model, as it uses unique identifier (<strong>foreign key</strong> in relational, <strong>document reference</strong> in documental) to link.</li></ul><p>It is possible to use <strong>denormalization</strong> (add <strong>redundant copies</strong>, <strong>grouping</strong> data, etc) to reduce need of join (in relational DB), but it takes extra work to enforce <strong>consistency</strong>.</p><hr><h3 id="Schemaless-x2F-Schema-flexibility-in-Document-Model"><a href="#Schemaless-x2F-Schema-flexibility-in-Document-Model" class="headerlink" title="Schemaless &#x2F; Schema flexibility in Document Model"></a>Schemaless &#x2F; Schema flexibility in Document Model</h3><ul><li>In <strong>JSON</strong>, usually we do not support <strong>enforcing</strong> of any schema (there may be some schema, but not enforced by the DB). We may see any value &#x2F; any key.</li><li><strong>schema-on-read</strong>: the structure of the data is implicit, and only interpreted when the data is read. Just like dynamic type checking rather than static type chekcing.</li><li>This can be tricky for <strong>MySQL</strong>, as it copies the <strong>entire table</strong> when doing <code>alter table</code>.</li></ul><hr><h3 id="Data-Locality-of-queries"><a href="#Data-Locality-of-queries" class="headerlink" title="Data Locality of queries"></a>Data Locality of queries</h3><ol><li>May grant performance advantage if you want to access entire document frequently.<br>(If in multiple tables, then may need more <strong>disk seek time</strong>)</li><li>Unless the modification does not change the <strong>encoded size</strong>, we usually need to rewrite <strong>entire</strong> document when making modifications. (thus should try to keep documents small)</li></ol><p>This idea is beyond document database, also applicable to relational DB. </p><ul><li>Google’s <strong>Spanner database</strong>: offers the same locality properties in a relational data model, by allowing the schema to declare that a table’s rows should be interleaved (nested) within a parent table. </li><li>Oracle: Offers the same using a feature called <strong>multi-table index cluster tables</strong>. </li><li>The column-family concept in the <strong>Bigtable data model</strong> (used in <strong>Cassandra</strong> and <strong>HBase</strong>) has a similar purpose of managing locality.</li><li>More and more relational DB is adding support to JSON (allowing values to be <strong>nested obj</strong>, rather than only trivial values).</li></ul><hr><h3 id="Query-Languages-for-Data"><a href="#Query-Languages-for-Data" class="headerlink" title="Query Languages for Data:"></a>Query Languages for Data:</h3><h4 id="Declarative"><a href="#Declarative" class="headerlink" title="Declarative"></a>Declarative</h4><ul><li>SQL is regarded as <strong>declarative</strong>. </li><li>Just specify the <strong>pattern</strong> of result data you want, what <strong>conditions</strong> to meet, but not about <strong>how</strong> to receive this result. </li><li>Usually order is not guaranteed as well, but it is good for parallelization</li><li>Details are hidden, and optimization can be done <strong>without modifying</strong> the query</li></ul><h4 id="Imperative"><a href="#Imperative" class="headerlink" title="Imperative"></a>Imperative</h4><ul><li><strong>IMS</strong> &#x2F; <strong>CODASYL</strong> query are using <strong>imperative</strong> code. </li><li>Guide device to do certain operations in certain <strong>order</strong>. This can be harder to optimize &#x2F; rewrite on multiple machines</li><li><strong>HTML</strong> is declarative as well. </li><li><strong>Document Object Model</strong> (DOM) API is imperative instead (a bad rewrite using DOM API can be less interactive as it detect conditions &amp; runs only one time).</li></ul><h4 id="Map-Reduce"><a href="#Map-Reduce" class="headerlink" title="Map-Reduce"></a>Map-Reduce</h4><ul><li><strong>Map-Reduce</strong> can be in between of declarative and imperative, consisting of important operations of map (collect) and reduce (fold &#x2F; inject). </li><li>Usually map func provides the <strong>partial result</strong>, and reduce func works on <strong>aggregation</strong>.   </li><li><strong>Restrictions</strong>: they have to be “pure” functions, no additional queries can be done, no side effect should be made (so that they can be executed in any time &#x2F; any order).</li></ul><hr><h3 id="Graph-Like-Data-Model"><a href="#Graph-Like-Data-Model" class="headerlink" title="Graph-Like Data Model:"></a>Graph-Like Data Model:</h3><ul><li>Describe data with <strong>Vertices</strong> and <strong>edges</strong>.</li><li>It is good for, e.g., describing <strong>social network</strong> and <strong>hierarchical</strong> relationships.</li></ul><p><strong>Property Graphs</strong>:  </p><ul><li>Each vertex consists a <strong>unique identifier</strong>, a set of outgoing edges, a set of incoming edges, a collection of properties (K-V pairs).</li><li>Each edge consists of a unique identifier, the vertex which the edge starts, a label to describe the kind of relationship between the two vertices, a collection of properties (K-V pairs).</li></ul><ol><li>Any two vertices can be linked; </li><li>You can find the incoming &#x2F; outgoing edges for a vertex; </li><li>You can use labels for different kinds of relationships, store different kind of info in a single graph. This actually allow us to store data in different <strong>granular</strong>.</li></ol><hr><h3 id="Cypher-Query-Language"><a href="#Cypher-Query-Language" class="headerlink" title="Cypher Query Language:"></a>Cypher Query Language:</h3><p>Demos an example of <strong>creation</strong> of a property graph:</p><img src="/2024/02/23/Designing-Data-Intensive-Applications-Note-4/1.png" class="" title="ML_note">    <p>Example of <strong>query</strong>:</p><img src="/2024/02/23/Designing-Data-Intensive-Applications-Note-4/2.png" class="" title="ML_note"><ul><li>Such query effectively specify an <strong>endpoint</strong> &#x2F; <strong>destination vertex</strong>, and we want to find a <strong>path</strong> to get there. </li><li>We first declare a person should have a “born_in” edge, then we want to keep tracking “within” edge to reach certain country &#x2F; address vertex.</li><li><strong>Application</strong> in SQL: This can be difficult as the number of <strong>“join”s</strong> needed is not clear. You can use <strong>RECURSIVE</strong> syntax (used to enlarge the same table &#x2F; set) for such purpose (but very complex and ineffective).</li></ul><p><strong>Triple-Stores</strong>: </p><ul><li>Basically the same as property graph model, just using different words of: <code>(subject, predicate, object)</code><br>This just like <code>(in_vertex, directional_edge, out_vertex)</code></li></ul><h3 id="SPARQL"><a href="#SPARQL" class="headerlink" title="SPARQL"></a>SPARQL</h3><p>A query language for triple-stores using the RDF (<strong>Resource Description Framework</strong>) data model.<br>It looks like:</p><img src="/2024/02/23/Designing-Data-Intensive-Applications-Note-4/3.png" class="" title="ML_note"><h3 id="Datalog"><a href="#Datalog" class="headerlink" title="Datalog"></a>Datalog</h3><p>Similar triple-stores languages, write as <code>predicate(subject, object).</code><br>See example of decalration as well as query below:</p><img src="/2024/02/23/Designing-Data-Intensive-Applications-Note-4/4.png" class="" title="ML_note"><p>It can also recursively find a person who born in USA and lives in Europe.<br>Note that in above example, rule “within_recursive” can derive <strong>itself</strong>. (You can view the first line of row as “tail recursive”)</p>]]>
    </content>
    <id>https://umiao.github.io/2024/02/23/Designing-Data-Intensive-Applications-Note-4/</id>
    <link href="https://umiao.github.io/2024/02/23/Designing-Data-Intensive-Applications-Note-4/"/>
    <published>2024-02-24T00:38:51.000Z</published>
    <summary>Discussion on data model and query language.</summary>
    <title>Designing Data-Intensive-Applications-Note-4</title>
    <updated>2026-03-10T01:09:07.006Z</updated>
  </entry>
</feed>
